{
  "name": "thes1s-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute thes1s --local --file=schema.sql",
    "db:migrate:remote": "wrangler d1 execute thes1s --remote --file=schema.sql",
    "db:seed": "node scripts/seed.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "@xmldom/xmldom": "^0.9.0",
    "yahoo-finance2": "^3.13.2",
    "zod": "^3.25.0"
  }
}
