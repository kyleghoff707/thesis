---
phase: quick-60
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/generate-full-story/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Full Story markdown report renders all section titles correctly (no 'undefined' headers)"
    - "All citation formats render as readable numbered references (no 'undefined. undefined: undefined')"
    - "All checklist items render with correct number, question text, verdict, evidence, and confidence"
    - "All red flags render as readable bullet points (no '[object Object]')"
  artifacts:
    - path: ".claude/skills/generate-full-story/SKILL.md"
      provides: "Defensive markdown generation template handling all observed agent output variants"
  key_links:
    - from: "SKILL.md Step 7 markdown template"
      to: "Agent section JSON outputs"
      via: "Field accessor patterns with fallback chains"
      pattern: "title.*sectionTitle|number.*id|item.*question|ref.*source"
---

<objective>
Fix the generate-full-story SKILL.md Step 7 markdown report assembly to handle the polymorphic
output formats that agents actually produce. Currently the template uses hardcoded field accessors
that only match one variant, causing "undefined" text and "[object Object]" throughout the report.

Purpose: The SFM Full Story run proved that 5 different agents produce 4 different citation formats,
2 different red flag formats, 2 different checklist item field name conventions, and inconsistent
title field usage. The markdown template must defensively handle all observed variants.

Output: Updated SKILL.md with explicit defensive formatting instructions in Step 7.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.claude/skills/generate-full-story/SKILL.md
@src/schemas/reportSection.js
@agents/orchestrator/schemas/checklist-item.schema.json

<interfaces>
<!-- Observed agent output variants from SFM run — the template must handle ALL of these -->

Section title fields (varies by agent):
- S1: `title: "Event Analysis"`, `sectionTitle: null`
- S2: `title: null`, `sectionTitle: "Section 2: Meaning — Do You Understand This Business?"`
- S3: `title: "Moat Checklist — 15-Point Competitive Advantage Assessment"`, `sectionTitle: null`
- S4: `title: "Management Assessment — 13-Point Checklist"`, `sectionTitle: null`
- S5: `title: "Valuation Confirmation"`, `sectionTitle: null`

Citation formats (4 variants across 5 agents):
- Canonical (S1, S3): `{id: 1, ref: "Source Name", text: "quoted text", source: "URL"}`
- String-only (S2): `"[1] Description — url"`
- Alt-object (S4): `{id: 1, source: "DataPacket — companyInfo", detail: "description", type: "primary"}`
- Full-object (S5): `{id: 1, source: "Yahoo Finance", title: "SFM Valuation", url: "https://...", detail: "desc"}`

Red flag formats (2 variants):
- String (S1, S2): `"Revenue concentration: 100% retail grocery..."`
- Object (S3, S4, S5): `{severity: "HIGH", flag: "Narrow moat category", detail: "...", resolution: "..."}`

Checklist item fields (2 naming conventions):
- Canonical (S3, S4): `{number: 1, item: "Can you identify the primary moat type?", verdict, evidence, confidence}`
- Alt (S2): `{id: 1, question: "Can you describe the business model?", verdict, evidence, confidence}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace vague Step 7 markdown template with defensive formatting rules</name>
  <files>.claude/skills/generate-full-story/SKILL.md</files>
  <action>
In the SKILL.md file, replace the Step 7 markdown template (the ```markdown block starting at approximately line 443 through line 516) with an updated template that includes explicit defensive formatting instructions. The changes are:

**1. Section title fallback chain (fixes "## Section 2: undefined"):**

Replace each section header pattern `## Section {N}: {title}` with explicit instruction:

```
For each section's header, resolve the display title using this fallback chain:
  displayTitle = section.title
                 || section.sectionTitle (strip any leading "Section N: " prefix)
                 || derive from sectionKey: event_analysis->"Event Analysis",
                    meaning_checklist->"Meaning Checklist", moat_checklist->"Moat Checklist",
                    management_checklist->"Management Checklist",
                    valuation_confirmation->"Valuation Confirmation"
```

The header line becomes: `## Section {N}: {displayTitle}`

**2. Citations defensive formatting (fixes "undefined. undefined: undefined (undefined)"):**

Replace `{citations as numbered list}` with explicit multi-format handling:

```
For each citation in section.citations, detect format and render:

  If citation is a plain string:
    Output the string as-is (it already contains numbering and formatting)

  If citation is an object with `ref` and `text` fields (canonical schema):
    Output: "{id}. {ref}: {text} ({source})"

  If citation is an object with `source` and `detail` fields (but no `ref`):
    Output: "{id}. {source}: {detail}"

  If citation is an object with `title` and `detail` fields:
    Output: "{id}. {title} — {detail} ({url || source})"

  Fallback for any other object shape:
    Output: "{id || index+1}. {first-string-valued-field}: {second-string-valued-field}"
```

**3. Checklist table rows defensive formatting (fixes "undefined |  | PASS"):**

Replace `{each checklist item as a table row, parsed from data field}` with explicit field resolution:

```
Parse section.data as JSON. For each item in data.items:
  itemNumber = item.number || item.id || (index + 1)
  itemText = item.item || item.question || "—"
  Truncate evidence to first 120 chars if longer, append "..."

  Output table row: | {itemNumber} | {itemText} | {item.verdict} | {evidence} | {item.confidence} |
```

**4. Red flags defensive formatting (fixes "[object Object]"):**

Replace `{redFlags as bullet list}` with explicit multi-format handling:

```
For each redFlag in section.redFlags:

  If redFlag is a plain string:
    Output: "- {redFlag}"

  If redFlag is an object:
    flag = redFlag.flag || redFlag.finding || redFlag.description || JSON.stringify(redFlag)
    severity = redFlag.severity ? " [{severity}]" : ""
    Output: "- {flag}{severity}"
```

**5. Apply all four defensive patterns to every section in the template.**

Make sure the Sections 1 and 5 (non-checklist) citation blocks use the defensive citation format.
Make sure Sections 2, 3, and 4 (checklist) citation blocks ALSO use the defensive citation format (the current template only shows citations for S1 and S5, but checklist sections also have citations).
Make sure ALL sections' red flag blocks use the defensive red flag format.

Keep all other parts of the Step 7 template unchanged (the JSON assembly, checklist score extraction, report metadata, etc.).
  </action>
  <verify>
    <automated>grep -c "undefined" .claude/skills/generate-full-story/SKILL.md | head -1</automated>
    Verify the updated template no longer contains bare `{citations as numbered list}` or `{each checklist item as a table row}` placeholders. The word "undefined" should only appear in the context of explaining what to do when a value IS undefined (i.e., in fallback chain descriptions), not as a rendered output.
    Also verify: `grep -c "object Object" .claude/skills/generate-full-story/SKILL.md` returns 0.
    Also verify: `grep "citations as numbered list" .claude/skills/generate-full-story/SKILL.md` returns nothing.
    Also verify: `grep "each checklist item as a table row" .claude/skills/generate-full-story/SKILL.md` returns nothing.
  </verify>
  <done>
    Step 7 markdown template in SKILL.md contains explicit defensive formatting rules for all 4 polymorphic fields (title, citations, checklist items, red flags) with fallback chains that handle every observed agent output variant from the SFM run. No vague placeholders remain.
  </done>
</task>

</tasks>

<verification>
After the edit, manually trace through the SFM section data against the new template rules:
1. S2 (title=null, sectionTitle="Section 2: Meaning...") -> should render "## Section 2: Meaning Checklist" via fallback
2. S2 citations (plain strings) -> should output as-is
3. S4 citations ({id, source, detail, type}) -> should render "{id}. {source}: {detail}"
4. S2 checklist items ({id, question}) -> should render "{id} | {question} | ..."
5. S4 redFlags ({severity, flag}) -> should render "- {flag} [{severity}]"
</verification>

<success_criteria>
The SKILL.md Step 7 markdown template contains zero vague field accessor placeholders. Every field that varies across agents has an explicit fallback chain documented. A re-run of /generate:full-story would produce a markdown report with no "undefined" text and no "[object Object]" text.
</success_criteria>

<output>
After completion, create `.planning/quick/260329-p60-fix-generate-full-story-skill-md-report-/260329-p60-SUMMARY.md`
</output>
