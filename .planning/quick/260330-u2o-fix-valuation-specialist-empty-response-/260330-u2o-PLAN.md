---
quick_id: 260330-u2o
type: quick
description: Fix valuation-specialist empty response for Full Story S5
---

<objective>
Fix silent null section propagation when parsed_output is null (model returns minimal tokens without matching schema). Add guard in dispatchAgent to detect null section and return an error. Also add null filtering in pipelineManager's allSections.push to prevent nulls from accumulating.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add null section guard in dispatchAgent and filter in pipelineManager</name>
  <files>src/engines/aiResearch.js, src/engines/pipelineManager.js</files>
  <action>
    1. In aiResearch.js after line 411 (`const section = retryResult.result;`), add a null guard:
       If section is null/undefined, return with error message instead of silently proceeding.

    2. In pipelineManager.js wave dispatch, guard allSections.push to skip null sections.
  </action>
</task>

</tasks>
