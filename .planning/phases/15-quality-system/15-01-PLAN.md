---
phase: 15-quality-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engines/critic.js
  - src/engines/qualityFormatter.js
  - scripts/run-quality-v4.js
autonomous: true
requirements: [QUAL-01, QUAL-03]

must_haves:
  truths:
    - "critic.js scores all 6 Full Story section types with methodology checks"
    - "Checklist sections (S2, S3, S4) have completeness weights favoring data population over narrative"
    - "Non-standard verdicts (CONTEXT, WATCHLIST) are mapped to PARTIAL for scoring"
    - "Debate section scored by process rigor — honest unresolved risks score higher than rubber-stamp"
    - "qualityFormatter labels Full Story sections with descriptive names including item counts"
    - "run-quality-v4.js can score Full Story output, not just Pitch Deck"
  artifacts:
    - path: "src/engines/critic.js"
      provides: "33 Full Story methodology checks across 6 section keys + helpers"
      contains: "event_analysis:"
    - path: "src/engines/qualityFormatter.js"
      provides: "Full Story section labels"
      contains: "event_analysis:"
    - path: "scripts/run-quality-v4.js"
      provides: "Full Story quality scoring CLI"
      contains: "fullStory"
  key_links:
    - from: "src/engines/critic.js"
      to: "METHODOLOGY_CHECKS"
      via: "6 new section keys in map"
      pattern: "event_analysis.*meaning_checklist.*moat_checklist.*management_checklist.*valuation_confirmation.*inversion_rebuttal"
    - from: "src/engines/critic.js scoreCompleteness"
      to: "QUALITY_WEIGHTS"
      via: "section-type-aware weight selection"
      pattern: "meaning_checklist.*moat_checklist.*management_checklist"
---

<objective>
Add 33 Full Story methodology checks to critic.js covering all 6 section types (event_analysis, meaning_checklist, moat_checklist, management_checklist, valuation_confirmation, inversion_rebuttal), adjust completeness weights for checklist sections, add polymorphic data parsers, update qualityFormatter.js with Full Story labels, and extend run-quality-v4.js to support Full Story scoring.

Purpose: QUAL-01 (methodology checks from curriculum) and QUAL-03 (dual scoring matching Pitch Deck pattern). After this plan, running the quality scorer on any Full Story section produces both mechanical and methodology scores with actionable feedback.

Output: Modified critic.js with 33 checks + 3 helpers, updated qualityFormatter.js with 6 labels, updated run-quality-v4.js with Full Story stage support.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-quality-system/15-CONTEXT.md
@.planning/phases/15-quality-system/15-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/engines/critic.js (methodology check pattern):
```javascript
const METHODOLOGY_CHECKS = {
  company_info: [
    { id: 'radar-event', label: 'Event analysis present', critical: true,
      test: (s) => /event|price\s*drop|catalyst/i.test(s.narrative || '') },
  ],
  // ... each section key maps to an array of {id, label, critical, test} objects
};
```

From src/engines/critic.js (scoring pipeline):
```javascript
export function validateSection(section, dataPacket, options = {}) { ... }
// Calls scoreCompleteness(section) and scoreMethodology(section)
// Returns { sectionKey, score, completeness, issues, passed, methodology, checkedAt }

function scoreMethodology(section) {
  // Looks up METHODOLOGY_CHECKS[section.key] -> runs runMethodologyChecks()
  // Unknown keys return { score: 100, checks: [], passed: true }
}

function runMethodologyChecks(section, checks) {
  // Critical weight 2, supplementary weight 1
  // score = passedWeight / totalWeight * 100, passed = score >= 50
}
```

From src/engines/critic.js (completeness weights):
```javascript
const QUALITY_WEIGHTS = {
  requiredFields: 40,
  narrativeDepth: 25,
  citationDensity: 20,
  dataPopulation: 15,
};
```

From src/engines/critic.js (test exports):
```javascript
export const _testExports = {
  classifyCitation, resolveDataPath, matchNumericValue, validateCitations,
  scoreCompleteness, validateConfidence, checkMultiSource, validateRedFlags,
  detectDataGaps, checkSearchCompliance, scoreMethodology, METHODOLOGY_CHECKS,
};
```

Real SFM section keys: event_analysis, meaning_checklist, moat_checklist, management_checklist, valuation_confirmation, inversion_rebuttal

SFM S2 (meaning_checklist) data field structure (JSON string):
```json
{"checklistType":"meaning","items":[{"id":1,"question":"...","verdict":"PASS","confidence":"HIGH","evidence":"..."},...]}
```

SFM S3 (moat_checklist) data field structure (JSON string):
```json
{"checklistType":"moat","items":[{"number":1,"item":"...","verdict":"PASS","evidence":"...","confidence":"HIGH"},...]}
```

SFM S4 (management_checklist) verdicts: PASS, PARTIAL, FAIL, CONTEXT, WATCHLIST

SFM S6 (inversion_rebuttal) data field structure (JSON string):
```json
{"debateStructure":{"totalExchanges":9,"strongBull":1,"strongBear":2,"unresolved":5,"mixed":0,"lean":1},"judgeOverallVerdict":{"direction":"Mixed","unresolvedCount":5,"investmentImplication":"..."}}
```

SFM S6 citations format: array of objects with {id, ref, text, source} where source contains URLs
SFM S2 citations format: array of plain strings "[1] description — url"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add helpers, completeness weight adjustment, and 33 methodology checks to critic.js</name>
  <files>src/engines/critic.js</files>
  <read_first>
    - src/engines/critic.js (full file — understand existing METHODOLOGY_CHECKS pattern, scoreCompleteness, scoreMethodology, _testExports)
    - .planning/phases/15-quality-system/15-CONTEXT.md (full 33-check inventory with exact IDs, labels, critical flags per section)
    - .planning/phases/15-quality-system/15-RESEARCH.md (code examples for parseChecklistData, normalizeVerdict, parseDebateData, weight adjustment)
    - .thes1s/reports/SFM/sections/fullStory-S2-meaning_checklist.json (real data — verify checklist field names: id, question, verdict, confidence, evidence)
    - .thes1s/reports/SFM/sections/fullStory-S3-moat_checklist.json (real data — verify field names: number, item, verdict, evidence, confidence)
    - .thes1s/reports/SFM/sections/fullStory-S4-management_checklist.json (real data — verify non-standard verdicts CONTEXT, WATCHLIST)
    - .thes1s/reports/SFM/sections/fullStory-S6-inversion_rebuttal.json (real data — verify debateStructure, judgeOverallVerdict, citation format)
    - .thes1s/reports/SFM/sections/fullStory-S1-event_analysis.json (real data — verify narrative content for regex pattern testing)
    - .thes1s/reports/SFM/sections/fullStory-S5-valuation_confirmation.json (real data — verify narrative content)
  </read_first>
  <action>
Add the following to src/engines/critic.js in this order:

**1. Helper functions (add after the `checkSearchCompliance` function, before the methodology scoring section):**

a) `normalizeVerdict(verdict)` — Maps non-standard verdicts to standard ones per D-06:
   - Input: string verdict (may be null/undefined)
   - CONTEXT -> 'PARTIAL', WATCHLIST -> 'PARTIAL'
   - PASS, FAIL, PARTIAL returned unchanged (case-insensitive comparison, return uppercase)
   - null/undefined -> null

b) `parseChecklistData(section)` — Extracts checklist items from section.data with polymorphic field fallback per D-02:
   - Parse `section.data` (may be JSON string or object)
   - Extract `items` array from parsed data
   - Map each item to normalized form: `{ id: item.id || item.number, question: item.question || item.item, verdict: normalizeVerdict(item.verdict), rawVerdict: item.verdict, evidence: item.evidence || '', confidence: item.confidence || null }`
   - Return `{ items: [...], summary: data.summary || null }`
   - On any parse failure, return `{ items: [], summary: null }`

c) `parseDebateData(section)` — Extracts debate structure from section.data:
   - Parse `section.data` (may be JSON string or object)
   - Return parsed object with `debateStructure` and `judgeOverallVerdict` fields
   - On parse failure, return null

d) `flagNonStandardVerdicts(items)` — Returns array of low-severity issue objects per D-06:
   - For each item where rawVerdict is not PASS/FAIL/PARTIAL (case-insensitive), push `{ type: 'methodology', severity: 'low', message: 'Non-standard verdict "${rawVerdict}" on item ${id} -- mapped to PARTIAL', field: 'data.items[${id}]' }`

**2. Completeness weight adjustment (modify `scoreCompleteness` function) per D-03:**

Change the composite calculation to use section-type-aware weights. Add a `sectionKey` parameter or read `section.key`:
- If `section.key` is one of `['meaning_checklist', 'moat_checklist', 'management_checklist']`, use weights: `{ requiredFields: 40, narrativeDepth: 15, citationDensity: 20, dataPopulation: 25 }` (swapped narrativeDepth 25->15 and dataPopulation 15->25, total stays 100)
- For all other sections (including debate and standard), use existing `QUALITY_WEIGHTS` unchanged
- The ONLY change is swapping the two weight values for checklist sections; `requiredFields` stays at 40, `citationDensity` stays at 20

**3. Add 33 methodology checks to METHODOLOGY_CHECKS map (add after existing Pitch Deck entries, before the closing `};`):**

Per the full inventory in 15-CONTEXT.md:

**event_analysis (5 checks):**
- `event-root-cause` (CRITICAL): Test narrative for root cause language — `/root\s*cause|caused\s*(the|by|a)|what\s*caused|trigger(ed|ing)?|precipitat/i`
- `event-historical` (CRITICAL): Test narrative for historical precedent — `/historical|precedent|prior\s*(event|instance|occurrence)|in\s*\d{4}.*similar|previously/i`
- `event-recovery` (supplementary): Test narrative for recovery timeline — `/recover|rebound|timeline|month|quarter|year.*return|bounce\s*back|time\s*to\s*recover/i`
- `event-debt` (supplementary): Test narrative for debt implications — `/debt|leverage|interest\s*(coverage|expense)|balance\s*sheet.*stress|liabilit/i`
- `event-analyst` (supplementary): Test narrative for analyst sentiment — `/analyst|consensus|wall\s*street|street\s*estimate|price\s*target|rating|upgrade|downgrade/i`

**meaning_checklist (5 checks):**
- `meaning-item-count` (CRITICAL): `parseChecklistData(s).items.length >= 15`
- `meaning-all-verdicts` (CRITICAL): All items have non-null, non-empty verdict after normalization: `items.length > 0 && items.every(i => i.verdict != null && i.verdict !== '')`
- `meaning-evidence-present` (CRITICAL): Every item has evidence > 10 chars: `items.length > 0 && items.every(i => (i.evidence || '').length > 10)`
- `meaning-radar-items` (supplementary): Items with id/number 1-3 have non-PARTIAL verdicts: check first 3 items, at least 2 have verdict === 'PASS' or verdict === 'FAIL'
- `meaning-kpi-numeric` (supplementary): At least one item's evidence contains a number (percentage or dollar): `/\d+(\.\d+)?%|\$\d+/`.test on any item's evidence

**moat_checklist (6 checks):**
- `moat-item-count` (CRITICAL): `parseChecklistData(s).items.length >= 15`
- `moat-all-verdicts` (CRITICAL): All items have verdict (same pattern as meaning)
- `moat-evidence-present` (CRITICAL): Every item has evidence > 10 chars
- `moat-type-identified` (CRITICAL): At least one item's evidence names a specific moat type — `/brand|switching\s*cost|toll\s*bridge|price\s*advantage|trade\s*secret|patent|network\s*effect/i` on any item's evidence
- `moat-durability` (supplementary): Narrative mentions 10+ year assessment — `/10.?year|20.?year|decade|long.?term|durab|sustain|endur/i` on narrative
- `moat-replicability` (supplementary): Any item's evidence addresses replicability — `/replic|barrier|difficult\s*to\s*copy|not\s*easily\s*(cop|replac|imitat)|entry\s*barrier/i`

**management_checklist (6 checks):**
- `mgmt-item-count` (CRITICAL): `parseChecklistData(s).items.length >= 13`
- `mgmt-all-verdicts` (CRITICAL): All items have verdict (after normalization — CONTEXT/WATCHLIST become PARTIAL)
- `mgmt-evidence-present` (CRITICAL): Every item has evidence > 10 chars
- `mgmt-financial-numeric` (CRITICAL): At least 3 items' evidence cite actual numbers (contains digits followed by % or $ or million/billion) — count items where `/\d+(\.\d+)?(%|\s*(million|billion|M|B))|\$\d+|ROE|ROIC|ROA/i` matches evidence
- `mgmt-ceo-named` (supplementary): Narrative or any item's evidence contains a proper noun that could be a CEO name — `/[A-Z][a-z]+\s+[A-Z][a-z]+/` in narrative or any evidence string
- `mgmt-insider-pct` (supplementary): Narrative or any item's evidence mentions insider ownership — `/insider\s*(own|hold|stak)|%.*insider|insider.*%|ownership\s*percentage/i`

**valuation_confirmation (5 checks):**
- `val-growth-quality` (CRITICAL): Narrative checks for debt-fueled growth — `/debt.?fuel|organic\s*growth|acqui.*growth|revenue.*growth.*debt|leverage.*growth|growth.*not.*debt/i`
- `val-fgr-rationality` (CRITICAL): Narrative mentions FGR rationality — `/FGR|future\s*growth\s*rate|rule\s*of\s*72|market\s*share\s*ceiling|growth\s*rate.*rational|sanity\s*check/i`
- `val-sensitivity` (CRITICAL): Narrative contains sensitivity/range analysis — `/sensitiv|range|scenario|bear\s*case|bull\s*case|conservative|optimistic|if\s*growth/i`
- `val-multiple-methods` (supplementary): 2+ valuation methods referenced — count matches of `/MOS|margin\s*of\s*safety|PBT|payback\s*time|ten\s*cap|owner\s*earn|equity\s*bond/i` unique method types >= 2
- `val-red-flags` (supplementary): Acquisition/merger red flags — `/acqui|merger|M&A|roll.?up|serial\s*acquir|goodwill|integration\s*risk/i`

**inversion_rebuttal (6 checks):**
- `debate-bull-count` (CRITICAL): >= 5 bull thesis points. First check `parseDebateData(s)` for debateStructure.totalExchanges >= 5. Fallback: count bull point patterns in narrative — `/bull\s*(thesis|point|case)|strength|investment\s*case|bull\s*#?\d/gi` matches >= 5
- `debate-bear-coverage` (CRITICAL): Bear inversion count >= bull count. Use debateStructure.totalExchanges if available (each exchange IS a bear inversion). Fallback: count bear patterns in narrative >= 5
- `debate-bear-citations` (CRITICAL): Web citations present (not just DataPacket). Check `section.citations` array for objects with `source` field containing `https://` OR string citations containing `https://`. At least 3 web citations required.
- `debate-rebuttal-coverage` (CRITICAL): Bull rebuts all bear points. Check narrative for rebuttal language — `/rebut|counter|respond|address|acknowledg/i` present, AND either debateStructure exists OR narrative contains multiple rebuttal instances (>= 3 matches)
- `debate-thesis-killer` (supplementary): At least 1 severe/thesis-killer risk identified — `/thesis.?killer|severe|critical\s*risk|deal.?breaker|red\s*flag.*severe|fatal|unresolved.*severe/i` in narrative
- `debate-honesty` (supplementary): At least 1 honest/weak rebuttal acknowledged — `/weak\s*rebut|honest|acknowledg.*weak|concede|cannot\s*(fully\s*)?rebut|bear\s*(has|makes)\s*a\s*(valid|strong)\s*point|unable\s*to\s*(fully\s*)?counter/i` in narrative

**4. Update _testExports** to include the new helpers:
Add `parseChecklistData`, `normalizeVerdict`, `parseDebateData`, `flagNonStandardVerdicts` to the `_testExports` object.

**5. Important implementation notes:**
- Do NOT modify `runMethodologyChecks()`, `validateSection()`, or `validateStage()` — they are generic and correct per the research.
- Do NOT modify `isExemptSection()` or `EXEMPT_METHODOLOGY_KEYS` — no Full Story sections need exemption per D-07.
- The `scoreCompleteness` function currently uses `QUALITY_WEIGHTS` directly in the composite calculation on line 351-355. Modify it to read `section.key` and select weights conditionally. Keep the existing QUALITY_WEIGHTS constant unchanged — define the checklist weights inline or as a separate constant.
- All regex patterns should use the `i` flag for case-insensitive matching.
- Checklist check test functions should call `parseChecklistData(s)` (not `parseChecklistData(s.data)`) — the helper expects the full section object.
  </action>
  <verify>
    <automated>cd /Users/kylehoff/Desktop/stock-analyzer && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - critic.js contains `function normalizeVerdict(verdict)` that maps CONTEXT to PARTIAL and WATCHLIST to PARTIAL
    - critic.js contains `function parseChecklistData(section)` that returns `{ items, summary }` with fallback chains `item.question || item.item` and `item.id || item.number`
    - critic.js contains `function parseDebateData(section)` that parses section.data and returns object with debateStructure
    - critic.js contains `function flagNonStandardVerdicts(items)` that returns issue array with severity 'low'
    - METHODOLOGY_CHECKS contains keys: `event_analysis`, `meaning_checklist`, `moat_checklist`, `management_checklist`, `valuation_confirmation`, `inversion_rebuttal`
    - event_analysis has 5 checks (2 critical: event-root-cause, event-historical; 3 supplementary)
    - meaning_checklist has 5 checks (3 critical: meaning-item-count, meaning-all-verdicts, meaning-evidence-present)
    - moat_checklist has 6 checks (4 critical: moat-item-count, moat-all-verdicts, moat-evidence-present, moat-type-identified)
    - management_checklist has 6 checks (4 critical: mgmt-item-count, mgmt-all-verdicts, mgmt-evidence-present, mgmt-financial-numeric)
    - valuation_confirmation has 5 checks (3 critical: val-growth-quality, val-fgr-rationality, val-sensitivity)
    - inversion_rebuttal has 6 checks (4 critical: debate-bull-count, debate-bear-coverage, debate-bear-citations, debate-rebuttal-coverage)
    - scoreCompleteness uses `{ requiredFields: 40, narrativeDepth: 15, citationDensity: 20, dataPopulation: 25 }` when section.key is meaning_checklist, moat_checklist, or management_checklist
    - _testExports contains parseChecklistData, normalizeVerdict, parseDebateData, flagNonStandardVerdicts
    - `npm test` passes (all existing 83+ tests still green)
  </acceptance_criteria>
  <done>critic.js contains 33 Full Story methodology checks across 6 section keys, 4 helper functions, completeness weight adjustment for checklist sections, and all new functions exported via _testExports. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update qualityFormatter.js labels and run-quality-v4.js Full Story support</name>
  <files>src/engines/qualityFormatter.js, scripts/run-quality-v4.js</files>
  <read_first>
    - src/engines/qualityFormatter.js (full file — see SECTION_LABELS map and stageTitle function)
    - scripts/run-quality-v4.js (full file — see how it reads pipeline-output.json and passes to validateStage)
    - .thes1s/reports/SFM/sections/fullStory-S1-event_analysis.json (verify section key format)
    - .thes1s/reports/SFM/full-story.json (verify assembled Full Story format — check if it has a .sections array)
  </read_first>
  <action>
**1. Update `src/engines/qualityFormatter.js`:**

Add 6 Full Story section labels to the `SECTION_LABELS` object (after the existing Pitch Deck labels):

```javascript
event_analysis: 'Event Analysis',
meaning_checklist: 'Meaning Checklist (15pt)',
moat_checklist: 'Moat Checklist (15pt)',
management_checklist: 'Management Checklist (13pt)',
valuation_confirmation: 'Valuation Confirmation',
inversion_rebuttal: 'Inversion & Rebuttal (Debate)',
```

Update the `stageTitle` function to handle Full Story:
```javascript
if (stage === 'fullStory' || stage === 'full-story') return 'Full Story';
```

Update the Scoring Methodology section at the bottom to mention checklist weight adjustment:
Change the line starting with `**Mechanical score**` to:
```
**Mechanical score**: 40% required fields, 25% narrative depth, 20% citations, 15% data population. Checklist sections use adjusted weights (15% narrative, 25% data). Penalties for high/medium/low issues.
```

**2. Update `scripts/run-quality-v4.js`:**

Add Full Story support alongside existing Pitch Deck support. The script should:

a) Accept an optional `--stage` flag: `node --loader ./scripts/node-esm-loader.js scripts/run-quality-v4.js SFM --stage fullStory`

b) Parse `--stage` from process.argv (default: auto-detect based on available files)

c) For Full Story stage (`fullStory`):
   - Read individual section files from `.thes1s/reports/{ticker}/sections/fullStory-S*.json` using `readdirSync` + filter for `fullStory-S` prefix
   - OR read from `.thes1s/reports/{ticker}/full-story.json` if it has a `.sections` array
   - Parse each JSON file into a section object
   - Pass all sections to `validateStage(sections, dataPacket)`

d) For auto-detection: check if `full-story.json` or `fullStory-S*` section files exist; if so, default to fullStory. Otherwise default to pitchDeck (existing behavior).

e) Write output to `quality/full-story-v4.quality.json` and `quality/full-story-v4.quality.md` (not overwriting pitch deck quality files)

f) Pass `{ stage: 'fullStory', ticker }` to `formatQualityReport`

The key change: instead of reading a single `pipeline-output.json`, Full Story reads from individual section files. Keep existing Pitch Deck path unchanged (backwards compatible).
  </action>
  <verify>
    <automated>cd /Users/kylehoff/Desktop/stock-analyzer && node --loader ./scripts/node-esm-loader.js scripts/run-quality-v4.js SFM --stage fullStory 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - qualityFormatter.js SECTION_LABELS contains `event_analysis: 'Event Analysis'`
    - qualityFormatter.js SECTION_LABELS contains `meaning_checklist: 'Meaning Checklist (15pt)'`
    - qualityFormatter.js SECTION_LABELS contains `moat_checklist: 'Moat Checklist (15pt)'`
    - qualityFormatter.js SECTION_LABELS contains `management_checklist: 'Management Checklist (13pt)'`
    - qualityFormatter.js SECTION_LABELS contains `valuation_confirmation: 'Valuation Confirmation'`
    - qualityFormatter.js SECTION_LABELS contains `inversion_rebuttal: 'Inversion & Rebuttal (Debate)'`
    - qualityFormatter.js stageTitle handles 'fullStory' returning 'Full Story'
    - run-quality-v4.js accepts `--stage fullStory` argument
    - run-quality-v4.js reads fullStory-S*.json section files from .thes1s/reports/{ticker}/sections/
    - Running `node --loader ./scripts/node-esm-loader.js scripts/run-quality-v4.js SFM --stage fullStory` produces console output with 6 sections scored
    - Output file exists at .thes1s/reports/SFM/quality/full-story-v4.quality.json
    - Output file exists at .thes1s/reports/SFM/quality/full-story-v4.quality.md
    - Existing `npm test` still passes
  </acceptance_criteria>
  <done>qualityFormatter.js displays Full Story section labels with item counts. run-quality-v4.js supports `--stage fullStory` flag, reads individual section JSON files, produces quality report with dual scores for all 6 Full Story sections.</done>
</task>

</tasks>

<verification>
1. `npm test` — all existing tests pass (no regressions)
2. `node --loader ./scripts/node-esm-loader.js scripts/run-quality-v4.js SFM --stage fullStory` — produces quality report with 6 sections, each having mechanical + methodology scores
3. grep -c 'event_analysis\|meaning_checklist\|moat_checklist\|management_checklist\|valuation_confirmation\|inversion_rebuttal' src/engines/critic.js — returns 6+ (each key appears at least once in METHODOLOGY_CHECKS)
</verification>

<success_criteria>
- critic.js contains 33 methodology checks across 6 Full Story section keys (5+5+6+6+5+6)
- Checklist sections use adjusted completeness weights (narrativeDepth: 15, dataPopulation: 25)
- Non-standard verdicts (CONTEXT, WATCHLIST) mapped to PARTIAL
- run-quality-v4.js scores SFM Full Story with both mechanical and methodology scores for all 6 sections
- qualityFormatter.js labels all 6 Full Story sections with descriptive names
- All existing tests pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/15-quality-system/15-01-SUMMARY.md`
</output>
