---
phase: 15-quality-system
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/engines/__tests__/critic.test.js
  - src/engines/__tests__/fixtures/sfm-fullstory-event-analysis.json
  - src/engines/__tests__/fixtures/sfm-fullstory-meaning-checklist.json
  - src/engines/__tests__/fixtures/sfm-fullstory-moat-checklist.json
  - src/engines/__tests__/fixtures/sfm-fullstory-management-checklist.json
  - src/engines/__tests__/fixtures/sfm-fullstory-valuation-confirmation.json
  - src/engines/__tests__/fixtures/sfm-fullstory-inversion-rebuttal.json
autonomous: true
requirements: [QUAL-01, QUAL-03]

must_haves:
  truths:
    - "Every Full Story methodology check has at least one passing and conceptual failing test"
    - "Polymorphic checklist parsing handles both Meaning format (id/question) and Moat/Management format (number/item)"
    - "Non-standard verdicts CONTEXT and WATCHLIST are tested as mapping to PARTIAL"
    - "Debate methodology checks pass on real SFM data with 9 exchanges and 35 citations"
    - "validateStage produces overallMethodologyScore for Full Story sections"
    - "Smoke test on real SFM data produces actionable methodology gap feedback"
  artifacts:
    - path: "src/engines/__tests__/critic.test.js"
      provides: "Full Story methodology check test coverage"
      contains: "Full Story"
    - path: "src/engines/__tests__/fixtures/sfm-fullstory-meaning-checklist.json"
      provides: "Real SFM Meaning Checklist fixture"
      contains: "meaning"
    - path: "src/engines/__tests__/fixtures/sfm-fullstory-inversion-rebuttal.json"
      provides: "Real SFM Debate fixture"
      contains: "debateStructure"
  key_links:
    - from: "src/engines/__tests__/critic.test.js"
      to: "src/engines/critic.js"
      via: "_testExports import"
      pattern: "parseChecklistData|normalizeVerdict|parseDebateData|flagNonStandardVerdicts"
---

<objective>
Create test fixtures from real SFM Full Story output and write comprehensive unit tests covering all 33 methodology checks, 4 helper functions, completeness weight adjustment, and end-to-end validateStage scoring. Verify that the quality system produces actionable methodology gap feedback on real data.

Purpose: Prove QUAL-01 (every check fires correctly) and QUAL-03 (dual scoring works end-to-end). Without tests, the 33 checks are unverified regex patterns that might silently fail on real data.

Output: 6 test fixture files + ~300-400 lines of new tests in critic.test.js covering Full Story quality scoring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-quality-system/15-CONTEXT.md
@.planning/phases/15-quality-system/15-RESEARCH.md
@.planning/phases/15-quality-system/15-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 implementation — helpers added to _testExports -->

From src/engines/critic.js (new exports after Plan 01):
```javascript
export const _testExports = {
  // ... existing exports ...
  parseChecklistData,    // (section) => { items: [{id, question, verdict, rawVerdict, evidence, confidence}], summary }
  normalizeVerdict,      // (verdict) => 'PASS' | 'FAIL' | 'PARTIAL' | null
  parseDebateData,       // (section) => { debateStructure, judgeOverallVerdict } | null
  flagNonStandardVerdicts, // (items) => [{type, severity, message, field}]
};
```

From src/engines/critic.js (METHODOLOGY_CHECKS keys after Plan 01):
- event_analysis: 5 checks (IDs: event-root-cause, event-historical, event-recovery, event-debt, event-analyst)
- meaning_checklist: 5 checks (IDs: meaning-item-count, meaning-all-verdicts, meaning-evidence-present, meaning-radar-items, meaning-kpi-numeric)
- moat_checklist: 6 checks (IDs: moat-item-count, moat-all-verdicts, moat-evidence-present, moat-type-identified, moat-durability, moat-replicability)
- management_checklist: 6 checks (IDs: mgmt-item-count, mgmt-all-verdicts, mgmt-evidence-present, mgmt-financial-numeric, mgmt-ceo-named, mgmt-insider-pct)
- valuation_confirmation: 5 checks (IDs: val-growth-quality, val-fgr-rationality, val-sensitivity, val-multiple-methods, val-red-flags)
- inversion_rebuttal: 6 checks (IDs: debate-bull-count, debate-bear-coverage, debate-bear-citations, debate-rebuttal-coverage, debate-thesis-killer, debate-honesty)

Existing test patterns from src/engines/__tests__/critic.test.js:
```javascript
import companyInfoSection from './fixtures/cost-section-company-info.json' with { type: 'json' };
// Tests use describe/it/expect from vitest
// scoreMethodology(section) returns { score, checks, passed }
// validateSection(section, dataPacket) returns full quality report
// validateStage(sections, dataPacket) returns aggregate with overallMethodologyScore
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test fixtures from real SFM Full Story output and write unit tests for helpers + all 33 checks</name>
  <files>
    src/engines/__tests__/fixtures/sfm-fullstory-event-analysis.json,
    src/engines/__tests__/fixtures/sfm-fullstory-meaning-checklist.json,
    src/engines/__tests__/fixtures/sfm-fullstory-moat-checklist.json,
    src/engines/__tests__/fixtures/sfm-fullstory-management-checklist.json,
    src/engines/__tests__/fixtures/sfm-fullstory-valuation-confirmation.json,
    src/engines/__tests__/fixtures/sfm-fullstory-inversion-rebuttal.json,
    src/engines/__tests__/critic.test.js
  </files>
  <read_first>
    - src/engines/__tests__/critic.test.js (full file — understand existing test structure, describe blocks, import patterns)
    - src/engines/critic.js (read the new helpers and METHODOLOGY_CHECKS entries added by Plan 01 — need exact function signatures and check IDs)
    - .thes1s/reports/SFM/sections/fullStory-S1-event_analysis.json (source for S1 fixture)
    - .thes1s/reports/SFM/sections/fullStory-S2-meaning_checklist.json (source for S2 fixture)
    - .thes1s/reports/SFM/sections/fullStory-S3-moat_checklist.json (source for S3 fixture)
    - .thes1s/reports/SFM/sections/fullStory-S4-management_checklist.json (source for S4 fixture)
    - .thes1s/reports/SFM/sections/fullStory-S5-valuation_confirmation.json (source for S5 fixture)
    - .thes1s/reports/SFM/sections/fullStory-S6-inversion_rebuttal.json (source for S6 fixture)
  </read_first>
  <action>
**1. Create 6 test fixtures** by extracting minimal but complete sections from the real SFM Full Story output:

For each of the 6 SFM section files, create a fixture JSON file in `src/engines/__tests__/fixtures/`. Each fixture must contain the full section object (key, title, sectionNumber, status, confidence, verdict, verdictRationale, summary, data, narrative, citations, redFlags, modelUsed, tokenCost). The `data` field should remain as a JSON string (as it appears in the real output) — do NOT pre-parse it. The `narrative` can be truncated to the first ~2000 characters if needed to keep fixture size manageable, but MUST retain enough content for the regex checks to pass (verify each check against the truncated narrative).

Fixture file names:
- `sfm-fullstory-event-analysis.json` (from fullStory-S1)
- `sfm-fullstory-meaning-checklist.json` (from fullStory-S2)
- `sfm-fullstory-moat-checklist.json` (from fullStory-S3)
- `sfm-fullstory-management-checklist.json` (from fullStory-S4)
- `sfm-fullstory-valuation-confirmation.json` (from fullStory-S5)
- `sfm-fullstory-inversion-rebuttal.json` (from fullStory-S6)

IMPORTANT for S6 fixture: Keep the full citations array (35 entries with URLs) and enough narrative to contain bull/bear/rebuttal/thesis-killer/honesty language. The debateStructure in data is critical — must be preserved.

IMPORTANT for S4 fixture: Must preserve the non-standard verdicts (CONTEXT, WATCHLIST) in the data field items.

**2. Extend critic.test.js** with the following new test blocks (add after existing tests):

a) **Import fixtures** at top of file:
```javascript
import eventAnalysisSection from './fixtures/sfm-fullstory-event-analysis.json' with { type: 'json' };
import meaningChecklistSection from './fixtures/sfm-fullstory-meaning-checklist.json' with { type: 'json' };
import moatChecklistSection from './fixtures/sfm-fullstory-moat-checklist.json' with { type: 'json' };
import managementChecklistSection from './fixtures/sfm-fullstory-management-checklist.json' with { type: 'json' };
import valuationConfirmationSection from './fixtures/sfm-fullstory-valuation-confirmation.json' with { type: 'json' };
import inversionRebuttalSection from './fixtures/sfm-fullstory-inversion-rebuttal.json' with { type: 'json' };
```

b) **Add new _testExports destructuring** for the 4 new helpers:
```javascript
const { parseChecklistData, normalizeVerdict, parseDebateData, flagNonStandardVerdicts } = _testExports;
```

c) **describe('normalizeVerdict')** — test cases:
- `normalizeVerdict('PASS')` returns `'PASS'`
- `normalizeVerdict('FAIL')` returns `'FAIL'`
- `normalizeVerdict('PARTIAL')` returns `'PARTIAL'`
- `normalizeVerdict('CONTEXT')` returns `'PARTIAL'`
- `normalizeVerdict('WATCHLIST')` returns `'PARTIAL'`
- `normalizeVerdict('pass')` returns `'PASS'` (case-insensitive)
- `normalizeVerdict(null)` returns `null`
- `normalizeVerdict(undefined)` returns `null`

d) **describe('parseChecklistData')** — test cases:
- Parses S2 fixture (Meaning format with id/question): returns 15 items, first item has id=1 and question string
- Parses S3 fixture (Moat format with number/item): returns 15 items, first item has id=1 and question string
- Parses S4 fixture (Management with non-standard verdicts): returns 13 items, CONTEXT and WATCHLIST verdicts normalized to PARTIAL, rawVerdict preserved
- Returns `{ items: [], summary: null }` for section with `data: null`
- Returns `{ items: [], summary: null }` for section with `data: "invalid json"`

e) **describe('parseDebateData')** — test cases:
- Parses S6 fixture: returns object with `debateStructure.totalExchanges === 9`
- Returns null for section with `data: null`

f) **describe('flagNonStandardVerdicts')** — test cases:
- S4 parsed items produce low-severity issues for CONTEXT and WATCHLIST items
- S2 parsed items produce no non-standard verdict issues (all PASS/FAIL/PARTIAL)

g) **describe('Full Story Methodology Checks')** with nested describe blocks per section:

For each section type, test against the real SFM fixture:

**describe('event_analysis'):**
- `scoreMethodology(eventAnalysisSection)` returns score > 0 with 5 checks
- event-root-cause passes on SFM data (narrative contains root cause language)
- event-historical passes on SFM data (narrative contains historical precedent language)
- A synthetic section with empty narrative: all critical checks fail, score is low

**describe('meaning_checklist'):**
- `scoreMethodology(meaningChecklistSection)` returns score > 0 with 5 checks
- meaning-item-count passes (15 items in data)
- meaning-all-verdicts passes (all items have verdicts)
- meaning-evidence-present passes (all items have evidence > 10 chars)
- A synthetic section with only 10 items: meaning-item-count fails

**describe('moat_checklist'):**
- `scoreMethodology(moatChecklistSection)` returns score > 0 with 6 checks
- moat-type-identified passes on SFM data (evidence contains "brand" or "switching cost")
- A synthetic section with no moat type language: moat-type-identified fails

**describe('management_checklist'):**
- `scoreMethodology(managementChecklistSection)` returns score > 0 with 6 checks
- mgmt-item-count passes (13 items)
- mgmt-all-verdicts passes (CONTEXT/WATCHLIST normalized to PARTIAL count as verdicts)
- mgmt-financial-numeric passes (items cite ROE, ROIC, % numbers)
- flagNonStandardVerdicts on parsed items returns issues for CONTEXT and WATCHLIST items

**describe('valuation_confirmation'):**
- `scoreMethodology(valuationConfirmationSection)` returns score > 0 with 5 checks
- val-fgr-rationality passes on SFM data (narrative mentions FGR or growth rate)
- val-sensitivity passes on SFM data (narrative contains sensitivity/range language)

**describe('inversion_rebuttal'):**
- `scoreMethodology(inversionRebuttalSection)` returns score > 0 with 6 checks
- debate-bull-count passes (debateStructure.totalExchanges = 9 >= 5)
- debate-bear-coverage passes (totalExchanges = 9 >= 5)
- debate-bear-citations passes (35 citations with web URLs)
- debate-honesty passes (narrative contains acknowledgment language)

h) **describe('Full Story Completeness Weight Adjustment'):**
- `scoreCompleteness(meaningChecklistSection)` uses adjusted weights (test that checklist section scores differently than a standard section with same content)
- `scoreCompleteness(eventAnalysisSection)` uses default weights (standard section)
- Concrete test: create two identical mock sections, one with key='meaning_checklist', one with key='test_standard'. The meaning_checklist one should score higher on data population contribution and lower on narrative contribution.

i) **describe('Full Story validateStage integration'):**
- `validateStage([eventAnalysisSection, meaningChecklistSection, moatChecklistSection, managementChecklistSection, valuationConfirmationSection, inversionRebuttalSection], {})` returns:
  - `overallMethodologyScore` that is a number (not null/undefined)
  - `sections` array with 6 entries
  - Each section has `methodology.score` that is a number 0-100
  - Each section has `methodology.checks` array with correct count per section type
  - `overallPassed` is a boolean
  </action>
  <verify>
    <automated>cd /Users/kylehoff/Desktop/stock-analyzer && npx vitest run src/engines/__tests__/critic.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists: src/engines/__tests__/fixtures/sfm-fullstory-event-analysis.json with key "event_analysis"
    - File exists: src/engines/__tests__/fixtures/sfm-fullstory-meaning-checklist.json with key "meaning_checklist"
    - File exists: src/engines/__tests__/fixtures/sfm-fullstory-moat-checklist.json with key "moat_checklist"
    - File exists: src/engines/__tests__/fixtures/sfm-fullstory-management-checklist.json with key "management_checklist"
    - File exists: src/engines/__tests__/fixtures/sfm-fullstory-valuation-confirmation.json with key "valuation_confirmation"
    - File exists: src/engines/__tests__/fixtures/sfm-fullstory-inversion-rebuttal.json with key "inversion_rebuttal"
    - critic.test.js contains `describe('normalizeVerdict'`
    - critic.test.js contains `describe('parseChecklistData'`
    - critic.test.js contains `describe('parseDebateData'`
    - critic.test.js contains `describe('flagNonStandardVerdicts'`
    - critic.test.js contains `describe('Full Story Methodology Checks'`
    - critic.test.js contains `describe('Full Story Completeness Weight Adjustment'`
    - critic.test.js contains `describe('Full Story validateStage integration'`
    - critic.test.js tests normalizeVerdict maps 'CONTEXT' to 'PARTIAL' and 'WATCHLIST' to 'PARTIAL'
    - critic.test.js tests parseChecklistData returns 15 items for meaning_checklist fixture
    - critic.test.js tests parseChecklistData returns 13 items for management_checklist fixture
    - critic.test.js tests scoreMethodology returns score > 0 for all 6 Full Story section fixtures
    - critic.test.js tests validateStage returns overallMethodologyScore for 6 Full Story sections
    - `npx vitest run src/engines/__tests__/critic.test.js` exits 0 with all tests passing
    - `npm test` exits 0 (full suite)
  </acceptance_criteria>
  <done>6 test fixtures created from real SFM data. critic.test.js extended with comprehensive coverage: 8+ normalizeVerdict tests, 5+ parseChecklistData tests, 2+ parseDebateData tests, 2+ flagNonStandardVerdicts tests, per-section methodology check tests for all 6 section types, completeness weight adjustment tests, and end-to-end validateStage integration test. All tests pass.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/engines/__tests__/critic.test.js` — all tests pass including new Full Story tests
2. `npm test` — full suite passes
3. `node --loader ./scripts/node-esm-loader.js scripts/run-quality-v4.js SFM --stage fullStory` — produces quality report with methodology scores for 6 sections
4. The quality report identifies specific methodology gaps (check that at least some checks show FAIL — not all 100% on first pass)
</verification>

<success_criteria>
- 6 test fixtures created from real SFM Full Story output
- 40+ new tests covering all 33 methodology checks, 4 helpers, weight adjustment, and validateStage integration
- All tests pass on real SFM fixture data
- Methodology scores are not trivially 100% — at least some checks should fail on SFM data, proving the checks are discriminating
- Full test suite (npm test) passes with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/15-quality-system/15-02-SUMMARY.md`
</output>
