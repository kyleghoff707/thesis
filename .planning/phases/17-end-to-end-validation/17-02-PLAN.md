---
phase: 17-end-to-end-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - .thes1s/reports/MNST/one-pager.json
  - .thes1s/reports/MNST/pipeline-output.json
  - .thes1s/reports/MNST/full-story-api.json
  - .thes1s/reports/MNST/budget.json
  - .thes1s/reports/MNST/quality/pitch-deck-v4.quality.json
  - .thes1s/reports/MNST/quality/full-story-v4.quality.json
autonomous: false
requirements:
  - QUAL-02

must_haves:
  truths:
    - "MNST completes all 3 stages (OP -> PD -> FS) in a single automated invocation"
    - "One Pager verdict is PASS"
    - "Pitch Deck scores 85+ on both mechanical and methodology"
    - "Full Story scores 85+ on both mechanical and methodology"
    - "Full Story demonstrates inheritance from Pitch Deck (checklist items reference PD findings)"
    - "Total pipeline cost is within $15 ceiling"
  artifacts:
    - path: ".thes1s/reports/MNST/one-pager.json"
      provides: "MNST One Pager structured output"
      contains: "overallVerdict"
    - path: ".thes1s/reports/MNST/pipeline-output.json"
      provides: "MNST Pitch Deck output"
      contains: "pitchDeck"
    - path: ".thes1s/reports/MNST/full-story-api.json"
      provides: "MNST Full Story output"
      contains: "fullStory"
    - path: ".thes1s/reports/MNST/budget.json"
      provides: "Combined cost breakdown across all 3 stages"
      contains: "withinCeiling"
    - path: ".thes1s/reports/MNST/quality/pitch-deck-v4.quality.json"
      provides: "PD quality scores"
      contains: "overallScore"
    - path: ".thes1s/reports/MNST/quality/full-story-v4.quality.json"
      provides: "FS quality scores"
      contains: "overallScore"
  key_links:
    - from: ".thes1s/reports/MNST/full-story-api.json"
      to: ".thes1s/reports/MNST/pipeline-output.json"
      via: "Full Story sections reference Pitch Deck findings"
      pattern: "Pitch Deck|pitch deck|PD"
---

<objective>
Run MNST through the complete 3-stage pipeline using `--stage all`, validate quality at every gate, diagnose and fix any failures, then have the PM review the output.

Purpose: Prove the end-to-end pipeline works on a fresh ticker with no prior artifacts — the capstone validation for v1.2 (QUAL-02).
Output: Complete MNST report artifacts (JSON + quality reports) demonstrating hedge-fund-quality output across all 3 stages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-end-to-end-validation/17-CONTEXT.md
@.planning/phases/17-end-to-end-validation/17-01-SUMMARY.md

@scripts/run-pipeline.js
@src/engines/critic.js
@scripts/run-quality-v4.js
@scripts/pdf/generate_one_pager_pdf.py
@scripts/pdf/generate_pitch_deck_pdf.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run MNST through the full 3-stage pipeline and validate results</name>
  <files>.thes1s/reports/MNST/</files>
  <read_first>
    - scripts/run-pipeline.js (the updated script from Plan 01 — verify --stage all is implemented)
    - .planning/phases/17-end-to-end-validation/17-CONTEXT.md (D-01 through D-11 for validation criteria)
    - scripts/run-quality-v4.js (reference for how to run quality scoring if needed standalone)
  </read_first>
  <action>
Execute the full pipeline on MNST:

```bash
node --loader ./scripts/node-esm-loader.js scripts/run-pipeline.js MNST --stage all
```

This will:
1. Assemble MNST DataPacket
2. Pre-process MNST 10-K/10-Q filings
3. Run One Pager, check PASS verdict
4. Run Pitch Deck, score with critic.js, check 85+ on both scores
5. Run Full Story (with PD inheritance), score with critic.js, check 85+ on both scores
6. Write all outputs to `.thes1s/reports/MNST/`
7. Write combined budget.json with per-stage cost breakdown

**If any gate fails (per D-06):**
1. Read the quality report at `.thes1s/reports/MNST/quality/{stage}-v4.quality.md` to identify specific failing checks
2. Diagnose root cause — is it a prompt issue, schema issue, critic issue, or data issue?
3. Fix the root cause (in the appropriate file: agent prompt, schema, critic.js, or pipeline code)
4. Re-run the pipeline: `node --loader ./scripts/node-esm-loader.js scripts/run-pipeline.js MNST --stage all`
5. Repeat until all gates pass

**After successful completion, verify:**
1. Check `budget.json` — total cost must be within $15 ceiling (per D-11)
2. Spot-check Full Story for Pitch Deck inheritance — grep for "Pitch Deck" or PD section references in Full Story sections:
   ```bash
   grep -il "pitch deck\|PD\|Pitch Deck" .thes1s/reports/MNST/sections/fullStory-*.json
   ```
3. Verify all expected output files exist:
   - `.thes1s/reports/MNST/one-pager.json`
   - `.thes1s/reports/MNST/pipeline-output.json`
   - `.thes1s/reports/MNST/full-story-api.json`
   - `.thes1s/reports/MNST/budget.json`
   - `.thes1s/reports/MNST/quality/pitch-deck-v4.quality.json`
   - `.thes1s/reports/MNST/quality/full-story-v4.quality.json`
   - `.thes1s/reports/MNST/sections/fullStory-S1-event_analysis.json` through S6
   - `.thes1s/reports/MNST/sections/debate-step-1.json` through debate-step-4.json

**Cost target reference:** SFM pipeline costs from prior phases:
- SFM Pitch Deck: ~$6.90
- SFM Full Story: ~$7.37
- Combined PD+FS: ~$14.27 (within $15 ceiling)
- One Pager is typically $0.05-0.15
- So MNST total should be roughly similar: ~$14-15
  </action>
  <verify>
    <automated>test -f .thes1s/reports/MNST/one-pager.json && test -f .thes1s/reports/MNST/pipeline-output.json && test -f .thes1s/reports/MNST/full-story-api.json && test -f .thes1s/reports/MNST/budget.json && test -f .thes1s/reports/MNST/quality/pitch-deck-v4.quality.json && test -f .thes1s/reports/MNST/quality/full-story-v4.quality.json && node -e "const b=JSON.parse(require('fs').readFileSync('.thes1s/reports/MNST/budget.json','utf8')); console.log('Cost:', b.totalCost.toFixed(4), 'Within ceiling:', b.withinCeiling); console.log('OP:', b.stages.onePager.verdict); console.log('PD:', b.stages.pitchDeck.mechanical, '/', b.stages.pitchDeck.methodology); console.log('FS:', b.stages.fullStory.mechanical, '/', b.stages.fullStory.methodology); if (b.stages.onePager.verdict !== 'PASS') process.exit(1); if (b.stages.pitchDeck.mechanical < 85 || b.stages.pitchDeck.methodology < 85) process.exit(1); if (b.stages.fullStory.mechanical < 85 || b.stages.fullStory.methodology < 85) process.exit(1); console.log('ALL GATES PASSED')" && echo "VALIDATION COMPLETE"</automated>
  </verify>
  <acceptance_criteria>
    - .thes1s/reports/MNST/one-pager.json exists and contains "overallVerdict": "PASS"
    - .thes1s/reports/MNST/pipeline-output.json exists and contains "pitchDeck" as stage
    - .thes1s/reports/MNST/full-story-api.json exists and contains "fullStory" as stage
    - .thes1s/reports/MNST/budget.json exists with withinCeiling: true and totalCost <= 15.0
    - .thes1s/reports/MNST/quality/pitch-deck-v4.quality.json has overallScore >= 85 and overallMethodologyScore >= 85
    - .thes1s/reports/MNST/quality/full-story-v4.quality.json has overallScore >= 85 and overallMethodologyScore >= 85
    - At least 4 of 6 Full Story section files exist in .thes1s/reports/MNST/sections/fullStory-S*.json
    - At least 2 of 4 debate step files exist in .thes1s/reports/MNST/sections/debate-step-*.json
  </acceptance_criteria>
  <done>MNST completes the full 3-stage pipeline with all gates passing: OP verdict PASS, PD 85+ dual score, FS 85+ dual score, total cost within $15 ceiling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: PM reviews MNST pipeline output quality</name>
  <files>.thes1s/reports/MNST/</files>
  <action>
Present the MNST pipeline results to the PM for quality review. The PM should examine:

1. Console output from the pipeline run — all 3 stages show PASS/85+ scores
2. Combined cost in `.thes1s/reports/MNST/budget.json` — within $15 ceiling
3. One Pager quality: `cat .thes1s/reports/MNST/one-pager.json | python3 -m json.tool | head -50`
4. PD quality report: `cat .thes1s/reports/MNST/quality/pitch-deck-v4.quality.md`
5. FS quality report: `cat .thes1s/reports/MNST/quality/full-story-v4.quality.md`
6. Spot-check debate quality: `cat .thes1s/reports/MNST/sections/fullStory-S6-inversion_rebuttal.json | python3 -m json.tool | head -80`
7. Full Story references Pitch Deck findings (inheritance proof)

Expected outcomes:
- One Pager verdict: PASS
- Pitch Deck scores: 85+ mechanical, 85+ methodology
- Full Story scores: 85+ mechanical, 85+ methodology
- Total cost: within $15.00
- Full Story sections mention Pitch Deck findings in narratives

Resume signal: Type "approved" if quality is satisfactory, or describe any issues to investigate.
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting PM approval"</automated>
  </verify>
  <done>PM has reviewed MNST pipeline output and approved the quality of all 3 stages.</done>
</task>

</tasks>

<verification>
- All 6 output files exist in .thes1s/reports/MNST/
- budget.json shows totalCost <= 15.0 and withinCeiling: true
- One Pager verdict is PASS
- PD and FS quality scores are both 85+ on mechanical and methodology
- Full Story sections reference Pitch Deck findings (inheritance proof)
</verification>

<success_criteria>
MNST runs through the complete 3-stage pipeline in a single automated invocation with:
1. One Pager verdict: PASS
2. Pitch Deck: 85+ on both mechanical and methodology scores
3. Full Story: 85+ on both mechanical and methodology scores
4. Full Story demonstrates Pitch Deck inheritance
5. Total pipeline cost within $15 ceiling
6. PM approves the output quality at the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/17-end-to-end-validation/17-02-SUMMARY.md`
</output>
