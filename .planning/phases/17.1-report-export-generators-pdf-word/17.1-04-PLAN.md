---
phase: 17.1-report-export-generators-pdf-word
plan: 04
type: execute
wave: 3
depends_on: [17.1-02, 17.1-03]
files_modified: []
autonomous: false
requirements: [QUAL-02]
must_haves:
  truths:
    - "All 6 MNST exports exist: 3 PDFs + 3 Word docs"
    - "PDFs have Thes1s branding, charts, and professional layout"
    - "Word docs have formatted text, styled tables, and readable content"
    - "No generator is hardcoded to a specific ticker"
  artifacts:
    - path: ".thes1s/reports/MNST/one-pager.pdf"
      provides: "One Pager PDF export"
    - path: ".thes1s/reports/MNST/pitch-deck.pdf"
      provides: "Pitch Deck PDF export"
    - path: ".thes1s/reports/MNST/full-story.pdf"
      provides: "Full Story PDF export"
    - path: ".thes1s/reports/MNST/one-pager.docx"
      provides: "One Pager Word doc export"
    - path: ".thes1s/reports/MNST/pitch-deck.docx"
      provides: "Pitch Deck Word doc export"
    - path: ".thes1s/reports/MNST/full-story.docx"
      provides: "Full Story Word doc export"
  key_links:
    - from: "scripts/pdf/generate_*.py"
      to: ".thes1s/reports/MNST/"
      via: "CLI execution with MNST argument"
      pattern: "sys\\.argv\\[1\\]"
---

<objective>
Validate all 6 MNST exports and get user visual approval.

Purpose: Automated tests can only verify file existence and size. The user must visually verify that PDFs have correct branding, charts render properly, tables are aligned, and Word docs are readable and professional.

Output: User approval of visual quality across all 6 exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17.1-report-export-generators-pdf-word/17.1-CONTEXT.md
@.planning/phases/17.1-report-export-generators-pdf-word/17.1-01-SUMMARY.md
@.planning/phases/17.1-report-export-generators-pdf-word/17.1-02-SUMMARY.md
@.planning/phases/17.1-report-export-generators-pdf-word/17.1-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate all 6 MNST exports and verify file integrity</name>
  <files></files>
  <read_first>
    - scripts/pdf/generate_one_pager_pdf.py (verify import lines work)
    - scripts/pdf/generate_pitch_deck_pdf.py (verify import lines work)
    - scripts/pdf/generate_full_story_pdf.py (verify import lines work)
  </read_first>
  <action>
Run all 6 generators sequentially and verify each output:

```bash
cd /Users/kylehoff/Desktop/stock-analyzer

# Generate all 6 exports
python3 scripts/pdf/generate_one_pager_pdf.py MNST
python3 scripts/pdf/generate_pitch_deck_pdf.py MNST
python3 scripts/pdf/generate_full_story_pdf.py MNST
python3 scripts/pdf/generate_one_pager_docx.py MNST
python3 scripts/pdf/generate_pitch_deck_docx.py MNST
python3 scripts/pdf/generate_full_story_docx.py MNST
```

Verify all 6 files exist and have reasonable sizes:
- one-pager.pdf > 5KB
- pitch-deck.pdf > 20KB
- full-story.pdf > 20KB
- one-pager.docx > 3KB
- pitch-deck.docx > 5KB
- full-story.docx > 5KB

If any generator fails, debug the error and fix it before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/kylehoff/Desktop/stock-analyzer && python3 -c "
import os
files = {
    'one-pager.pdf': 5000,
    'pitch-deck.pdf': 20000,
    'full-story.pdf': 20000,
    'one-pager.docx': 3000,
    'pitch-deck.docx': 5000,
    'full-story.docx': 5000,
}
base = '.thes1s/reports/MNST'
all_ok = True
for fname, min_size in files.items():
    path = os.path.join(base, fname)
    if not os.path.exists(path):
        print(f'MISSING: {fname}')
        all_ok = False
    else:
        size = os.path.getsize(path)
        ok = size >= min_size
        status = 'OK' if ok else 'TOO SMALL'
        print(f'{status}: {fname} = {size:,} bytes (min {min_size:,})')
        if not ok:
            all_ok = False
assert all_ok, 'Not all exports valid'
print('All 6 exports verified')
"</automated>
  </verify>
  <acceptance_criteria>
    - .thes1s/reports/MNST/one-pager.pdf exists and is > 5KB
    - .thes1s/reports/MNST/pitch-deck.pdf exists and is > 20KB
    - .thes1s/reports/MNST/full-story.pdf exists and is > 20KB
    - .thes1s/reports/MNST/one-pager.docx exists and is > 3KB
    - .thes1s/reports/MNST/pitch-deck.docx exists and is > 5KB
    - .thes1s/reports/MNST/full-story.docx exists and is > 5KB
  </acceptance_criteria>
  <done>All 6 MNST export files exist with valid sizes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual quality approval of all 6 MNST exports</name>
  <files></files>
  <read_first>
    - .thes1s/reports/MNST/one-pager.pdf (open to verify)
    - .thes1s/reports/MNST/pitch-deck.pdf (open to verify)
    - .thes1s/reports/MNST/full-story.pdf (open to verify)
  </read_first>
  <action>
Present the user with all 6 generated files and ask them to visually verify quality.

What was built: 6 export generators (3 PDF + 3 Word) for MNST pipeline output. PDFs have Thes1s branding (teal/slate palette, logo, charts, tables). Word docs have formatted text, styled tables, and section headings.

How to verify:
1. Open `.thes1s/reports/MNST/one-pager.pdf` -- verify title page has Thes1s logo and branding, section narratives are readable, financial charts from DataPacket render (revenue, EPS), footer shows "Thes1s | One Pager | Page N"
2. Open `.thes1s/reports/MNST/pitch-deck.pdf` -- verify all 10 sections present with narratives, tables render with teal headers and alternating rows, red flags appear where relevant, citations listed per section, financial charts render
3. Open `.thes1s/reports/MNST/full-story.pdf` -- verify checklist tables show # / Item / Verdict / Confidence columns, PASS/FAIL/PARTIAL verdicts are present, evidence text appears below checklist tables, 4-step debate renders (Bull Thesis, Bear Inversion, Bull Rebuttal, Judge Verdict), Judge exchange table shows Topic / Bull / Bear / Verdict
4. Open `.thes1s/reports/MNST/one-pager.docx` in Word/Preview -- verify teal headings, Arial font, readable formatting, section narratives present
5. Open `.thes1s/reports/MNST/pitch-deck.docx` -- verify tables have teal header row with white text, alternating row shading, citations and red flags present
6. Open `.thes1s/reports/MNST/full-story.docx` -- verify checklist tables with verdicts, debate text renders (Bull, Bear, Rebuttal, Judge)

Resume signal: Type "approved" if all 6 exports look good, or describe specific issues to fix.
  </action>
  <verify>User approval received</verify>
  <acceptance_criteria>
    - User has visually inspected all 6 files
    - User has typed "approved" or provided feedback for fixes
  </acceptance_criteria>
  <done>User has approved visual quality of all 6 MNST exports (3 PDFs + 3 Word docs).</done>
</task>

</tasks>

<verification>
All 6 MNST files exist, pass size checks, and user has visually approved quality.
</verification>

<success_criteria>
- All 6 generators run without errors on MNST
- All 6 output files exist with reasonable sizes
- User approves visual quality of PDFs (branding, charts, tables) and Word docs (formatting, content)
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-report-export-generators-pdf-word/17.1-04-SUMMARY.md`
</output>
