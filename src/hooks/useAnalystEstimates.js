// Backward-compatible re-export — replaced by useAnalystData (multi-source).
// Existing consumers can keep importing from here without changes.

export { useAnalystData as useAnalystEstimates } from './useAnalystData';
