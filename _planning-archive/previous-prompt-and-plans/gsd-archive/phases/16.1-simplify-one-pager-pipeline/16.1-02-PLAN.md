---
phase: 16.1-simplify-one-pager-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["16.1-01"]
files_modified:
  - .claude/skills/generate-one-pager/SKILL.md
autonomous: false

must_haves:
  truths:
    - "The one pager generates end-to-end on a real ticker via CLI"
    - "The output JSON is readable by the existing PDF generator without errors"
    - "The gate check (Pitch Deck reads one-pager.json overallVerdict) still works"
    - "The CC skill is updated to use the simplified API path"
    - "Total cost is under $1 for a single one pager"
  artifacts:
    - path: ".claude/skills/generate-one-pager/SKILL.md"
      provides: "Simplified CC skill using single-call generator"
    - path: ".thes1s/reports/{TICKER}/one-pager.json"
      provides: "Generated one pager output from live test"
  key_links:
    - from: ".claude/skills/generate-one-pager/SKILL.md"
      to: "src/engines/onePagerGenerator.js"
      via: "CLI invocation of run-pipeline.js onePager"
      pattern: "run-pipeline.*onePager"
    - from: ".thes1s/reports/{TICKER}/one-pager.json"
      to: "scripts/pdf/generate_one_pager_pdf.py"
      via: "PDF generator reads sections by key from one-pager.json"
      pattern: "sections.*key"
---

<objective>
Validate the single-call one pager on a real ticker and update the CC skill to match the simplified pipeline.

Purpose: Prove the new generator works end-to-end on live data, confirm backward compatibility with PDF generation, and update the CC skill so it uses the simplified path instead of the old 11-step multi-agent workflow.

Output: Validated one-pager.json on a real ticker, updated CC skill.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.1-simplify-one-pager-pipeline/16.1-01-SUMMARY.md

@src/engines/onePagerGenerator.js
@scripts/run-pipeline.js
@.claude/skills/generate-one-pager/SKILL.md
@scripts/pdf/generate_one_pager_pdf.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live test on real ticker and validate output compatibility</name>
  <files></files>
  <read_first>
    - src/engines/onePagerGenerator.js (verify the generator from Plan 01)
    - scripts/run-pipeline.js (verify the onePager shortcut path from Plan 01)
    - scripts/pdf/generate_one_pager_pdf.py (understand what fields PDF gen reads)
  </read_first>
  <action>
Run the one pager generator on a real ticker (COST, the example company from the curriculum) to validate end-to-end functionality.

1. Run the generator via CLI: `node --loader ./scripts/node-esm-loader.js scripts/run-pipeline.js COST onePager`

2. Validate the output file `.thes1s/reports/COST/one-pager.json` has these exact top-level keys: ticker, companyName, stage (value: "onePager"), generatedAt, sections (array of 6), overallVerdict (PASS|FAIL|WATCHLIST), sectionKeys (6 entries).

3. Validate each of the 6 sections has: key, title, sectionNumber, status, confidence, verdict, verdictRationale, summary, narrative, citations, redFlags, modelUsed, tokenCost. The 6 keys must be: company_info, minimum_standards, meaning, growth_metrics, valuation_summary, overall_verdict.

4. Validate PDF generator backward compatibility by checking that every section has non-empty narrative, non-null verdict, and at least 1 red flag. Run a Node.js validation script that reads the JSON file, builds a sections-by-key map, and checks each of the 6 required keys has narrative, verdict, and redFlags.

5. Validate gate check compatibility by confirming overallVerdict is not null and not undefined.

6. Log cost (expected under $1) and timing (expected under 3 minutes) from CLI output.

If the generator fails or produces invalid output, diagnose and fix onePagerGenerator.js. Common issues: DataPacket too large for context window (truncate non-essential fields like filingContent), schema validation failure (check Zod types match), null parsed_output (check stop_reason and output_tokens).
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const rpt = JSON.parse(fs.readFileSync('.thes1s/reports/COST/one-pager.json', 'utf8')); const ok = rpt.sections.length === 6 && rpt.overallVerdict != null && rpt.sectionKeys.length === 6; console.log(ok ? 'PASS' : 'FAIL'); if (!ok) process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - .thes1s/reports/COST/one-pager.json exists
    - File contains "stage": "onePager"
    - File contains "overallVerdict" with value PASS, FAIL, or WATCHLIST
    - File contains "sections" array with exactly 6 items
    - Each section has non-empty narrative, verdict, redFlags, citations
    - sectionKeys array has exactly 6 entries matching the canonical keys
    - All 6 section keys present: company_info, minimum_standards, meaning, growth_metrics, valuation_summary, overall_verdict
    - PDF compatibility check passes (all required fields present per section)
    - Gate check passes (overallVerdict is not null)
  </acceptance_criteria>
  <done>A real ticker one pager generates via single Claude call, produces valid backward-compatible JSON, and costs under $1.</done>
</task>

<task type="auto">
  <name>Task 2: Simplify generate-one-pager CC skill</name>
  <files>.claude/skills/generate-one-pager/SKILL.md</files>
  <read_first>
    - .claude/skills/generate-one-pager/SKILL.md (current 11-step CC skill to be replaced)
    - src/engines/onePagerGenerator.js (the new single-call generator)
    - scripts/run-pipeline.js (the updated CLI entry point)
  </read_first>
  <action>
Replace the entire generate-one-pager SKILL.md with a simplified version. The new file has 4 steps instead of 11, under 80 lines total.

The YAML frontmatter must have: name: generate-one-pager, description: "Generate a Rule One One Pager investment screening for a given stock ticker", argument-hint: TICKER, disable-model-invocation: true.

The body has these 4 sections:

**Step 1: Validate Input** — Uppercase $0 as TICKER. If empty, print usage and stop.

**Step 2: Generate One Pager** — Run `node --loader ./scripts/node-esm-loader.js scripts/run-pipeline.js {TICKER} onePager`. This assembles the DataPacket and generates the complete one pager in a single Claude Sonnet API call. Output goes to `.thes1s/reports/{TICKER}/one-pager.json`. List what to monitor in CLI output: assembly time, generation time/cost, per-section verdicts, overall verdict. List troubleshooting: check .env.local has VITE_CLAUDE_KEY, check DataPacket assembly, check generator log.

**Step 3: Review Results** — Read the output file. Print summary: Overall Verdict, Sections (6/6), Per-section verdicts and confidence, Red flags aggregate and critical ones, Cost.

**Step 4: Quality Check (Optional)** — Run critic.js validateStage on the output. Quality is informational, not blocking.

**Constraints section** — Contamination Boundary: NEVER read from knowledge/stage-1-one-pager/examples/, knowledge/stage-2-pitch-deck/examples/, knowledge/stage-3-full-story/examples/, knowledge/pre-course-examples/.

The file must NOT contain any references to: "Dispatch 3 Parallel", "Synthesis Writer", "ReportSectionSchema", "DataPacket slices", "Step 5" through "Step 11", "Agent Configurations", "subagent". All that complexity is now inside the generator.
  </action>
  <verify>
    <automated>wc -l .claude/skills/generate-one-pager/SKILL.md | awk '{print ($1 < 100) ? "PASS" : "FAIL"}'</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/generate-one-pager/SKILL.md exists
    - File contains the command "run-pipeline.js" with "onePager" argument
    - File does NOT contain the string "Dispatch 3 Parallel"
    - File does NOT contain the string "Synthesis Writer"
    - File does NOT contain the string "ReportSectionSchema"
    - File contains "Contamination Boundary" constraint section
    - File has fewer than 100 lines
    - File frontmatter contains "disable-model-invocation: true"
  </acceptance_criteria>
  <done>The CC skill is a concise 4-step workflow that delegates all complexity to the single-call generator via run-pipeline.js.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User verifies one pager quality and conciseness</name>
  <files>.thes1s/reports/COST/one-pager.json</files>
  <action>Present the generated one-pager output to the user for quality review. Show the overall verdict and per-section verdicts, a sample of narrative content (company_info and overall_verdict sections), cost and timing from the generation run, and aggregated red flags. Ask the user to verify the output is concise, reasonable, and feels like a screening document rather than a full report.</action>
  <verify>User provides approval or feedback</verify>
  <done>User confirms the one pager output quality is acceptable for a screening document.</done>
  <what-built>
Single-call One Pager generator replacing the old 4-agent, 6-section parallel pipeline. One Sonnet API call produces a concise screening document with PASS/FAIL/WATCHLIST verdict. Output is backward-compatible with PDF generator and gate check.
  </what-built>
  <how-to-verify>
1. Check the generated one-pager at .thes1s/reports/COST/one-pager.json:
   - Is the overall verdict reasonable for Costco?
   - Are the section narratives concise (1-3 paragraphs each, not 10-page essays)?
   - Do the red flags make sense?
   - Does it feel like a "one pager" screening document?

2. Check the cost from the CLI output:
   - Should be well under $1 (vs $2-3 for the old multi-agent approach)
   - Should complete in under 3 minutes

3. Optionally run the PDF generator to verify visual output:
   cd scripts/pdf and run python3 generate_one_pager_pdf.py COST
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with the generated one pager quality/format</resume-signal>
</task>

</tasks>

<verification>
1. Live test produces valid one-pager.json for COST
2. All 6 sections present with required fields
3. PDF generator reads the output without errors
4. Gate check (overallVerdict != null) passes
5. CC skill updated and simplified
6. Cost under $1, runtime under 3 minutes
</verification>

<success_criteria>
- A real ticker one pager generates via single Sonnet call in under 3 minutes
- Output is backward-compatible (PDF gen, gate check, useOnePager hook all work)
- CC skill is simplified from 350 lines to under 100
- The one pager reads like a concise screening document, not a 10-page report
- Cost is significantly lower than the old multi-agent approach
</success_criteria>

<output>
After completion, create `.planning/phases/16.1-simplify-one-pager-pipeline/16.1-02-SUMMARY.md`
</output>
