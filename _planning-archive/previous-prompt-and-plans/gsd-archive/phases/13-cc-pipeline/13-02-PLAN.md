---
phase: 13-cc-pipeline
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - .claude/skills/generate-full-story/SKILL.md
autonomous: false
requirements: [ORCH-01]
must_haves:
  truths:
    - "Running /generate:full-story TICKER produces 5 sections (S1-S5) with correct keys from dispatch-table.json"
    - "Each generated section references specific Pitch Deck findings (not re-derived from scratch)"
    - "Checklist sections (S2, S3, S4) produce scored data with PASS/FAIL/PARTIAL verdicts per checklist-item.schema.json"
    - "The checkpoint displays checklist scores prominently (e.g., 12/15 PASS, 2 PARTIAL, 1 FAIL)"
    - "Gate check fails cleanly when no Pitch Deck exists, directing user to run Pitch Deck first"
    - "S6 (inversion_rebuttal) is a documented placeholder for Phase 14, not generated"
    - "Full Story output is labeled status: partial (5/6 sections) until Phase 14 adds S6"
    - "PM can ask questions routed to the specialist or request section re-runs at the checkpoint"
  artifacts:
    - path: ".claude/skills/generate-full-story/SKILL.md"
      provides: "Complete CC skill for Full Story generation pipeline"
      min_lines: 300
      contains: "generate-full-story"
  key_links:
    - from: ".claude/skills/generate-full-story/SKILL.md"
      to: "agents/orchestrator/dispatch-table.json"
      via: "reads fullStory config for section keys and agent assignments"
      pattern: "dispatch-table.json"
    - from: ".claude/skills/generate-full-story/SKILL.md"
      to: ".thes1s/reports/{TICKER}/pitch-deck.json"
      via: "gate check reads PD verdict, inheritance reads PD section data"
      pattern: "pitch-deck.json"
    - from: ".claude/skills/generate-full-story/SKILL.md"
      to: "agents/orchestrator/schemas/checklist-item.schema.json"
      via: "checklist sections must produce data conforming to this schema"
      pattern: "checklist-item.schema.json"
---

<objective>
Create the Full Story CC skill that orchestrates 5 sections with Pitch Deck inheritance and a checkpoint for PM review.

Purpose: This is the core deliverable of Phase 13 -- a Claude Code skill that generates the Full Story (Stage 3) by dispatching 5 specialist agents, each receiving relevant Pitch Deck findings as context. The skill follows the proven Pitch Deck CC skill pattern (gate check, config loading, prompt assembly, sequential dispatch, checkpoint dialogue) but is simpler: no data preparation, no PSR agents, single generation wave, 1 checkpoint. S6 (inversion_rebuttal) is a known placeholder for Phase 14's adversarial debate.

Output: `.claude/skills/generate-full-story/SKILL.md` (~400 lines)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cc-pipeline/13-CONTEXT.md
@.planning/phases/13-cc-pipeline/13-RESEARCH.md
@.planning/phases/13-cc-pipeline/13-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs -->

From agents/orchestrator/dispatch-table.json (fullStory):
- sectionKeys: ["event_analysis", "meaning_checklist", "moat_checklist", "management_checklist", "valuation_confirmation", "inversion_rebuttal"]
- Phase 1: 5 parallel agents (risk-analyst S1, business-analyst S2, competitor-evaluator S3, management-evaluator S4, valuation-specialist S5)
- Phase 2: 4-step debate (synthesis-writer bull, risk-analyst bear, synthesis-writer rebuttal, financial-analyst judge) -- Phase 14 scope

From agents/orchestrator/config.json (sectionMapping.fullStory):
- "1": "risk-analyst"
- "2": "business-analyst"
- "3": "competitor-evaluator"
- "4": "management-evaluator"
- "5": "valuation-specialist"
- "6": "synthesis-writer"

From agents/orchestrator/schemas/checklist-item.schema.json:
- checklistType: "meaning" | "moat" | "management"
- items[]: { number, item, verdict: "PASS"|"FAIL"|"PARTIAL", evidence, confidence }
- summary: { passCount, failCount, partialCount, totalItems, scoreDisplay }

From .claude/skills/generate-pitch-deck/SKILL.md patterns:
- Gate check: read report JSON, verify overallVerdict exists
- Agent dispatch: read config.json + prompt.md + curriculum + universalContext + DataPacket slice
- JSON extraction fallback: look for ```json block, extract and write via cat heredoc
- Narrative recovery: check narrative.length < 200, inject from Part 1 prose if found
- Retry: 30s wait, retry once, save status:failed if still broken
- Checkpoint: structured summary + conversational dialogue loop
- Section re-run: dispatch via /generate:section skill

Pitch Deck section keys (for inheritance mapping):
- pitchDeck sectionKeys: ["radar", "simple_predictable", "market_position", "barriers_moats", "fcf", "management", "roe_roic_debt", "balance_sheet", "pest", "valuation"]

PD inheritance map (per D-04):
- S1 event_analysis <- PD pest (S9) + radar (S1)
- S2 meaning_checklist <- PD simple_predictable (S2) + market_position (S3)
- S3 moat_checklist <- PD barriers_moats (S4) + market_position (S3)
- S4 management_checklist <- PD management (S6) + balance_sheet (S8)
- S5 valuation_confirmation <- PD fcf (S5) + roe_roic_debt (S7) + valuation (S10)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the generate-full-story CC skill</name>
  <files>.claude/skills/generate-full-story/SKILL.md</files>
  <read_first>
    - .claude/skills/generate-pitch-deck/SKILL.md (template -- the full ~1000 line skill to model after, but simpler)
    - .claude/skills/generate-section/SKILL.md (section re-run pattern used at checkpoint)
    - agents/orchestrator/dispatch-table.json (fullStory phase structure, agent assignments, sectionKeys)
    - agents/orchestrator/config.json (sectionMapping.fullStory for routing PM questions)
    - agents/orchestrator/schemas/checklist-item.schema.json (checklist data format for score extraction)
    - agents/risk-analyst/config.json (model, curriculum, dataPacketSlice for S1 event_analysis)
    - agents/business-analyst/config.json (model, curriculum, dataPacketSlice for S2 meaning_checklist)
    - agents/competitor-evaluator/config.json (model, curriculum, dataPacketSlice for S3 moat_checklist)
    - agents/management-evaluator/config.json (model, curriculum, dataPacketSlice for S4 management_checklist)
    - agents/valuation-specialist/config.json (model, curriculum, dataPacketSlice for S5 valuation_confirmation)
    - src/schemas/reportSection.js (ReportSectionSchema that all section outputs must conform to)
    - .planning/phases/13-cc-pipeline/13-CONTEXT.md (locked decisions D-01 through D-11)
    - .planning/phases/13-cc-pipeline/13-RESEARCH.md (architecture patterns, pitfalls, code examples)
  </read_first>
  <action>
    Create `.claude/skills/generate-full-story/SKILL.md` (~400 lines) implementing the Full Story CC skill pipeline. The skill follows the Pitch Deck CC skill pattern but is structurally simpler (no data prep, no PSR, single wave, 1 checkpoint).

    **SKILL.md frontmatter:**
    ```
    # ~~~~
    name: generate-full-story
    description: Generate a 5-section Rule One Full Story (Stage 3) deep analysis for a given stock ticker, building on Pitch Deck findings with scored checklists
    argument-hint: TICKER
    disable-model-invocation: true
    ~~~~
    ```

    **Step 1: Validate Input and Gate Check**
    - Ticker is `$0`. Uppercase it, store as `TICKER`.
    - If `$0` is empty, print usage: `/generate:full-story TICKER` and stop.
    - Create output directories: `.thes1s/reports/{TICKER}/` and `.thes1s/reports/{TICKER}/sections/`
    - **Gate Check (per D-09, D-11):** Read `.thes1s/reports/{TICKER}/pitch-deck.json`. Verify the file exists and `overallVerdict` is set (not null).
    - **Fallback (per Research Pitfall 2):** If `pitch-deck.json` not found, try `.thes1s/reports/{TICKER}/pipeline-output.json`. Check for `overallVerdict` or at least 10 sections with verdicts. If pipeline-output.json exists but has no `overallVerdict` field, check if sections exist with verdicts (the API pipeline stores verdict per section but may not have an `overallVerdict` top-level field -- treat as passed if 10 sections exist).
    - If both fail: print "Gate check FAILED: Pitch Deck must be completed before generating a Full Story.\nRun /generate:pitch-deck {TICKER} first." and stop.
    - If gate passes: log "Step 1: Gate check PASSED -- Pitch Deck verdict: {verdict}\nSetting up Full Story generation for {TICKER}..."

    **Step 2: Load Orchestrator and Agent Configurations**
    - Read `agents/orchestrator/dispatch-table.json` -- extract `fullStory` config (phase structure, agent assignments, sectionKeys)
    - Read `agents/orchestrator/config.json` -- extract `sectionMapping.fullStory` for routing PM questions at checkpoint
    - For each of these 5 agents: `risk-analyst`, `business-analyst`, `competitor-evaluator`, `management-evaluator`, `valuation-specialist`:
      - Read `agents/{agent-name}/config.json` (model, curriculum, dataPacketSlice, universalContext, universalContextFiles, tools)
      - Read `agents/{agent-name}/prompt.md` (full system prompt with Full Story instructions)
      - Read each curriculum file listed in config
      - If `universalContext: true`, read each file in `universalContextFiles`
    - Read `src/schemas/reportSection.js` -- extract the JSON Schema definition for section output validation
    - Read `agents/orchestrator/schemas/checklist-item.schema.json` -- needed for checkpoint score extraction
    - Log: "Step 2: Configurations loaded for 5 agents"

    **Step 3: Prepare Pitch Deck Inheritance (per D-03, D-04, D-05)**

    Define the inheritance mapping as a constant data structure at the top of this step (per CONTEXT specifics -- not scattered across prompt assembly):

    ```
    PD_INHERITANCE_MAP:
      event_analysis:         [pest, radar]
      meaning_checklist:      [simple_predictable, market_position]
      moat_checklist:         [barriers_moats, market_position]
      management_checklist:   [management, balance_sheet]
      valuation_confirmation: [fcf, roe_roic_debt, valuation]
    ```

    For each Full Story section key, read its inherited Pitch Deck section data:

    **Reading PD sections (per Research Pitfall 3 -- handle both formats):**
    1. First try: Read `.thes1s/reports/{TICKER}/sections/{pd_key}.json` (CC skill format -- individual files)
    2. If not found: Extract from `.thes1s/reports/{TICKER}/pitch-deck.json` sections array (match by `key` field)
    3. If not found: Extract from `.thes1s/reports/{TICKER}/pipeline-output.json` sections array (match by `key` field)
    4. **Key normalization for pipeline-output.json:** The API pipeline may use different section keys than the dispatch table (e.g., `barriers_and_moats` vs `barriers_moats`, `pest_risks` vs `pest`, `valuation_summary` vs `valuation`). When matching, try both the exact key AND common variants:
       - `barriers_moats` -> also try `barriers_and_moats`
       - `pest` -> also try `pest_risks`
       - `valuation` -> also try `valuation_summary`
       - `roe_roic_debt` -> also try `growth_metrics` (S7 in pipeline used this key)
       - `fcf` -> also try `growth_metrics` (S5 in pipeline used this key)
       - `simple_predictable` -> also try `minimum_standards`
       - `radar` -> also try `company_info`

    For each found PD section, format as inherited context:
    ```
    ## Prior Pitch Deck Analysis

    ### {PD Section Title} (Section {N})
    Verdict: {verdict} | Confidence: {confidence}

    Summary: {summary}

    Key Findings:
    {narrative, first 2000 chars -- enough for context, not full prose}

    Red Flags:
    - {red flags list}

    Citations:
    - {citations list}

    Data:
    {data field if present -- contains structured findings}
    ```

    Log: "Step 3: Pitch Deck inheritance prepared -- {N}/5 sections have PD context"

    **Step 4: Prepare DataPacket Slices (per D-10)**
    - Read `.thes1s/reports/{TICKER}/data-packet.json` (the existing DataPacket assembled during Pitch Deck generation -- NOT re-assembled)
    - For each of the 5 agents, slice the DataPacket per their `config.json` `dataPacketSlice` array
    - Always include regardless of config: `ticker`, `companyInfo`, `classification`, `caveats`
    - Log: "Step 4: DataPacket sliced for 5 agents"

    **Step 5: Dispatch 5 Agents Sequentially (per D-01, D-02, Research Pitfall 4)**

    Despite sections being logically independent (D-02), dispatch agents one at a time due to CC RAM constraint (same as Pitch Deck skill). The dispatch order is: S1, S2, S3, S4, S5.

    For each section, follow the standardized prompt assembly pattern from the Pitch Deck skill:

    **Per-agent prompt content** (concatenated in order):
    1. Agent prompt.md content (includes Full Story section instructions from Phase 12)
    2. Sliced DataPacket as fenced JSON code block labeled "DataPacket"
    3. Curriculum files from config.json
    4. Universal context files (if universalContext: true)
    5. ReportSectionSchema definition with output instruction: "Your output MUST be valid JSON conforming to this schema. Output ONLY the JSON object, no surrounding text."
    6. Inherited Pitch Deck section context (formatted per Step 3 for this section's PD dependencies)
    7. PSR findings: read `.thes1s/reports/{TICKER}/sections/annual-reader-insights.json` and `.thes1s/reports/{TICKER}/sections/quarterly-reader-insights.json` if they exist. Include as "Primary Source Reader Findings" context. If not found, skip silently.
    8. Task instruction: "Analyze {TICKER} and produce section {N} ({key}) of the Full Story as a JSON object conforming to ReportSectionSchema. Build on the Pitch Deck findings provided above -- cite specific findings from the prior analysis, do not re-derive from scratch. For checklist sections: your `data` field MUST contain a JSON string conforming to ChecklistSectionData schema (checklistType, items array with verdict/evidence/confidence per item, summary with scoreDisplay). Return the single JSON object."

    **After each agent completes:**

    a. **JSON extraction fallback:** Look for a ```json code block in the agent's response. If found, extract and write to `.thes1s/reports/{TICKER}/sections/fullStory-S{N}-{key}.json`. If the agent wrote the file directly, verify it exists.

    b. **Narrative recovery:** Check `narrative` field length. If < 200 chars, search the agent's response above the JSON block for Part 1 narrative prose (markdown with ## headings). If substantial prose found (> 200 chars), inject into the narrative field and re-save. If no recoverable narrative, retry once with enhanced instruction about full narrative requirement.

    c. **Validation:** Verify the section JSON has required fields: `key` (matches sectionKey), `title`, `sectionNumber`, `status`, `confidence`, `verdict`, `verdictRationale`, `summary`, `narrative`, `citations` (array), `redFlags` (array with >= 1 item).

    d. **Retry on failure (per Pitch Deck skill retry pattern):** If JSON parsing or validation fails, wait 30 seconds, construct retry prompt with error details, dispatch again. If retry also fails, save with `status: "failed"` and continue.

    e. **Log per section:** "  S{N} {key}: {verdict} | {confidence} | {citation_count} citations | {red_flag_count} red flags | narrative: {length} chars"

    **Section naming convention:** `fullStory-S{N}-{key}.json` (stage-prefixed per CONTEXT established pattern).

    Log:
    ```
    Step 5: Dispatching 5 agents sequentially (RAM constraint)...
      S1 event_analysis (risk-analyst): ...
      S2 meaning_checklist (business-analyst): ...
      S3 moat_checklist (competitor-evaluator): ...
      S4 management_checklist (management-evaluator): ...
      S5 valuation_confirmation (valuation-specialist): ...
    ```

    **Step 6: Checkpoint -- Full Story Deep Analysis Review (per D-06, D-07, D-08)**

    Print a structured checkpoint summary:

    ```
    ================================================================
      CHECKPOINT: Full Story Deep Analysis Review
    ================================================================

    Sections completed: 5/5 (S6 inversion_rebuttal deferred to Phase 14 debate)

    --- Section 1: Event Analysis ---
      Agent: risk-analyst
      Verdict: {verdict} | Confidence: {confidence}
      Summary: {summary, first 200 chars}
      Red Flags: {count} items
      Citations: {count} sources

    --- Section 2: Meaning Checklist ---
      Agent: business-analyst
      Score: {scoreDisplay}    <-- extracted from data field
      Verdict: {verdict} | Confidence: {confidence}
      Summary: {summary, first 200 chars}
      Red Flags: {count} items

    --- Section 3: Moat Checklist ---
      Agent: competitor-evaluator
      Score: {scoreDisplay}
      Verdict: {verdict} | Confidence: {confidence}
      Summary: {summary, first 200 chars}
      Red Flags: {count} items

    --- Section 4: Management Checklist ---
      Agent: management-evaluator
      Score: {scoreDisplay}
      Verdict: {verdict} | Confidence: {confidence}
      Summary: {summary, first 200 chars}
      Red Flags: {count} items

    --- Section 5: Valuation Confirmation ---
      Agent: valuation-specialist
      Verdict: {verdict} | Confidence: {confidence}
      Summary: {summary, first 200 chars}
      Red Flags: {count} items
      Citations: {count} sources

    --- Cross-Cutting Findings ---
      {aggregated crossCuttingFindings from all 5 sections}

    ================================================================
    ```

    **Checklist score extraction (per Research Pitfall 5):** For sections S2, S3, S4, parse the `data` field as JSON. Extract `summary.scoreDisplay` string. If parsing fails, log warning and show "Score: unable to parse" instead.

    **Conversational dialogue loop (per D-07):**
    ```
    Review the findings above. You can:
      - Ask a question about any section (the responsible analyst will answer)
      - Say "re-run section X" to regenerate with guidance (uses /generate:section)
      - Say "continue" to assemble the final report
      - Say "stop" to pause generation (progress is saved)

    Your input:
    ```

    Handle PM responses:
    - **Question about a section:** Identify which section using sectionMapping.fullStory. Dispatch the responsible agent with: the question + original section context + section output. Print the agent's answer.
    - **"re-run section X":** Invoke `/generate:section {TICKER} fullStory {X} "{optional guidance}"`. After re-run, update the checkpoint display with the new section data.
    - **"continue":** Advance to Step 7.
    - **"stop":** Save current state and stop. Progress can be resumed.

    **Step 7: Assemble Final Report (per D-08 -- extensible for Phase 14)**

    Collect all 5 section outputs + checkpoint data into the final report:

    ```json
    {
      "ticker": "{TICKER}",
      "companyName": "{from DataPacket companyInfo.name}",
      "stage": "fullStory",
      "generatedAt": "{ISO timestamp}",
      "status": "partial",
      "completedSections": 5,
      "totalSections": 6,
      "pendingPhase": "Phase 14 debate (S6 inversion_rebuttal)",
      "sections": [
        { S1 through S5 ReportSectionSchema objects ordered by sectionNumber }
      ],
      "checklistScores": {
        "meaning": "{scoreDisplay from S2 data}",
        "moat": "{scoreDisplay from S3 data}",
        "management": "{scoreDisplay from S4 data}"
      },
      "sectionKeys": ["event_analysis", "meaning_checklist", "moat_checklist", "management_checklist", "valuation_confirmation", "inversion_rebuttal"],
      "overallVerdict": null,
      "pitchDeckVerdict": "{verdict from gate check}"
    }
    ```

    Write to `.thes1s/reports/{TICKER}/full-story.json`.

    Generate human-readable markdown at `.thes1s/reports/{TICKER}/full-story.md`:
    - Header with company name, ticker, generated date, status: PARTIAL (5/6 sections)
    - Per-section: narrative, verdict, confidence, red flags, citations
    - Checklist sections: include the full scored table (item, verdict, evidence)
    - Note at the end: "S6 Inversion & Rebuttal will be added in Phase 14 (adversarial debate)"

    Log:
    ```
    Step 7: Report assembled
      Sections: 5/6 (partial -- S6 deferred to Phase 14)
      Checklist Scores:
        Meaning: {scoreDisplay}
        Moat: {scoreDisplay}
        Management: {scoreDisplay}
      Output: .thes1s/reports/{TICKER}/full-story.json
      Output: .thes1s/reports/{TICKER}/full-story.md
    ```

    **Step 8: Phase 2 -- THE DEBATE (Phase 14)**

    Include a clearly demarcated placeholder for Phase 14 (per D-08, Research Pattern 4):

    ```markdown
    ## Step 8: Phase 2 -- THE DEBATE (Phase 14)

    > This section is not yet implemented. Phase 14 will add the 4-step adversarial
    > debate (Bull -> Bear -> Bull Rebuttal -> Judge) that produces S6 (inversion_rebuttal).
    > Until then, S6 is omitted from the Full Story output.
    > The checkpoint after the debate will be added as Step 9.
    > See agents/orchestrator/dispatch-table.json fullStory.phases[1] for the debate structure.
    > See agents/orchestrator/schemas/debate-step.schema.json for the output format.
    ```

    **Constraints section** at the end of the skill:

    Include the same contamination boundary as Pitch Deck skill:
    ```
    ### Contamination Boundary (CRITICAL)
    During generation, NEVER read from any of these paths:
    - knowledge/stage-1-one-pager/examples/
    - knowledge/stage-2-pitch-deck/examples/
    - knowledge/stage-3-full-story/examples/
    - knowledge/pre-course-examples/
    ```

    Include error resilience rules:
    - If DataPacket has errors, log but proceed
    - If some PD sections are missing for inheritance, proceed with available context
    - Section saves happen before report assembly -- even if assembly fails, sections are preserved
    - Do NOT re-run data preparation (per D-09, D-10)
  </action>
  <verify>
    <automated>test -f /Users/kylehoff/Desktop/stock-analyzer/.claude/skills/generate-full-story/SKILL.md && wc -l /Users/kylehoff/Desktop/stock-analyzer/.claude/skills/generate-full-story/SKILL.md | awk '{if ($1 >= 300) print "PASS: "$1" lines"; else print "FAIL: only "$1" lines"}' && grep -q 'pitch-deck.json' /Users/kylehoff/Desktop/stock-analyzer/.claude/skills/generate-full-story/SKILL.md && echo "PASS: gate check present" && grep -q 'PD_INHERITANCE_MAP' /Users/kylehoff/Desktop/stock-analyzer/.claude/skills/generate-full-story/SKILL.md && echo "PASS: inheritance map present" && grep -q 'fullStory-S' /Users/kylehoff/Desktop/stock-analyzer/.claude/skills/generate-full-story/SKILL.md && echo "PASS: section naming present" && grep -q 'Contamination Boundary' /Users/kylehoff/Desktop/stock-analyzer/.claude/skills/generate-full-story/SKILL.md && echo "PASS: contamination boundary present"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.claude/skills/generate-full-story/SKILL.md`
    - File is >= 300 lines
    - File contains frontmatter with `name: generate-full-story` and `disable-model-invocation: true`
    - File contains "Gate check" step that reads pitch-deck.json with pipeline-output.json fallback
    - File contains PD_INHERITANCE_MAP with all 5 mappings: event_analysis->pest+radar, meaning_checklist->simple_predictable+market_position, moat_checklist->barriers_moats+market_position, management_checklist->management+balance_sheet, valuation_confirmation->fcf+roe_roic_debt+valuation
    - File contains key normalization for pipeline-output.json variant keys (barriers_and_moats, pest_risks, valuation_summary)
    - File contains sequential dispatch of 5 agents: risk-analyst, business-analyst, competitor-evaluator, management-evaluator, valuation-specialist
    - File contains checkpoint with scoreDisplay extraction from checklist section data fields
    - File contains "fullStory-S{N}-{key}.json" section naming convention
    - File contains report assembly writing to full-story.json with status: "partial" and overallVerdict: null
    - File contains Phase 14 debate placeholder (Step 8)
    - File contains Contamination Boundary section excluding example directories
    - File does NOT contain any reference to trading_strategy or pace_plan
    - File does NOT reference generating S6 inversion_rebuttal content
  </acceptance_criteria>
  <done>Complete Full Story CC skill exists at .claude/skills/generate-full-story/SKILL.md implementing the 7-step pipeline (gate check, config load, PD inheritance, DataPacket slice, 5-agent dispatch, checkpoint with checklist scores, report assembly) plus Phase 14 placeholder</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: PM reviews the Full Story CC skill</name>
  <files>.claude/skills/generate-full-story/SKILL.md</files>
  <action>
    Present the completed SKILL.md to the PM for review. Display:
    1. File location and line count
    2. Summary of the 8-step pipeline structure
    3. The PD inheritance mapping
    4. The checkpoint display format
    5. Ask PM to review and approve or request changes
  </action>
  <what-built>
    The generate-full-story CC skill (~400 lines) that orchestrates Full Story generation:
    1. Gate check (pitch-deck.json or pipeline-output.json)
    2. Orchestrator + agent config loading
    3. Pitch Deck inheritance with key normalization for both CC and API pipeline formats
    4. DataPacket slicing (reuses existing data, no re-assembly)
    5. Sequential dispatch of 5 agents (S1-S5)
    6. Checkpoint with checklist scores (Meaning X/15, Moat X/15, Management X/13)
    7. Final report assembly (full-story.json + full-story.md, status: partial)
    8. Phase 14 debate placeholder
  </what-built>
  <how-to-verify>
    1. Read `.claude/skills/generate-full-story/SKILL.md` -- review the full skill
    2. Verify the PD inheritance mapping matches your expectations (D-04)
    3. Verify the checkpoint display format shows checklist scores prominently
    4. Verify the gate check handles both pitch-deck.json and pipeline-output.json
    5. Verify the Phase 14 placeholder is clearly marked for future extension
    6. Optional: Run `/generate:full-story SFM` to test end-to-end on the existing SFM Pitch Deck data
  </how-to-verify>
  <verify>PM reviews the skill and types "approved" or describes issues</verify>
  <done>PM has reviewed and approved the Full Story CC skill</done>
  <resume-signal>Type "approved" or describe issues to address</resume-signal>
</task>

</tasks>

<verification>
1. `.claude/skills/generate-full-story/SKILL.md` exists with >= 300 lines
2. Skill contains all 7 implementation steps plus Phase 14 placeholder
3. Skill references correct dispatch-table.json section keys (6 fullStory keys)
4. Skill handles both CC and API pipeline PD formats for inheritance
5. Skill contains contamination boundary preventing example access
6. No references to trading_strategy or pace_plan in the skill
7. PM has reviewed and approved the skill
</verification>

<success_criteria>
- The Full Story CC skill is complete, PM-approved, and ready for live testing
- The skill follows all locked decisions (D-01 through D-11) from CONTEXT.md
- The skill is extensible for Phase 14 debate without rewriting
- Running the skill on a ticker with an existing Pitch Deck would produce 5 sections with checklist scores
</success_criteria>

<output>
After completion, create `.planning/phases/13-cc-pipeline/13-02-SUMMARY.md`
</output>
