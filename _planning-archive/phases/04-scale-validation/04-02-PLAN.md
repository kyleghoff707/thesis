---
phase: 04-scale-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - validation/reports/sp500-fmp-accuracy.json
  - validation/data/sp500-tickers.json
  - src/engines/edgarFinancials.js
  - src/engines/industryOverlays.js
  - validation/reports/sp500-outlier-investigation.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - SCALE-01
  - SCALE-02
  - SCALE-04

must_haves:
  truths:
    - "FMP data is fetched and cached for all 503 S&P 500 companies"
    - "First full S&P 500 comparison produces a baseline accuracy report"
    - "RACE (Ferrari EUR filer) is investigated and either fixed or excluded with documentation"
    - "Financial sector outliers (MET, WFC) are investigated using FMP as arbiter"
    - "Other low-accuracy companies (CRM, EW, EQIX) are investigated during fix+validate cycle"
    - "Engine fixes are applied only for FMP-confirmed Tier 1 bugs (per D-04)"
    - "50-company MS baseline remains at 94%+ after every engine fix (regression gate)"
    - "Tier 1 accuracy stabilizes (improvement < 0.5% between iterations)"
    - "REQUIREMENTS.md SCALE-01 definition is updated to match D-01 (94%+ target)"
  artifacts:
    - path: "validation/reports/sp500-fmp-accuracy.json"
      provides: "S&P 500 FMP accuracy report (final iteration)"
    - path: "validation/reports/sp500-outlier-investigation.md"
      provides: "Investigation notes for RACE, MET, WFC, CRM, EW, EQIX"
    - path: "validation/data/sp500-tickers.json"
      provides: "Cached S&P 500 ticker list from Wikipedia"
  key_links:
    - from: "src/engines/edgarFinancials.js"
      to: "validation/scripts/compare-sp500-fmp.mjs"
      via: "engine bundle rebuild + re-comparison after fixes"
      pattern: "node validation/scripts/bundle.mjs"
    - from: "validation/scripts/compare-morningstar.mjs"
      to: "validation/reports/morningstar-accuracy.json"
      via: "MS regression gate after engine fixes"
      pattern: "morningstar-accuracy"
---

<objective>
Run the full S&P 500 FMP comparison pipeline: fetch all data, run initial comparison, investigate outliers, apply iterative engine fixes for FMP-confirmed Tier 1 bugs, and re-validate until accuracy stabilizes. Also update REQUIREMENTS.md to align SCALE-01 with D-01's locked 94%+ target.

Purpose: This is the core validation loop that proves the normalization engine works at S&P 500 scale (per D-03: iterative fix+validate). Each fix must pass the MS regression gate (per D-01: 94%+ on 50-company truth set) and only target FMP-confirmed Tier 1 bugs (per D-04: fix boundary).

Output: A stable S&P 500 accuracy report, investigation documentation for all flagged outliers (per D-05/D-06/D-07), engine fixes that improve Tier 1 accuracy without regressing the MS baseline, and REQUIREMENTS.md updated to reflect the locked 94%+ target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-scale-validation/04-CONTEXT.md
@.planning/phases/04-scale-validation/04-RESEARCH.md
@.planning/phases/04-scale-validation/04-01-SUMMARY.md

@validation/scripts/compare-sp500-fmp.mjs
@validation/scripts/compare-morningstar.mjs
@validation/scripts/fetch-sp500-fmp.mjs
@validation/scripts/lib/sp500-fmp-comparator.mjs
@validation/scripts/lib/sp500-reporter.mjs
@validation/scripts/lib/field-alias-map.mjs
@validation/scripts/bundle.mjs
@src/engines/edgarFinancials.js
@src/engines/industryOverlays.js
@src/engines/industryClassifier.js

<interfaces>
<!-- Key contracts from Plan 01 outputs -->

From validation/scripts/compare-sp500-fmp.mjs:
```bash
# Full run (all 503 companies):
node validation/scripts/compare-sp500-fmp.mjs

# Single ticker debug:
node validation/scripts/compare-sp500-fmp.mjs --ticker AAPL

# Show all companies in console output:
node validation/scripts/compare-sp500-fmp.mjs --show-all

# Output: console report to stdout, JSON to validation/reports/sp500-fmp-accuracy.json
```

From validation/scripts/fetch-sp500-fmp.mjs:
```bash
# Batch fetch all 503 S&P 500 companies from FMP:
node validation/scripts/fetch-sp500-fmp.mjs

# Caches to validation/cache/fmp/{TICKER}-fmp.json (7-day TTL)
# Caches ticker list to validation/data/sp500-tickers.json
```

From validation/scripts/lib/sp500-reporter.mjs:
```javascript
// JSON report structure:
{
  summary: { tier1Accuracy, tier2Accuracy, tier3Accuracy, overallAccuracy, ... },
  topTier1Failures: [{ field, companyCount, avgPct }],
  companies: [{ ticker, tier1Accuracy, overallAccuracy, topFailures }],
}
```

Fix+validate cycle pattern (from Phase 3):
```bash
# 1. Make engine fix in edgarFinancials.js or industryOverlays.js
# 2. Rebuild bundle
node validation/scripts/bundle.mjs
# 3. Check MS regression (must stay 94%+)
node validation/scripts/compare-morningstar.mjs
# 4. Check S&P 500 improvement
node validation/scripts/compare-sp500-fmp.mjs
# 5. Repeat until Tier 1 accuracy stabilizes
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fetch FMP data + run initial S&P 500 comparison + investigate outliers + update REQUIREMENTS.md</name>
  <files>validation/data/sp500-tickers.json, validation/reports/sp500-fmp-accuracy.json, validation/reports/sp500-outlier-investigation.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - validation/scripts/fetch-sp500-fmp.mjs (to understand fetch invocation)
    - validation/scripts/compare-sp500-fmp.mjs (to understand comparison invocation)
    - validation/scripts/lib/sp500-reporter.mjs (to understand report format)
    - validation/scripts/lib/sp500-fmp-comparator.mjs (to understand FIELD_TIERS and comparison logic)
    - .planning/phases/04-scale-validation/04-CONTEXT.md (D-05: RACE EUR, D-06: MET/WFC financial sector, D-07: CRM/EW/EQIX)
    - .planning/REQUIREMENTS.md (to update SCALE-01 definition)
    - src/engines/industryOverlays.js (for financial sector overlay logic)
    - src/engines/industryClassifier.js (for SIC-based industry detection)
  </read_first>
  <action>
**Step 0: Update REQUIREMENTS.md SCALE-01 definition (per D-01)**

Read `.planning/REQUIREMENTS.md`. Find the line for SCALE-01 which currently reads:
```
- [ ] **SCALE-01**: 98%+ accuracy on 50-company Morningstar truth set (annual financials)
```

Update it to:
```
- [ ] **SCALE-01**: 94%+ accuracy on 50-company Morningstar truth set (annual financials) — revised from 98% per D-01; remaining diffs are methodology, not bugs
```

Also update the "Core Value" line at the top from "98%+ accuracy match" to "94%+ accuracy match" to be consistent.

Also update SCALE-03 to reflect D-09 (deferred):
```
- [ ] **SCALE-03**: Structural validation across US-listed equities beyond S&P 500 (deferred per D-09 — S&P 500 validation sufficient for this milestone)
```

**Step 1: Fetch all FMP data (single session)**

Run: `node validation/scripts/fetch-sp500-fmp.mjs`

Note: D-10 originally suggested a 2-day batch, but with FMP Starter plan rate limits (250 calls/day, each ticker = 3 calls), the actual fetching strategy is determined by the rate limiter in the fetch script. The 200ms delay between tickers keeps us well under the per-minute limit; the daily limit may require stopping and resuming. If the script hits a rate limit error, wait and re-run — the disk cache means it picks up where it left off.

This fetches all 503 S&P 500 tickers from FMP with 200ms rate limiting. 50 tickers already cached from Phase 2 will be reused if within 7-day TTL. Wait for completion and verify: should report ~453 fresh fetches + ~50 cache hits + few failures.

If any tickers fail, note them. Common reasons: ticker changed (recent S&P 500 changes), FMP doesn't have the ticker.

**Step 2: Run initial full comparison**

Run: `node validation/scripts/compare-sp500-fmp.mjs`

This produces:
- Console report to stdout with Tier 1/2/3 accuracy breakdown
- JSON report at `validation/reports/sp500-fmp-accuracy.json`

Record the initial Tier 1 accuracy as the baseline for this plan. This is the number we'll try to improve.

**Step 3: Investigate outliers (per D-05, D-06, D-07)**

Create `validation/reports/sp500-outlier-investigation.md` documenting findings for each outlier.

**RACE (D-05 -- EUR filer):**
- Run: `node validation/scripts/compare-sp500-fmp.mjs --ticker RACE`
- Check if RACE is properly skipped (SKIP_EUR status). If yes, document: "RACE files in EUR. Excluded from accuracy calculation with SKIP_EUR status. FMP may report USD-converted values; our engine reports raw XBRL EUR values. Currency conversion is out of scope."
- If not properly skipped, add RACE to the EUR_COMPANIES set in `comparator.mjs`.

**Financial sector (D-06 -- MET, WFC):**
- Run `--ticker MET` and `--ticker WFC` individually.
- For each: examine the Tier 1 failure patterns from the JSON report. Focus on which specific Tier 1 fields have DIFFs.
- Compare FMP values against our engine values for the top failing fields. Determine:
  - If our industry overlay is producing wrong values (fixable in industryOverlays.js)
  - If FMP uses different definitions than our overlays (METHODOLOGY_DIFF -- document but don't fix)
  - If the issue is a tag resolution gap (fixable in edgarFinancials.js)
- Document findings per company with specific field-level analysis.

**Other outliers (D-07 -- CRM, EW, EQIX):**
- Run `--ticker CRM,EW,EQIX` to see their accuracy profiles.
- Examine top Tier 1 failure patterns. Common causes from Phase 3 experience: fiscal year misalignment for non-Dec FY companies, restated vs original financial differences, M&A activity distorting historical comparison.
- Document findings. If any failures are clearly engine bugs (not methodology diffs), note them for the fix cycle in Task 2.

**Step 4: Analyze top Tier 1 failure patterns across all 503 companies**

From the JSON report's `topTier1Failures` array, identify the top 5 Tier 1 fields by company count. For each, determine whether it's:
- A systematic engine bug (same field wrong for 20+ companies) -- candidate for fix
- An FMP-specific methodology diff (e.g., FMP calculates depreciation differently) -- document as METHODOLOGY_DIFF
- A data coverage gap (engine returns null, FMP has value) -- investigate tag gaps

Document the analysis in the outlier investigation file, section "Systematic Tier 1 Failure Analysis".
  </action>
  <verify>
    <automated>test -f validation/data/sp500-tickers.json && test -f validation/reports/sp500-fmp-accuracy.json && test -f validation/reports/sp500-outlier-investigation.md && echo "All artifacts exist" && node -e "const r=JSON.parse(require('fs').readFileSync('validation/reports/sp500-fmp-accuracy.json','utf8')); console.log('Companies:', r.summary.totalCompanies, 'Tier1:', r.summary.tier1Accuracy?.toFixed(1)+'%')" && grep "94%+" .planning/REQUIREMENTS.md | head -1</automated>
  </verify>
  <acceptance_criteria>
    - validation/data/sp500-tickers.json exists and contains 500+ tickers
    - validation/reports/sp500-fmp-accuracy.json exists and contains summary.tier1Accuracy as a number
    - validation/reports/sp500-fmp-accuracy.json contains summary.totalCompanies >= 490
    - validation/reports/sp500-outlier-investigation.md contains sections for "RACE", "MET", "WFC", "CRM", "EW", "EQIX"
    - validation/reports/sp500-outlier-investigation.md contains "Systematic Tier 1 Failure Analysis" section
    - validation/cache/fmp/ directory contains 490+ files (one per S&P 500 ticker)
    - .planning/REQUIREMENTS.md SCALE-01 reads "94%+ accuracy" (not "98%+"), matching D-01
    - .planning/REQUIREMENTS.md Core Value line reads "94%+ accuracy match" (not "98%+")
  </acceptance_criteria>
  <done>
    - FMP data cached for 490+ S&P 500 companies
    - Initial S&P 500 FMP accuracy baseline established in sp500-fmp-accuracy.json
    - All 6 outlier companies investigated and documented with field-level analysis
    - Top Tier 1 failure patterns identified and categorized (bug vs methodology vs coverage)
    - REQUIREMENTS.md SCALE-01 updated from 98%+ to 94%+ per D-01
  </done>
</task>

<task type="auto">
  <name>Task 2: Iterative fix+validate cycle for FMP-confirmed Tier 1 bugs</name>
  <files>src/engines/edgarFinancials.js, src/engines/industryOverlays.js, validation/reports/sp500-fmp-accuracy.json</files>
  <read_first>
    - validation/reports/sp500-fmp-accuracy.json (initial baseline from Task 1)
    - validation/reports/sp500-outlier-investigation.md (outlier findings from Task 1)
    - src/engines/edgarFinancials.js (engine code to fix)
    - src/engines/industryOverlays.js (overlay code to fix for financial sector)
    - validation/scripts/lib/sp500-fmp-comparator.mjs (FIELD_TIERS for tier classification)
    - validation/scripts/lib/field-alias-map.mjs (field name aliases)
    - .planning/phases/04-scale-validation/04-CONTEXT.md (D-03: iterative fix+validate, D-04: only fix FMP-confirmed Tier 1 bugs)
  </read_first>
  <action>
Execute the iterative fix+validate cycle following Phase 3's proven pattern. Each iteration:

**Iteration Loop (maximum 5 iterations):**

1. **Identify highest-impact Tier 1 fix**: From the JSON report's `topTier1Failures` and the outlier investigation, pick the Tier 1 failure pattern that affects the MOST companies and is clearly an engine bug (not methodology diff).

2. **Apply fix in edgarFinancials.js or industryOverlays.js**: Make the targeted change. Only fix what FMP confirms as wrong per D-04. DO NOT chase:
   - Tier 2 or Tier 3 disagreements
   - Methodology diffs (FMP calculates differently)
   - Fields where our engine value is arguably correct and FMP's is the outlier

3. **Rebuild engine bundle**: `node validation/scripts/bundle.mjs`

4. **MS regression gate**: Run `node validation/scripts/compare-morningstar.mjs` and check the accuracy. It MUST remain >= 94.0%. If it drops below 94.0%, REVERT the fix immediately and document why.

5. **S&P 500 progress check**: `node validation/scripts/compare-sp500-fmp.mjs`
   - Compare new Tier 1 accuracy against previous iteration.
   - Record the delta.

6. **Stabilization check**: Stop iterating when ANY of these conditions are met:
   - Tier 1 accuracy improvement < 0.5% from previous iteration AND no Tier 1 failure pattern affects 5+ companies with a clear fix
   - Maximum 5 iterations reached (if cap reached, document remaining patterns and surface to user in Task 3 checkpoint — do NOT continue beyond 5 iterations)

**Important constraints:**
- The RESEARCH notes that FMP fiscal year uses `fiscalYear` field -- if you see systematic 1-year offsets for non-December FY companies (NKE, WMT, FDX, COST), this is a fiscal year alignment issue, not an engine bug. Document it but don't "fix" it by shifting engine data.
- Financial sector fixes (per D-06) should go in industryOverlays.js. If FMP confirms our bank/REIT/insurance overlay values are wrong for specific fields, fix the overlay tags. If FMP and our engine agree but MS was different, the overlay is correct -- document this finding.
- Keep each fix atomic: one fix per iteration, verified before moving to the next. This prevents fix interactions that are hard to debug.

**After all iterations (or after reaching 5-iteration cap):**
- Run final `node validation/scripts/compare-sp500-fmp.mjs` to produce the definitive sp500-fmp-accuracy.json.
- Run final `node validation/scripts/compare-morningstar.mjs` to confirm MS baseline holds.
- Update the outlier investigation document with any new findings from the fix cycle.
- If iterations were capped at 5 with remaining fixable patterns, add a section to sp500-outlier-investigation.md titled "Remaining Patterns (Surfaced to User)" listing each pattern, affected company count, and why it was not fixed in this cycle.

**If no fixable Tier 1 bugs are found (accuracy is already good):**
- Add to sp500-outlier-investigation.md: "Initial S&P 500 Tier 1 accuracy is X.X%. No systematic Tier 1 failure patterns affect 5+ companies with clear engine fixes. Phase 3 fixes generalized well to S&P 500 scale. No fixable bugs found."
- Skip further iterations and proceed to the checkpoint.
  </action>
  <verify>
    <automated>node -e "const ms=JSON.parse(require('fs').readFileSync('validation/reports/morningstar-accuracy.json','utf8')); const acc=ms.summary?.accuracy ?? ms.accuracy ?? ms.overall_accuracy; console.log('MS accuracy:', typeof acc === 'number' ? acc.toFixed(1)+'%' : JSON.stringify(acc)); if (typeof acc === 'number' && acc < 94.0) { process.exit(1); }" && echo "---" && node -e "const r=JSON.parse(require('fs').readFileSync('validation/reports/sp500-fmp-accuracy.json','utf8')); console.log('S&P 500 Tier 1:', r.summary.tier1Accuracy?.toFixed(1)+'%', 'Overall:', r.summary.overallAccuracy?.toFixed(1)+'%')"</automated>
  </verify>
  <acceptance_criteria>
    - validation/reports/morningstar-accuracy.json shows overall accuracy >= 94.0% (MS regression gate holds per D-01)
    - validation/reports/sp500-fmp-accuracy.json shows final Tier 1 accuracy as a number (no NaN, no null)
    - validation/reports/sp500-fmp-accuracy.json summary.totalCompanies >= 490
    - validation/reports/sp500-outlier-investigation.md contains either a fix log for each iteration OR "No fixable bugs found" or "no fixable bugs found" phrase (unconditional -- must be present regardless of path taken)
    - If engine fixes were applied: validation/scripts/bundled-engines.mjs was rebuilt (check timestamp newer than edgarFinancials.js)
    - Fix cycle completed within 5 iterations (or fewer if stabilization criteria met earlier)
  </acceptance_criteria>
  <done>
    - Iterative fix+validate cycle complete (either fixes applied or "no fixable bugs found" documented in sp500-outlier-investigation.md)
    - MS 50-company accuracy remains >= 94.0% (verified via morningstar-accuracy.json, not by re-running the slow comparison script)
    - S&P 500 Tier 1 accuracy is stable (delta < 0.5% between last two iterations, or 5-iteration cap reached)
    - All engine changes (if any) are atomic, verified, and documented
    - Final sp500-fmp-accuracy.json reflects the definitive S&P 500 accuracy numbers
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User review of S&P 500 accuracy results and fix cycle outcomes</name>
  <files>validation/reports/sp500-fmp-accuracy.json, validation/reports/sp500-outlier-investigation.md</files>
  <action>
Present the S&P 500 FMP comparison results and fix cycle outcomes for user review.

Show the user:
1. The sp500-fmp-accuracy.json summary (tier1Accuracy, tier2Accuracy, overallAccuracy, topTier1Failures)
2. The outlier investigation findings for RACE, MET, WFC, CRM, EW, EQIX
3. The MS regression gate status (must be >= 94.0%)
4. Any engine fixes that were applied during the fix+validate cycle
5. Whether accuracy has stabilized
6. If the 5-iteration cap was reached: present the remaining patterns from "Remaining Patterns (Surfaced to User)" section and ask if user wants to continue fixing or accept current accuracy

Ask the user to review and approve before proceeding to Plan 03 (accounting identity checks).

Verification steps for the user:
1. Review `validation/reports/sp500-fmp-accuracy.json` -- check tier1Accuracy, overall accuracy, and topTier1Failures
2. Review `validation/reports/sp500-outlier-investigation.md` -- verify RACE, MET, WFC, CRM, EW, EQIX are investigated with field-level analysis
3. Run `node validation/scripts/compare-morningstar.mjs` -- confirm 94%+ MS accuracy maintained
4. Run `node validation/scripts/compare-sp500-fmp.mjs --ticker AAPL` -- verify tiered output format looks correct
5. Check if any engine fixes were applied and whether they make sense
6. Decide: Are the accuracy numbers acceptable? Should more fixes be attempted? Or is this good to ship?
  </action>
  <verify>
    <automated>echo "Checkpoint: awaiting user approval"</automated>
  </verify>
  <done>User types "approved" to proceed to Plan 03, or describes issues to address</done>
</task>

</tasks>

<verification>
1. `node -e "const ms=JSON.parse(require('fs').readFileSync('validation/reports/morningstar-accuracy.json','utf8')); const acc=ms.summary?.accuracy ?? ms.accuracy; console.log('MS accuracy:', acc)"` -- MS accuracy >= 94.0% (fast JSON read, no re-run)
2. `node -e "const r=JSON.parse(require('fs').readFileSync('validation/reports/sp500-fmp-accuracy.json','utf8')); console.log(JSON.stringify(r.summary, null, 2))"` -- S&P 500 accuracy numbers present and reasonable
3. `test -f validation/reports/sp500-outlier-investigation.md && grep -c "##" validation/reports/sp500-outlier-investigation.md` -- Investigation document exists with sections
4. `ls validation/cache/fmp/ | wc -l` -- 490+ cached FMP files
5. `grep "94%+" .planning/REQUIREMENTS.md | head -2` -- SCALE-01 updated to 94%+
</verification>

<success_criteria>
- FMP data fetched and cached for all 503 S&P 500 companies
- Initial and final S&P 500 comparison reports generated (console + JSON)
- RACE, MET, WFC, CRM, EW, EQIX all investigated with documented findings
- MS 50-company baseline remains >= 94.0% after any engine fixes
- S&P 500 Tier 1 accuracy stabilized (no more clear improvement opportunities, or 5-iteration cap reached)
- REQUIREMENTS.md SCALE-01 updated to 94%+ per D-01
- User has reviewed and approved the results
</success_criteria>

<output>
After completion, create `.planning/phases/04-scale-validation/04-02-SUMMARY.md`
</output>
