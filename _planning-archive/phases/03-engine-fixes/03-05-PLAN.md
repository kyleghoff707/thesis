---
phase: 03-engine-fixes
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/engines/edgarFinancials.js
  - src/engines/__tests__/edgarFinancials.test.js
autonomous: true
gap_closure: true
requirements: [ENGINE-01, ENGINE-02]

must_haves:
  truths:
    - "OtherNonCurrentAssets residual computed when named NCA item coverage >= 95%"
    - "OtherNonCurrentLiabilities residual computed when named NCL item coverage >= 95%"
    - "OtherCurrentAssets residual computed when named CA item coverage >= 95%"
    - "All residual computations have overcounting guard (negative residual -> null)"
    - "All residual computations have no-overwrite rule (existing XBRL values preserved)"
  artifacts:
    - path: "src/engines/edgarFinancials.js"
      provides: "Three new residual computations in computeDerivedFields"
      contains: "other_noncurrent_assets"
    - path: "src/engines/__tests__/edgarFinancials.test.js"
      provides: "Tests for OtherNCA, OtherNCL, OtherCA residuals"
  key_links:
    - from: "src/engines/edgarFinancials.js"
      to: "computeDerivedFields"
      via: "residual computation with 95% coverage gate"
      pattern: "Coverage >= 0.95"
---

<objective>
Add residual "Other" field computation for three balance sheet categories: OtherNonCurrentAssets, OtherNonCurrentLiabilities, and OtherCurrentAssets. These follow the same pattern as the existing OtherCurrentLiabilities residual (Plan 03) — gated at 95% named item coverage to prevent B7 error amplification.

Purpose: Fix ~170 residual Other DIFFs in the MS comparison by computing residual values that match Morningstar's DataID methodology. Currently only OtherCL is computed; OtherNCA (62 DIFFs), OtherNCL (53 DIFFs), and OtherCA (25 DIFFs) are not.

Output: Updated edgarFinancials.js with three new residual blocks + tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-engine-fixes/03-03-SUMMARY.md
@.planning/phases/03-engine-fixes/03-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement OtherNCA, OtherNCL, and OtherCA residual computations with 95% coverage gates</name>
  <files>src/engines/edgarFinancials.js, src/engines/__tests__/edgarFinancials.test.js</files>
  <read_first>
    - src/engines/edgarFinancials.js (lines 1137-1161 for existing OtherCL pattern, lines 200-400 for BALANCE_TAXONOMY named items)
    - src/engines/__tests__/edgarFinancials.test.js (lines for "Residual Other computation" describe block — the existing 8 OtherCL tests to replicate pattern)
    - .planning/phases/03-engine-fixes/03-RESEARCH.md (Category F: Residual Other Fields section — confirmed MS formulas from DataID and QuantConnect/LEAN reverse engineering)
  </read_first>
  <behavior>
    - Test: OtherNCA computed when 6/6 named NCA items present, result = assets - current_assets - property_plant_equipment - goodwill - intangible_assets - long_term_investments - deferred_tax_assets - operating_lease_rou_asset (note: PPE already includes ROU, but rou_asset is the raw pre-merge value; use noncurrent_assets - named items)
    - Test: OtherNCA NOT computed when coverage < 95% (5/6 named items present)
    - Test: OtherNCA negative residual -> null (overcounting guard)
    - Test: OtherNCA existing XBRL value preserved (no-overwrite)
    - Test: OtherNCL computed when 5/5 named NCL items present, result = noncurrent_liabilities - long_term_debt - operating_lease_liability_noncurrent - finance_lease_liability_noncurrent - deferred_tax_liabilities - pension_liabilities - deferred_revenue_noncurrent
    - Test: OtherNCL NOT computed when coverage < 95%
    - Test: OtherNCL negative residual -> null
    - Test: OtherNCL existing XBRL value preserved
    - Test: OtherCA computed when 5/5 named CA items present, result = current_assets - cash - short_term_investments - accounts_receivable - inventory - prepaid_expenses
    - Test: OtherCA NOT computed when coverage < 95%
    - Test: OtherCA negative residual -> null
    - Test: OtherCA existing XBRL value preserved
  </behavior>
  <action>
    **Write tests first (RED):**

    Add three new describe blocks inside the existing "Residual Other computation" section of edgarFinancials.test.js. Follow the exact pattern of the 8 existing OtherCL tests. Each residual field gets 4 tests: (1) computes when coverage >= 95%, (2) does NOT compute when coverage < 95%, (3) negative residual -> null, (4) existing XBRL preserved.

    **OtherNonCurrentAssets formula:**
    ```
    OtherNCA = noncurrent_assets - property_plant_equipment - goodwill - intangible_assets - long_term_investments - deferred_tax_assets - operating_lease_rou_asset
    ```
    Named NCA items (6 items, gate at 95% = need all 6):
    - property_plant_equipment (NOTE: this is the post-ROU-merge value from line ~987)
    - goodwill
    - intangible_assets
    - long_term_investments
    - deferred_tax_assets
    - operating_lease_rou_asset

    IMPORTANT: noncurrent_assets is derived earlier (line ~1005: assets - current_assets). The residual computation must come AFTER noncurrent_assets is derived. Place the OtherNCA block after line 1007 (after noncurrent_assets derivation) but within the balance sheet derived section.

    Wait — the OtherNCA residual needs the post-ROU-merge PP&E value. But the ROU merge happens at line 986-988. The named items list should use the raw pre-merge property_plant_equipment_net value and raw operating_lease_rou_asset separately to avoid double-counting. Actually, the simpler approach: use noncurrent_assets as the total, and subtract each named item individually. Since PP&E at this point already includes ROU (line 986-988), we should NOT also subtract operating_lease_rou_asset — that would double-count.

    CORRECTED approach: Use noncurrent_assets (which = assets - current_assets, post-ROU-merge-into-PPE). Subtract: property_plant_equipment (already includes ROU), goodwill, intangible_assets, long_term_investments, deferred_tax_assets. That's 5 items, not 6. The ROU asset is already inside PP&E.

    Named NCA items for coverage gate (5 items):
    - property_plant_equipment (post-ROU-merge)
    - goodwill
    - intangible_assets
    - long_term_investments
    - deferred_tax_assets

    Residual = noncurrent_assets - sum(5 named items)

    **OtherNonCurrentLiabilities formula:**
    ```
    OtherNCL = noncurrent_liabilities - long_term_debt - operating_lease_liability_noncurrent - finance_lease_liability_noncurrent - deferred_tax_liabilities - pension_liabilities - deferred_revenue_noncurrent
    ```
    Named NCL items (6 items, gate at 95% = need 6/6):
    - long_term_debt
    - operating_lease_liability_noncurrent
    - finance_lease_liability_noncurrent
    - deferred_tax_liabilities
    - pension_liabilities
    - deferred_revenue_noncurrent

    Residual = noncurrent_liabilities - sum(6 named items)

    Prereq: noncurrent_liabilities must be non-null (derived at line 1010-1012).

    **OtherCurrentAssets formula:**
    ```
    OtherCA = current_assets - cash - short_term_investments - accounts_receivable - inventory - prepaid_expenses
    ```
    Named CA items (5 items, gate at 95% = need 5/5):
    - cash
    - short_term_investments
    - accounts_receivable
    - inventory
    - prepaid_expenses

    Residual = current_assets - sum(5 named items)

    **Implementation (GREEN):**

    In computeDerivedFields, add three new residual blocks. Place them:
    - OtherNCA: after the noncurrent_assets derivation (after line ~1007), near the existing OtherCL block
    - OtherNCL: after the noncurrent_liabilities derivation (after line ~1011)
    - OtherCA: before the noncurrent_assets derivation (since it only needs current_assets)

    Actually, for code organization, place ALL residual blocks together near the existing OtherCL block (lines 1137-1161). This keeps the pattern visible. The prereqs (noncurrent_assets, noncurrent_liabilities) are derived earlier, so ordering is fine.

    Each block follows the exact pattern of OtherCL (lines 1137-1161):
    ```javascript
    if (bal.other_noncurrent_assets == null && bal.noncurrent_assets != null) {
      const ncaNamedItems = [
        bal.property_plant_equipment,
        bal.goodwill,
        bal.intangible_assets,
        bal.long_term_investments,
        bal.deferred_tax_assets,
      ];
      const ncaCoverage = ncaNamedItems.filter(v => v != null).length / ncaNamedItems.length;
      if (ncaCoverage >= 0.95) {
        const namedSum = ncaNamedItems.reduce((sum, v) => sum + (v ?? 0), 0);
        const residual = bal.noncurrent_assets - namedSum;
        if (residual >= 0) {
          bal.other_noncurrent_assets = residual;
        }
      }
    }
    ```

    Follow the same pattern for OtherNCL and OtherCA.

    Also add getDerivedFormula entries for the three new fields:
    - 'other_noncurrent_assets': 'noncurrent_assets - property_plant_equipment - goodwill - intangible_assets - long_term_investments - deferred_tax_assets'
    - 'other_noncurrent_liabilities': 'noncurrent_liabilities - long_term_debt - operating_lease_noncurrent - finance_lease_noncurrent - deferred_tax_liabilities - pension - deferred_revenue_noncurrent'
    - 'other_current_assets': 'current_assets - cash - short_term_investments - accounts_receivable - inventory - prepaid_expenses'
  </action>
  <verify>
    <automated>cd /Users/kylehoff/gsd-workspaces/normalization-engine/stock-analyzer && npx vitest run src/engines/__tests__/edgarFinancials.test.js</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "other_noncurrent_assets" src/engines/edgarFinancials.js returns >= 5 (field in residual block + coverage gate + formula lines)
    - grep -c "other_noncurrent_liabilities" src/engines/edgarFinancials.js returns >= 5
    - grep -c "other_current_assets" src/engines/edgarFinancials.js returns >= 5 (must include the NEW residual block, not just the existing taxonomy entry)
    - grep "ncaCoverage >= 0.95" src/engines/edgarFinancials.js returns a match
    - grep "nclCoverage >= 0.95" src/engines/edgarFinancials.js returns a match
    - grep "caCoverage >= 0.95" src/engines/edgarFinancials.js returns a match
    - grep -c "Residual Other" src/engines/__tests__/edgarFinancials.test.js returns >= 4 (existing OtherCL + 3 new describe blocks)
    - npx vitest run src/engines/__tests__/edgarFinancials.test.js passes with 0 failures
    - Total test count >= 63 (51 existing + 12 new: 4 per residual field x 3 fields)
  </acceptance_criteria>
  <done>
    Three new residual "Other" computations (OtherNCA, OtherNCL, OtherCA) implemented in computeDerivedFields with 95% coverage gates, overcounting guards, and no-overwrite rules. 12+ new tests pass. All existing 51 tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate residual Other improvements against MS comparison</name>
  <files>validation/reports/morningstar-accuracy.json</files>
  <read_first>
    - validation/scripts/compare-morningstar.mjs (to understand how to run the comparison)
    - validation/scripts/bundle.mjs (must rebuild engine bundle before comparison)
  </read_first>
  <action>
    Run the full 50-company MS comparison to measure the impact of the three new residual computations.

    Steps:
    1. Rebuild the engine bundle: `node validation/scripts/bundle.mjs`
    2. Run MS comparison: `node validation/scripts/compare-morningstar.mjs`
    3. Read the new morningstar-accuracy.json and compare against the pre-gap-closure baseline (91.1% accuracy, 1244 DIFFs)
    4. Count how many OtherNCA, OtherNCL, OtherCA DIFFs remain vs the 170 from baseline
    5. Verify no regressions: total DIFFs should not increase in any OTHER field category

    Expected: Some reduction in the 170 residual Other DIFFs. The exact number depends on how many companies have 95%+ named item coverage for NCA/NCL/CA categories. Conservative estimate: 40-80 DIFFs fixed (many companies lack one or more named items, failing the 95% gate).

    If DIFFs increase for any field, investigate immediately — the overcounting guard should prevent this, but verify.
  </action>
  <verify>
    <automated>cd /Users/kylehoff/gsd-workspaces/normalization-engine/stock-analyzer && node validation/scripts/bundle.mjs && node validation/scripts/compare-morningstar.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - node validation/scripts/bundle.mjs exits with code 0
    - node validation/scripts/compare-morningstar.mjs exits with code 0
    - morningstar-accuracy.json exists and has overallAccuracy >= 91.1 (no regression)
    - The combined count of other_noncurrent_assets + other_noncurrent_liabilities + other_current_assets + other_current_liabilities DIFFs is <= 170 (no regression on residual fields)
  </acceptance_criteria>
  <done>
    MS comparison confirms no regression from residual Other improvements. Accuracy >= 91.1%. Residual Other DIFF count documented.
  </done>
</task>

</tasks>

<verification>
- All edgarFinancials tests pass (51 existing + 12 new = 63+)
- MS comparison accuracy >= 91.1% (no regression)
- Residual Other DIFF count reduced or unchanged
</verification>

<success_criteria>
Three new residual "Other" balance sheet computations (OtherNCA, OtherNCL, OtherCA) implemented with the same 95% coverage gate pattern as the existing OtherCL. No regressions on any field. MS accuracy stable or improved.
</success_criteria>

<output>
After completion, create `.planning/phases/03-engine-fixes/03-05-SUMMARY.md`
</output>
