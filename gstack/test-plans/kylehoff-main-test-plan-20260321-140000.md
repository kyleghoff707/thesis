# Test Plan
Generated by /plan-eng-review on 2026-03-21
Branch: main
Repo: kyleghoff707/stock-analyzer

## Affected Pages/Routes
- `/` with any ticker → Insiders tab → Executive Compensation section — verify names, titles, totals, director data

## Key Interactions to Verify
- Load AAPL → Insiders → Executive Compensation: names should be clean (no "&nbsp;", no concatenated titles), no duplicate entries, CEO total comp ~$63M
- Load ODFL → Insiders → Executive Compensation: CEO total comp should be ~$12.6M (not $39K)
- Load JPM → Insiders → Executive Compensation: "James Dimon" as name, "Chairman and CEO" as separate title, director count > 0
- Load MLI → Insiders → Executive Compensation: CEO Pay Ratio should NOT be "2024:1"
- Load MET → Insiders → Executive Compensation: should show MetLife executives (Michel Khalaf), NOT Texas Roadhouse

## Edge Cases
- BRK-B: XBRL-only company, Buffett at ~$400K — should NOT trigger low-value fallback
- RACE: Foreign filer, no DEF 14A — should show "No proxy statement found" gracefully
- XPEL: Small company — if no exec table found, should show empty state
- NVDA: Should have ~5 executives (not 11), no title fragments as names

## Critical Paths
- Full compensation data pipeline: ticker → CIK → DEF 14A filings → HTML parse → merge → display
- XBRL fallback path: HTML parse returns 0 execs OR low-value totals → XBRL ECD parse → display
- Director tab: ticker → DEF 14A → director table parse → display with all fields
