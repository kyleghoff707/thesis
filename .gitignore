# Python
__pycache__/
*.pyc

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Knowledge folder (course curriculum, personal research — local only)
knowledge/

# Validation data (cached API responses, reports, bundled engines — local only)
validation/cache/
validation/data/
validation/reports/
validation/scripts/bundled-engines.mjs
# Wrangler local state (D1 SQLite, etc.)
.wrangler/
api/.wrangler/

# Hidden gstack runtime folder (recreated by skills)
.gstack/

# gstack project artifacts — track plans/reports, ignore screenshots/logs
gstack/qa-reports/screenshots/
gstack/design-reports/screenshots/
gstack/canary-reports/baselines/
gstack/canary-reports/screenshots/
gstack/benchmark-reports/baselines/
gstack/investigations/screenshots/
gstack/browse-logs/
gstack/deploy-reports/*.png

# EDGAR API response cache for accuracy tests (regenerated on demand)
src/engines/__tests__/fixtures/edgar-cache/

# Node adapter file cache (runtime, regenerated on demand)
.thesis/
.claude/worktrees/
.claude/scheduled_tasks.lock
.council/

api/src/pipeline/curriculumBundle.js

# Agent YAML sync backups (pre-migration snapshots, local-only)
agents/.backup-*/
agents/.staging-sync/

# Personal notes / runbooks — not part of the public repo
_notes/
