---
phase: 14-adversarial-debate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/generate-full-story/SKILL.md
autonomous: false
requirements: [DEBATE-01, DEBATE-02, DEBATE-03, DEBATE-04]

must_haves:
  truths:
    - "The debate executes 5 sequential agent calls (Bull, Bear, Bull Rebuttal, Judge, Composition) with each step receiving prior steps' output"
    - "Bear inversions include web search citations with full URLs and DataPacket references"
    - "Bull rebuttal addresses every bear point with honest strength ratings (strong/moderate/weak)"
    - "Judge scores each exchange as Strong Bull / Strong Bear / Unresolved with reasoning"
    - "Composition produces dual-view S6: verdict summary table + exchange detail with all citations preserved"
    - "PM can re-run from any debate step with cascade and optional guidance text + file attachments"
    - "After debate approval, report assembly updates full-story.json from partial (5/6) to complete (6/6)"
  artifacts:
    - path: ".claude/skills/generate-full-story/SKILL.md"
      provides: "Complete debate orchestration (Steps 8-10) replacing Phase 14 placeholder"
      contains: "Step 8a: Bull Thesis"
  key_links:
    - from: "SKILL.md Step 8a (Bull)"
      to: "SKILL.md Step 8b (Bear)"
      via: "debate-step-1.json file read by Bear dispatch"
      pattern: "debate-step-1\\.json"
    - from: "SKILL.md Step 8d (Judge)"
      to: "SKILL.md Step 8e (Composition)"
      via: "All 4 debate step files read by Composition"
      pattern: "debate-step-4\\.json"
    - from: "SKILL.md Step 9 (Checkpoint)"
      to: "SKILL.md Step 10 (Final Assembly)"
      via: "PM says continue -> assembly reads S6 and updates full-story.json"
      pattern: "fullStory-S6-inversion_rebuttal\\.json"
---

<objective>
Implement the 4-step adversarial debate plus composition and checkpoint in the generate-full-story CC skill, replacing the Phase 14 placeholder. This produces the S6 (inversion_rebuttal) section via 5 sequential agent calls with inter-step context passing, a debate checkpoint with re-run-from-any-step support, and a final report assembly update that marks the Full Story as complete.

Purpose: The debate is the conviction gate -- it stress-tests the investment thesis from both sides before the PM commits capital. Without S6, the Full Story is partial (5/6 sections) and has no overall verdict.
Output: Updated `.claude/skills/generate-full-story/SKILL.md` with Steps 8-10 fully implemented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-adversarial-debate/14-CONTEXT.md
@.planning/phases/14-adversarial-debate/14-RESEARCH.md

@.claude/skills/generate-full-story/SKILL.md
@agents/orchestrator/schemas/debate-step.schema.json
@agents/orchestrator/dispatch-table.json
@agents/orchestrator/config.json
@agents/synthesis-writer/config.json
@agents/synthesis-writer/prompt.md
@agents/risk-analyst/config.json
@agents/risk-analyst/prompt.md
@agents/financial-analyst/config.json
@agents/financial-analyst/prompt.md
@src/schemas/reportSection.js

<interfaces>
<!-- Key schemas and configs the executor needs. Extracted from codebase. -->

From agents/orchestrator/dispatch-table.json (fullStory.phases[1] -- the debate config):
```json
{
  "phase": 2,
  "description": "THE DEBATE -- 4-step adversarial analysis",
  "isDebate": true,
  "sequential": true,
  "steps": [
    { "step": 1, "role": "bull", "agent": "synthesis-writer", "webSearch": false, "receivesContext": ["sections_1_through_5"] },
    { "step": 2, "role": "bear", "agent": "risk-analyst", "webSearch": true, "receivesContext": ["bull_output"] },
    { "step": 3, "role": "bull_rebuttal", "agent": "synthesis-writer", "webSearch": false, "receivesContext": ["bull_output", "bear_output"] },
    { "step": 4, "role": "judge", "agent": "financial-analyst", "webSearch": false, "receivesContext": ["bull_output", "bear_output", "bull_rebuttal_output"] }
  ],
  "outputSection": 6,
  "outputKey": "inversion_rebuttal"
}
```

From agents/orchestrator/schemas/debate-step.schema.json (content variants per role):
- BullThesis: `content.thesisPoints[]` (point, evidence, sourceSection) + `content.overallThesis`
- BearInversion: `content.inversions[]` (targetPoint, counterArgument, evidence, severity, sources) + `content.overallBearCase`
- BullRebuttal: `content.rebuttals[]` (bearPoint, rebuttal, rebuttalStrength, honest)
- JudgeVerdict: `content.exchanges[]` (topic, bullStrength, bearStrength, verdict, reasoning) + `content.overallVerdict` (direction, unresolvedCount, summary, investmentImplication)

Agent role assignments:
- synthesis-writer (bull, bull_rebuttal, composition) -- model: opus, no tools, no DataPacket
- risk-analyst (bear) -- model: opus, web search enabled, DataPacket: companyInfo, events, analystEstimates, classification
- financial-analyst (judge) -- model: sonnet, no web search, DataPacket: financials, ttm, growthRates, returnMetrics, debtMetrics, fcf, keyMetrics

From agents/synthesis-writer/config.json:
- curriculum: ["knowledge/research-references/buffett-writing-style-guide.md"]
- universalContext: true (rule-one-fundamentals.md + tools-for-analysis.md)
- dataPacketSlice: [] (empty -- synthesis-writer works from section outputs, not raw data)

From agents/risk-analyst/config.json:
- curriculum: ["pitch-deck-III.md", "story-form-II.md", "advanced-financial-analysis.md", "fgr.md"]
- universalContext: true
- dataPacketSlice: ["companyInfo", "events", "analystEstimates", "classification"]

From agents/financial-analyst/config.json:
- curriculum: ["advanced-financial-analysis.md", "fgr.md", "capex-cash-flow-explained.md"]
- universalContext: true
- dataPacketSlice: ["financials", "ttm", "growthRates", "returnMetrics", "debtMetrics", "fcf", "keyMetrics"]

File paths for debate intermediates:
- `.thes1s/reports/{TICKER}/sections/debate-step-1.json` (Bull)
- `.thes1s/reports/{TICKER}/sections/debate-step-2.json` (Bear)
- `.thes1s/reports/{TICKER}/sections/debate-step-3.json` (Bull Rebuttal)
- `.thes1s/reports/{TICKER}/sections/debate-step-4.json` (Judge)
- `.thes1s/reports/{TICKER}/sections/fullStory-S6-inversion_rebuttal.json` (Final composed S6)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement debate orchestration (Steps 8a-8e) in SKILL.md</name>
  <files>.claude/skills/generate-full-story/SKILL.md</files>
  <read_first>
    - .claude/skills/generate-full-story/SKILL.md (current file -- understand existing Step 5 dispatch pattern, Step 7 assembly, and Step 8 placeholder location)
    - agents/orchestrator/schemas/debate-step.schema.json (output format for all 4 debate steps)
    - agents/orchestrator/dispatch-table.json (fullStory.phases[1] debate config with sequential steps, receivesContext chains)
    - agents/synthesis-writer/config.json (model, curriculum, universalContext for bull + bull_rebuttal + composition)
    - agents/risk-analyst/config.json (model, curriculum, dataPacketSlice, universalContext for bear)
    - agents/financial-analyst/config.json (model, curriculum, dataPacketSlice, universalContext for judge)
    - agents/synthesis-writer/prompt.md (lines 393-468 -- bull thesis + bull rebuttal instructions)
    - agents/risk-analyst/prompt.md (lines 650-713 -- bear inversion instructions with web search)
    - agents/financial-analyst/prompt.md (lines 706-775 -- judge verdict scoring instructions)
    - .planning/phases/14-adversarial-debate/14-RESEARCH.md (architecture patterns, code examples, anti-patterns)
  </read_first>
  <action>
Replace the Step 8 placeholder (lines 623-631 of SKILL.md) with 5 sub-steps (8a through 8e) implementing the sequential debate orchestration. Follow the EXACT same dispatch pattern used in Step 5 (lines 183-308) for prompt assembly, but adapted for debate-specific context accumulation.

**Step 8 preamble:** Add a section header explaining Phase 2 of the Full Story pipeline -- the adversarial debate. Note that it runs AFTER the Step 6 checkpoint approves S1-S5.

```
## Step 8: Phase 2 -- THE DEBATE (5 sequential agent calls)

The adversarial debate stress-tests the investment thesis by having Bull, Bear, Bull Rebuttal, and Judge agents argue over the S1-S5 findings. A 5th composition call assembles the dual-view S6 narrative. Each step receives prior step outputs as context -- strictly sequential, no parallelism.

File layout for intermediate outputs:
- `.thes1s/reports/{TICKER}/sections/debate-step-1.json` (Bull thesis)
- `.thes1s/reports/{TICKER}/sections/debate-step-2.json` (Bear inversion)
- `.thes1s/reports/{TICKER}/sections/debate-step-3.json` (Bull rebuttal)
- `.thes1s/reports/{TICKER}/sections/debate-step-4.json` (Judge verdict)
- `.thes1s/reports/{TICKER}/sections/fullStory-S6-inversion_rebuttal.json` (Final composed S6)
```

**Step 8a: Bull Thesis (synthesis-writer, role: "bull")** -- per D-04, DEBATE-01

Read the synthesis-writer agent configuration:
1. `agents/synthesis-writer/config.json` -- model (opus), curriculum, universalContext settings
2. `agents/synthesis-writer/prompt.md` -- full agent prompt (includes "Debate Step 1: Bull Thesis" instructions at lines ~397-428)
3. Each curriculum file: `knowledge/research-references/buffett-writing-style-guide.md`
4. Universal context files: `knowledge/research-references/rule-one-fundamentals.md` and `knowledge/research-references/tools-for-analysis.md`

Read all 5 completed section outputs for context:
- `.thes1s/reports/{TICKER}/sections/fullStory-S1-event_analysis.json`
- `.thes1s/reports/{TICKER}/sections/fullStory-S2-meaning_checklist.json`
- `.thes1s/reports/{TICKER}/sections/fullStory-S3-moat_checklist.json`
- `.thes1s/reports/{TICKER}/sections/fullStory-S4-management_checklist.json`
- `.thes1s/reports/{TICKER}/sections/fullStory-S5-valuation_confirmation.json`

Build the bull dispatch prompt (concatenate in this order):
1. Agent prompt.md content
2. Curriculum files
3. Universal context files
4. All 5 section outputs as structured context:
   ```
   ## Prior Full Story Sections (S1-S5) -- YOUR SOURCE MATERIAL

   For each section, present: key, verdict, confidence, summary, redFlags, and narrative (first 2000 chars).
   These are the findings you must synthesize into the strongest possible bull case.
   ```
5. Debate step schema definition (BullThesis variant from debate-step.schema.json)
6. Task instruction: "You are the BULL in the adversarial debate for {TICKER}. Synthesize the strongest possible investment case from the S1-S5 findings above. Each thesis point MUST cite the specific section it comes from. Include at least 5 thesis points covering meaning, moat, management, valuation, and events. Output a JSON object with step=1, role='bull', agent='synthesis-writer', and content matching BullThesis format."

Note: synthesis-writer has NO DataPacket slice (empty array in config) -- it works from section outputs only. No web search.

Dispatch via Agent tool.

After completion:
1. Extract JSON from response (look for ```json block or raw JSON object)
2. Validate required fields: step=1, role="bull", agent="synthesis-writer", content.thesisPoints (array with >= 5 items), content.overallThesis (string)
3. Verify each thesis point has: point, evidence, sourceSection
4. Save to `.thes1s/reports/{TICKER}/sections/debate-step-1.json`

Log:
```
Step 8a: Bull Thesis complete
  Thesis points: {count} (minimum 5 required)
  Source sections cited: {unique sourceSection values}
```

**Step 8b: Bear Inversion (risk-analyst, role: "bear") [WEB SEARCH ENABLED]** -- per D-04, D-11, D-12, D-13, DEBATE-02

Read debate-step-1.json:
- Read `.thes1s/reports/{TICKER}/sections/debate-step-1.json`
- Extract the thesisPoints array -- these are the specific targets the Bear will attack

Read the risk-analyst agent configuration:
1. `agents/risk-analyst/config.json` -- model (opus), curriculum, dataPacketSlice, universalContext settings
2. `agents/risk-analyst/prompt.md` -- full agent prompt (includes "Debate Step 2: Bear Inversion" instructions at lines ~654-713)
3. Each curriculum file: `knowledge/stage-2-pitch-deck/pitch-deck-III.md`, `knowledge/stage-3-full-story/story-form-II.md`, `knowledge/research-references/advanced-financial-analysis.md`, `knowledge/research-references/fgr.md`
4. Universal context files: `knowledge/research-references/rule-one-fundamentals.md` and `knowledge/research-references/tools-for-analysis.md`

Slice the DataPacket for risk-analyst: companyInfo, events, analystEstimates, classification (plus always-include: ticker, caveats).

Build the bear dispatch prompt:
1. Agent prompt.md content
2. Sliced DataPacket as fenced JSON code block labeled "DataPacket"
3. Curriculum files
4. Universal context files
5. Bull thesis context:
   ```
   ## Bull Thesis (Step 1 Output -- YOUR TARGET)

   The synthesis-writer produced the following bull case for {TICKER}.
   Your job is to demolish every point with cited evidence.
   Add 1-2 NEW attack vectors the bull conveniently omitted (per D-04).

   {Full JSON content of debate-step-1.json}
   ```
6. Debate step schema definition (BearInversion variant from debate-step.schema.json)
7. Task instruction: "You are the BEAR in the adversarial debate for {TICKER}. You are an activist short seller playing to WIN (per D-11). Attack every bull thesis point with web-searched, cited evidence. Add 1-2 new attack vectors the bull conveniently omitted. You MUST perform at least 1 web search per bull thesis point PLUS 1-2 broad searches ('{TICKER} short seller thesis', '{TICKER} SEC investigation'). Minimum 7 web searches total (per D-12). Output a JSON object with step=2, role='bear', agent='risk-analyst', and content matching BearInversion format. Every inversion MUST have a non-empty sources array with full URLs."

Note: The risk-analyst inherits WebSearch and WebFetch tools in CC subagent context.

Dispatch via Agent tool.

After completion:
1. Extract JSON from response
2. Validate required fields: step=2, role="bear", agent="risk-analyst", content.inversions (array, non-empty), content.overallBearCase (string)
3. Verify each inversion has: targetPoint, counterArgument, evidence, severity (one of thesis_killer/significant/minor), sources (array with >= 1 URL)
4. Count total unique URLs across all inversions -- if < 5, log a WARNING: "Bear cited fewer than 5 unique URLs -- debate quality may be compromised"
5. Save to `.thes1s/reports/{TICKER}/sections/debate-step-2.json`

Log:
```
Step 8b: Bear Inversion complete
  Inversions: {count} (attacking {bull_point_count} bull points + {new_count} new vectors)
  Thesis killers: {count matching severity=thesis_killer}
  Sources cited: {total unique URLs across all inversions}
```

**Step 8c: Bull Rebuttal (synthesis-writer, role: "bull_rebuttal")** -- per D-04, DEBATE-03

Read debate-step-1.json and debate-step-2.json from disk.

Read the synthesis-writer agent configuration (same as Step 8a).

Read all 5 S1-S5 section outputs for evidence (same files as Step 8a -- the bull_rebuttal needs this to find counter-evidence).

Build the bull rebuttal dispatch prompt:
1. Agent prompt.md content
2. Curriculum files
3. Universal context files
4. S1-S5 section summaries (for each: key, verdict, summary, red flags -- NOT full narratives to manage context size per Pitfall 1)
5. Prior debate context:
   ```
   ## Prior Debate Steps

   ### Step 1: Bull Thesis
   {Full JSON content of debate-step-1.json}

   ### Step 2: Bear Inversion
   {Full JSON content of debate-step-2.json}
   ```
6. Debate step schema definition (BullRebuttal variant from debate-step.schema.json)
7. Task instruction: "You are the BULL REBUTTAL in the adversarial debate for {TICKER}. Address EVERY bear inversion point -- do not skip any. Rate each rebuttal honestly: strong (clear evidence negates the bear), moderate (partially addresses it), weak (bear case is stronger). When the bear case is genuinely strong, set honest=true and acknowledge it (per D-09). Do not fabricate evidence -- use only S1-S5 findings. Output a JSON object with step=3, role='bull_rebuttal', agent='synthesis-writer', and content matching BullRebuttal format."

No web search. No DataPacket.

Dispatch via Agent tool.

After completion:
1. Extract JSON from response
2. Validate required fields: step=3, role="bull_rebuttal", agent="synthesis-writer", content.rebuttals (array, length >= number of bear inversions)
3. Verify each rebuttal has: bearPoint, rebuttal, rebuttalStrength (one of strong/moderate/weak), honest (boolean)
4. Save to `.thes1s/reports/{TICKER}/sections/debate-step-3.json`

Log:
```
Step 8c: Bull Rebuttal complete
  Rebuttals: {count} (vs {bear_inversion_count} bear points)
  Strength distribution: {strong_count} strong, {moderate_count} moderate, {weak_count} weak
  Honest acknowledgments: {count where honest=true}
```

**Step 8d: Judge Verdict (financial-analyst, role: "judge")** -- per DEBATE-04

Read debate-step-1.json, debate-step-2.json, and debate-step-3.json from disk.

Read the financial-analyst agent configuration:
1. `agents/financial-analyst/config.json` -- model (sonnet), curriculum, dataPacketSlice, universalContext settings
2. `agents/financial-analyst/prompt.md` -- full agent prompt (includes "Debate Step 4: Judge Verdict" instructions at lines ~710-775)
3. Each curriculum file: `knowledge/research-references/advanced-financial-analysis.md`, `knowledge/research-references/fgr.md`, `knowledge/research-references/capex-cash-flow-explained.md`
4. Universal context files: `knowledge/research-references/rule-one-fundamentals.md` and `knowledge/research-references/tools-for-analysis.md`

Slice the DataPacket for financial-analyst: financials, ttm, growthRates, returnMetrics, debtMetrics, fcf, keyMetrics (plus always-include: ticker, companyInfo, classification, caveats).

Build the judge dispatch prompt:
1. Agent prompt.md content
2. Sliced DataPacket as fenced JSON code block labeled "DataPacket"
3. Curriculum files
4. Universal context files
5. S1-S5 section summaries (key, verdict, summary -- brief, for reference only)
6. All 3 prior debate step outputs:
   ```
   ## The Debate (Steps 1-3 -- YOUR EVIDENCE TO EVALUATE)

   ### Step 1: Bull Thesis
   {Full JSON content of debate-step-1.json}

   ### Step 2: Bear Inversion
   {Full JSON content of debate-step-2.json}

   ### Step 3: Bull Rebuttal
   {Full JSON content of debate-step-3.json}
   ```
7. Debate step schema definition (JudgeVerdict variant from debate-step.schema.json)
8. Task instruction: "You are the JUDGE in the adversarial debate for {TICKER}. Score EVERY exchange between bull and bear. For each exchange, assess bullStrength and bearStrength (strong/moderate/weak) and produce a verdict (Strong Bull / Strong Bear / Unresolved) with reasoning citing specific evidence each side presented. Produce an overallVerdict with direction (Bull/Bear/Mixed), unresolvedCount, summary, and investmentImplication. Be genuinely neutral -- if the bear has stronger evidence, say so. Output a JSON object with step=4, role='judge', agent='financial-analyst', and content matching JudgeVerdict format."

No web search.

Dispatch via Agent tool.

After completion:
1. Extract JSON from response
2. Validate required fields: step=4, role="judge", agent="financial-analyst", content.exchanges (array, non-empty), content.overallVerdict (object with direction, unresolvedCount, summary, investmentImplication)
3. Verify each exchange has: topic, bullStrength, bearStrength, verdict (one of "Strong Bull"/"Strong Bear"/"Unresolved"), reasoning
4. Save to `.thes1s/reports/{TICKER}/sections/debate-step-4.json`

Log:
```
Step 8d: Judge Verdict complete
  Exchanges scored: {count}
  Verdicts: {Strong Bull count} Strong Bull, {Strong Bear count} Strong Bear, {Unresolved count} Unresolved
  Overall direction: {direction}
  Unresolved risks: {unresolvedCount}
```

**Step 8e: Composition (synthesis-writer, role: "composition")** -- per D-01, D-02, D-03, D-08, D-09, D-13

Read all 4 debate step files from disk. Read S1-S5 section summaries for reference.

Read the synthesis-writer agent configuration (same as Steps 8a/8c).

Build the composition dispatch prompt:
1. Agent prompt.md content
2. Curriculum files
3. Universal context files
4. S1-S5 brief summaries (key + verdict + summary only -- context management)
5. All 4 debate step outputs in full:
   ```
   ## Complete Debate Record (Steps 1-4)

   ### Step 1: Bull Thesis
   {Full JSON content of debate-step-1.json}

   ### Step 2: Bear Inversion
   {Full JSON content of debate-step-2.json}

   ### Step 3: Bull Rebuttal
   {Full JSON content of debate-step-3.json}

   ### Step 4: Judge Verdict
   {Full JSON content of debate-step-4.json}
   ```
6. ReportSectionSchema definition from `src/schemas/reportSection.js`
7. Composition task instruction (detailed per D-01, D-02, D-03, D-13):
   ```
   ## Composition Task

   You are composing the final Inversion & Rebuttal section (S6) of the Full Story for {TICKER}.

   Produce a ReportSectionSchema JSON object with:
   - key: "inversion_rebuttal"
   - sectionNumber: 6
   - title: "Inversion & Rebuttal"
   - verdict: derive from Judge's overallVerdict.direction (Bull -> "PASS", Bear -> "FAIL", Mixed -> "WATCHLIST")
   - verdictRationale: condensed from judge.overallVerdict.summary
   - confidence: derive from unresolvedCount (0-1 -> "HIGH", 2-3 -> "MEDIUM", 4+ -> "LOW")
   - summary: 1-2 sentence summary of debate outcome
   - redFlags: extract from bear inversions with severity=thesis_killer or significant
   - citations: merge ALL bear source URLs (FULL clickable URLs) + DataPacket references from all steps

   The narrative MUST use a DUAL-VIEW format:

   ### View 1: Verdict Summary Table (TOP of narrative)
   Create a markdown table: | # | Topic | Verdict | Bull Strength | Bear Strength |
   Populate from Judge's exchanges array. Below the table add:
   - **Overall Direction:** {from judge.overallVerdict.direction}
   - **Unresolved Risks:** {from judge.overallVerdict.unresolvedCount}
   - **Investment Implication:** {from judge.overallVerdict.investmentImplication}

   ### View 2: Exchange Detail (BELOW the table)
   For each exchange (match by position -- exchange #1 = bull point #1 = bear inversion #1, etc.):
   1. **Bull:** thesis point with evidence and source section
   2. **Bear:** counter-argument with ALL URLs from the bear's sources array as CLICKABLE LINKS -- never drop a URL (per D-13)
   3. **Bull Rebuttal:** rebuttal text with strength rating. If honest=true, note: "The bull acknowledges this bear point as stronger."
   4. **Judge Verdict:** verdict and reasoning

   Bear's extra attack vectors (1-2 new vectors per D-04) appear at the end with no corresponding original bull point -- just Bear + Rebuttal + Judge for those.

   Output ONLY the JSON object, no surrounding text.
   ```

Dispatch via Agent tool.

After completion:
1. Extract JSON from response
2. Validate ReportSectionSchema fields: key="inversion_rebuttal", sectionNumber=6, title, verdict (PASS/FAIL/WATCHLIST), confidence, summary, narrative (>= 500 chars), citations (array), redFlags (array)
3. Count URLs in narrative -- compare against total URLs in debate-step-2.json sources arrays. If narrative URL count < 50% of bear source URLs, log WARNING: "Composition may have dropped bear citations -- verify S6 narrative"
4. Save to `.thes1s/reports/{TICKER}/sections/fullStory-S6-inversion_rebuttal.json`
5. If validation fails, retry once with error details. If retry also fails, save with status="failed" and continue.

Log:
```
Step 8e: S6 Composition complete
  Verdict: {verdict} | Confidence: {confidence}
  Narrative: {length} chars (dual-view format)
  Citations: {count} sources
  Red Flags: {count} items

Step 8: DEBATE COMPLETE
  Total debate calls: 5
  Bull points: {count} | Bear inversions: {count} | Rebuttals: {count}
  Judge: {direction} ({unresolvedCount} unresolved)
  S6 verdict: {verdict}
```

**Error handling for all debate steps:**
If any step fails (JSON parsing or validation) after 1 retry:
- Save with step number, role, and error details to the expected file path
- Print WARNING but continue to next step
- If step 1 (bull) fails, the debate cannot proceed -- print error and leave S6 as placeholder
- If step 2-4 fail, downstream steps that depend on the failed step also cannot proceed
- If step 5 (composition) fails, the 4 debate step files are still saved -- PM can re-run composition at checkpoint

Also update the "Section Keys Reference" block at the bottom of the SKILL.md to change line 676 from `6. inversion_rebuttal (Phase 14 debate -- synthesis-writer + risk-analyst + financial-analyst)` to `6. inversion_rebuttal (debate: synthesis-writer bull/rebuttal/composition + risk-analyst bear + financial-analyst judge)`.

Also update the "Section completed" line in the Constraints > Progress Display section to include Step 8 and Steps 9-10 (these will be added in Task 2).
  </action>
  <verify>
    <automated>grep -c "Step 8a: Bull Thesis" .claude/skills/generate-full-story/SKILL.md && grep -c "Step 8b: Bear Inversion" .claude/skills/generate-full-story/SKILL.md && grep -c "Step 8c: Bull Rebuttal" .claude/skills/generate-full-story/SKILL.md && grep -c "Step 8d: Judge Verdict" .claude/skills/generate-full-story/SKILL.md && grep -c "Step 8e:" .claude/skills/generate-full-story/SKILL.md && grep -c "debate-step-1.json" .claude/skills/generate-full-story/SKILL.md && grep -c "debate-step-2.json" .claude/skills/generate-full-story/SKILL.md && grep -c "activist short seller" .claude/skills/generate-full-story/SKILL.md && grep -c "DUAL-VIEW" .claude/skills/generate-full-story/SKILL.md && grep -c "ReportSectionSchema" .claude/skills/generate-full-story/SKILL.md && npm test</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains "## Step 8a: Bull Thesis (synthesis-writer)" with dispatch instructions referencing agents/synthesis-writer/config.json and prompt.md
    - SKILL.md contains "## Step 8b: Bear Inversion (risk-analyst) [WEB SEARCH ENABLED]" with explicit minimum "7 web searches" instruction
    - SKILL.md contains "## Step 8c: Bull Rebuttal (synthesis-writer)" with instruction "Address EVERY bear inversion point"
    - SKILL.md contains "## Step 8d: Judge Verdict (financial-analyst)" with scoring rules (Strong Bull / Strong Bear / Unresolved)
    - SKILL.md contains "## Step 8e:" for composition step with dual-view format instructions
    - Each debate step saves to `.thes1s/reports/{TICKER}/sections/debate-step-{N}.json` (paths appear in SKILL.md)
    - Step 8e composition saves to `.thes1s/reports/{TICKER}/sections/fullStory-S6-inversion_rebuttal.json`
    - Bear dispatch prompt includes "activist short seller" mindset instruction (per D-11)
    - Composition prompt includes "DUAL-VIEW format" with verdict summary table specification (per D-01, D-02)
    - Composition prompt includes instruction to preserve "ALL URLs from the bear's sources array as CLICKABLE LINKS" (per D-13)
    - Composition outputs ReportSectionSchema (not debate-step schema)
    - Steps 8a-8d output debate-step.schema.json format (NOT ReportSectionSchema)
    - Context accumulation follows the pattern: Step 1 gets S1-S5, Step 2 gets Step 1, Step 3 gets Steps 1+2, Step 4 gets Steps 1+2+3, Step 5 gets Steps 1+2+3+4
    - The old Step 8 placeholder text ("This section is not yet implemented") is completely removed
    - npm test passes (no regression in existing 173 tests)
  </acceptance_criteria>
  <done>The 5-step debate orchestration is fully specified in SKILL.md Steps 8a-8e. Each step has explicit dispatch instructions (which agent config to read, how to build the prompt, what context to include, how to validate the output, where to save). The debate placeholder is replaced with working instructions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement debate checkpoint (Step 9), re-run cascade (Step 9 dialogue), and final assembly update (Step 10)</name>
  <files>.claude/skills/generate-full-story/SKILL.md</files>
  <read_first>
    - .claude/skills/generate-full-story/SKILL.md (re-read after Task 1 modifications -- understand the new Step 8 structure)
    - .claude/skills/generate-pitch-deck/SKILL.md (checkpoint dialogue loop pattern to replicate -- look for the checkpoint section)
    - .claude/skills/generate-section/SKILL.md (section re-run pattern -- argument parsing, dispatch)
    - .planning/phases/14-adversarial-debate/14-CONTEXT.md (D-05, D-06, D-07 -- checkpoint UX, re-run cascade, guidance + file attachments)
    - .planning/phases/14-adversarial-debate/14-RESEARCH.md (Pattern 4: Debate Checkpoint, Re-Run Cascade Logic, Pattern 5: Report Assembly Update)
    - agents/orchestrator/schemas/debate-step.schema.json (for understanding Judge overallVerdict structure used in checkpoint display)
  </read_first>
  <action>
Add Step 9 (debate checkpoint) and Step 10 (final assembly update) after the Step 8 debate content from Task 1. Also update the existing Step 7 progress display references and the Constraints section to reflect the full 10-step pipeline.

**Step 9: Debate Checkpoint** -- per D-05, D-06, D-07

Add immediately after Step 8e. Read the Judge verdict from `debate-step-4.json` and the composed S6 from `fullStory-S6-inversion_rebuttal.json`.

Print the checkpoint display:
```
================================================================
  DEBATE CHECKPOINT: Inversion & Rebuttal (S6)
================================================================

Overall Verdict: {overallVerdict.direction} ({overallVerdict.unresolvedCount} unresolved risks)
Investment Implication: {overallVerdict.investmentImplication}

--- Exchange Summary ---

  #1 {exchanges[0].topic}: {exchanges[0].verdict}
  #2 {exchanges[1].topic}: {exchanges[1].verdict}
  ... (one line per exchange)

--- S6 Section ---
  Verdict: {S6 verdict} | Confidence: {S6 confidence}
  Narrative: {S6 narrative length} chars

================================================================

Review the debate results. You can:
  - Type an exchange number (e.g., "3") to see the full exchange detail
  - Say "re-run from bull" to restart the entire debate (5 agent calls)
  - Say "re-run from bear" to re-run bear + rebuttal + judge + composition (4 calls)
  - Say "re-run from rebuttal" to re-run rebuttal + judge + composition (3 calls)
  - Say "re-run judge" to re-run judge + composition (2 calls)
  - Say "re-run composition" to re-run just the composition (1 call)
  - Add guidance: "re-run from bear: focus on tariff risk and supply chain"
  - Add a file: "re-run from bear with file: ~/Desktop/short-report.pdf"
  - Say "continue" to accept and assemble the final report
  - Say "stop" to pause (all debate step files are already saved)

Your input:
```

**PM dialogue loop handling:**

Enter a dialogue loop. Handle PM responses:

- **Number (e.g., "1", "3", "exchange 2"):** Read the corresponding exchange from debate-step-4.json (Judge's exchanges array). Also read the matching entries from debate-step-1.json (bull thesis point), debate-step-2.json (bear inversion), and debate-step-3.json (bull rebuttal) -- matching by position index. Display the full exchange detail:
  ```
  --- Exchange {N}: {topic} ---

  BULL: {thesisPoints[N-1].point}
  Evidence: {thesisPoints[N-1].evidence}
  Source: {thesisPoints[N-1].sourceSection}

  BEAR: {inversions[N-1].counterArgument}
  Evidence: {inversions[N-1].evidence}
  Severity: {inversions[N-1].severity}
  Sources: {inversions[N-1].sources joined with newlines}

  BULL REBUTTAL: {rebuttals[N-1].rebuttal}
  Strength: {rebuttals[N-1].rebuttalStrength}
  {If rebuttals[N-1].honest == true: "** Bull acknowledges bear is stronger on this point **"}

  JUDGE: {exchanges[N-1].verdict}
  Bull strength: {exchanges[N-1].bullStrength} | Bear strength: {exchanges[N-1].bearStrength}
  Reasoning: {exchanges[N-1].reasoning}
  ```
  Note: Bear's extra attack vectors (those beyond the original bull thesis points) will have index > bull thesis points count. For these, show "BULL: (none -- bear-initiated attack vector)" instead of the bull thesis point.
  After displaying, return to the dialogue prompt.

- **"re-run from {step}" with optional guidance and file:** Parse the PM's request:
  1. Extract target step name: "bull" -> step 1, "bear" -> step 2, "rebuttal" -> step 3, "judge" -> step 4, "composition" -> step 5
  2. Extract optional guidance text: anything after ":" that is not "with file:"
     Example: "re-run from bear: focus on regulatory risk" -> guidance = "focus on regulatory risk"
  3. Extract optional file path: text after "with file:"
     Example: "re-run from bear with file: ~/Desktop/short-report.pdf" -> filePath = "~/Desktop/short-report.pdf"

  If a file path was provided:
  - Read the file content using the Read tool
  - Store as pmSourceMaterial for injection into the targeted step's prompt

  Determine the execution range: from target step through step 5 (composition). Per D-06:
  | Re-run from | Steps executed | Agent calls |
  |-------------|----------------|-------------|
  | bull (1)    | 1, 2, 3, 4, 5 | 5 calls     |
  | bear (2)    | 2, 3, 4, 5    | 4 calls     |
  | rebuttal (3)| 3, 4, 5       | 3 calls     |
  | judge (4)   | 4, 5          | 2 calls     |
  | composition (5) | 5          | 1 call      |

  For each step in the execution range:
  - Read the agent config for this step's agent
  - Build the prompt following the same pattern as Step 8 above
  - For prior debate steps that are NOT in the re-run range, read their existing files from disk (they are preserved from the previous run)
  - For the TARGETED step only (the one the PM named): inject PM guidance and file content into the prompt:
    ```
    ## PM RE-RUN GUIDANCE

    The Portfolio Manager has requested this debate step be re-run with the following direction:

    {PM guidance text}

    ## PM-PROVIDED SOURCE MATERIAL

    {File contents read from the provided path}

    Incorporate this guidance and source material into your analysis. The PM has
    specifically chosen to provide this -- it overrides your default research scope
    for this step.
    ```
  - Dispatch the agent
  - Validate output (same validation as Step 8)
  - Save to debate-step-{N}.json (OVERWRITES the previous run's file)
  - Log progress for each re-run step

  After all steps in the re-run range complete:
  - Re-display the debate checkpoint with updated results (re-read all debate step files from disk)
  - Return to the dialogue prompt

- **"continue":** Exit the dialogue loop, advance to Step 10 (final assembly update).

- **"stop":** Print "Debate paused. All step files saved. Resume with /generate:full-story {TICKER}" and stop execution. When the skill is re-invoked, it should detect existing debate step files and skip to the checkpoint. Add a detection note at the top of Step 8 preamble: "If debate-step-4.json and fullStory-S6-inversion_rebuttal.json already exist in .thes1s/reports/{TICKER}/sections/, skip Steps 8a-8e and go directly to Step 9 (checkpoint)."

**Step 10: Final Report Assembly Update** -- per Pitfall 5, Pitfall 6

After the PM says "continue" at the debate checkpoint. This step updates the existing full-story.json and full-story.md to include S6 and mark the report as complete. It does NOT rebuild from scratch (per Pitfall 5).

1. Read `fullStory-S6-inversion_rebuttal.json` from disk.
2. Read `full-story.json` from `.thes1s/reports/{TICKER}/full-story.json`.
3. Insert S6 into the sections array (append as the 6th element).
4. Update fields:
   - `status`: "partial" -> "complete"
   - `completedSections`: 5 -> 6
   - Remove `pendingPhase` field
   - Set `overallVerdict` based on Judge's verdict logic (from financial-analyst prompt.md lines 771-774):
     - PASS: overallVerdict.direction is "Bull" AND unresolvedCount <= 2 AND no thesis_killer bear points survived with Strong Bear verdict
     - FAIL: overallVerdict.direction is "Bear" OR unresolvedCount >= 4 OR any thesis_killer survived as Strong Bear
     - WATCHLIST: overallVerdict.direction is "Bull" or "Mixed" with unresolvedCount == 3
5. Write updated `full-story.json`.

6. Read `full-story.md` from `.thes1s/reports/{TICKER}/full-story.md`.
7. Replace the S6 placeholder text:
   ```
   ## Section 6: Inversion & Rebuttal

   > S6 Inversion & Rebuttal will be added in Phase 14 (adversarial debate).
   ```
   With the actual S6 content:
   ```
   ## Section 6: Inversion & Rebuttal
   **Verdict:** {verdict} | **Confidence:** {confidence}

   {S6 narrative -- the full dual-view format with verdict table + exchange detail}

   **Red Flags:**
   {for each redFlag, apply Red Flag Formatting rules from Step 7}

   **Citations:**
   {for each citation, apply Citation Formatting rules from Step 7}
   ```
8. Update the header line from "PARTIAL (5/6 sections)" to "COMPLETE (6/6 sections)".
9. Add an overall verdict line after the header: "**Overall Verdict:** {overallVerdict}"
10. Write updated `full-story.md`.

Log:
```
Step 10: Final report assembly updated
  Status: complete (6/6 sections)
  Overall Verdict: {overallVerdict}
  Output: .thes1s/reports/{TICKER}/full-story.json
  Output: .thes1s/reports/{TICKER}/full-story.md
```

**Finally, update the Constraints > Progress Display section** at the end of SKILL.md to reflect the complete 10-step pipeline:
```
Step 1: Validating input and gate check...
Step 2: Loading orchestrator and agent configurations...
Step 3: Preparing Pitch Deck inheritance...
Step 4: Preparing DataPacket slices...
Step 5: Dispatching 5 agents sequentially...
Step 6: CHECKPOINT -- Full Story Deep Analysis Review
Step 7: Assembling partial report (5/6 sections)...
Step 8: THE DEBATE -- 5 sequential agent calls...
Step 9: DEBATE CHECKPOINT -- Review debate results
Step 10: Final assembly update (6/6 sections, overall verdict)
```

Also update the "Section Keys Reference" comment at the bottom to remove the "(Phase 14)" annotation from section 6 since it is now implemented.
  </action>
  <verify>
    <automated>grep -c "DEBATE CHECKPOINT" .claude/skills/generate-full-story/SKILL.md && grep -c "re-run from bull" .claude/skills/generate-full-story/SKILL.md && grep -c "re-run from bear" .claude/skills/generate-full-story/SKILL.md && grep -c "PM RE-RUN GUIDANCE" .claude/skills/generate-full-story/SKILL.md && grep -c "PM-PROVIDED SOURCE MATERIAL" .claude/skills/generate-full-story/SKILL.md && grep -c "Step 10:" .claude/skills/generate-full-story/SKILL.md && grep -c "status.*complete" .claude/skills/generate-full-story/SKILL.md && grep -c "completedSections.*6" .claude/skills/generate-full-story/SKILL.md && grep -c "overallVerdict" .claude/skills/generate-full-story/SKILL.md && npm test</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains "## Step 9:" with "DEBATE CHECKPOINT" header text
    - Step 9 checkpoint displays: overall verdict direction, unresolvedCount, investmentImplication, and exchange summary table (one line per exchange with topic + verdict)
    - Step 9 dialogue loop lists all 6 PM commands: exchange number, re-run from bull, re-run from bear, re-run from rebuttal, re-run judge, re-run composition, add guidance, add file, continue, stop
    - SKILL.md contains "PM RE-RUN GUIDANCE" injection template with "{PM guidance text}" placeholder
    - SKILL.md contains "PM-PROVIDED SOURCE MATERIAL" injection template with "{File contents read from the provided path}" placeholder
    - Re-run cascade table appears: bull->5 calls, bear->4, rebuttal->3, judge->2, composition->1
    - Re-run logic specifies: "For prior debate steps NOT in the re-run range, read their existing files from disk"
    - SKILL.md contains "## Step 10:" with final assembly update instructions
    - Step 10 changes status from "partial" to "complete" and completedSections from 5 to 6
    - Step 10 removes pendingPhase field from full-story.json
    - Step 10 sets overallVerdict based on Judge's verdict logic (PASS/FAIL/WATCHLIST rules specified)
    - Step 10 replaces the S6 placeholder text in full-story.md with actual S6 content
    - Step 10 updates full-story.md header from "PARTIAL (5/6)" to "COMPLETE (6/6)"
    - The "stop" command handling includes resume detection note ("If debate-step-4.json and fullStory-S6-inversion_rebuttal.json already exist... skip to Step 9")
    - Progress Display section updated to show all 10 steps
    - npm test passes (no regression in existing 173 tests)
  </acceptance_criteria>
  <done>The debate checkpoint presents Judge results with exchange drill-down, the re-run cascade enables the PM to re-run from any debate step with guidance text and file attachments, and the final assembly update marks the Full Story as complete with an overall verdict derived from the Judge's scoring logic.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: PM reviews the complete debate orchestration in SKILL.md</name>
  <files>.claude/skills/generate-full-story/SKILL.md</files>
  <action>
PM reviews the complete debate orchestration. What was built:
- Step 8 (5 sub-steps): Bull thesis, Bear inversion with web search, Bull rebuttal, Judge verdict, Composition into dual-view S6
- Step 9: Debate checkpoint with exchange drill-down, re-run from any step with cascade, PM guidance + file attachment injection
- Step 10: Final report assembly update (partial -> complete, overall verdict set)

How to verify:
1. Open `.claude/skills/generate-full-story/SKILL.md` and review:
   - Step 8a-8e: Each debate step has explicit agent config references, prompt assembly instructions, output validation, and file save paths
   - Step 8b (Bear): Contains "activist short seller" mindset, "at least 7 web searches" minimum, URL citation requirements
   - Step 8e (Composition): Contains dual-view format spec with verdict summary table + exchange detail, bear URL preservation
   - Step 9: Checkpoint shows exchange summary, offers re-run from any step with guidance + file
   - Step 10: Updates full-story.json (status=complete, 6/6) and full-story.md (replaces S6 placeholder)
2. Verify the old Step 8 placeholder ("This section is not yet implemented") is gone
3. Verify the Progress Display section shows all 10 steps
4. To test the full pipeline: `/generate:full-story {TICKER}` on a ticker with an existing Pitch Deck (creates S1-S5, then runs the debate, then assembles)

Resume signal: Type "approved" or describe issues to address.
  </action>
  <verify>PM confirms SKILL.md debate orchestration is complete and correct</verify>
  <done>PM has reviewed and approved the debate orchestration in SKILL.md</done>
</task>

</tasks>

<verification>
1. `grep "Step 8a" .claude/skills/generate-full-story/SKILL.md` returns a match (Bull Thesis step exists)
2. `grep "Step 8b" .claude/skills/generate-full-story/SKILL.md` returns a match (Bear Inversion step exists)
3. `grep "Step 8e" .claude/skills/generate-full-story/SKILL.md` returns a match (Composition step exists)
4. `grep "Step 9" .claude/skills/generate-full-story/SKILL.md` returns a match (Debate checkpoint exists)
5. `grep "Step 10" .claude/skills/generate-full-story/SKILL.md` returns a match (Final assembly exists)
6. `grep "DEBATE CHECKPOINT" .claude/skills/generate-full-story/SKILL.md` returns a match
7. `grep "re-run from" .claude/skills/generate-full-story/SKILL.md` returns matches
8. `grep "PM RE-RUN GUIDANCE" .claude/skills/generate-full-story/SKILL.md` returns a match
9. `grep "This section is not yet implemented" .claude/skills/generate-full-story/SKILL.md` returns NO match (placeholder removed)
10. `npm test` passes (no regression)
</verification>

<success_criteria>
- The generate-full-story SKILL.md contains complete debate orchestration instructions (Steps 8-10) that Claude Code can execute without ambiguity
- All 4 debate steps dispatch the correct agent (synthesis-writer for bull/rebuttal/composition, risk-analyst for bear, financial-analyst for judge)
- Only the Bear step has web search enabled
- Each step saves intermediate output to debate-step-{N}.json for re-run support
- The composition step produces a ReportSectionSchema-conformant S6 with dual-view narrative
- The debate checkpoint enables PM to drill into exchanges, re-run from any step with cascade, provide guidance text and file attachments
- The final assembly update transitions the Full Story from partial (5/6) to complete (6/6) with an overall verdict
- No existing tests regress
</success_criteria>

<output>
After completion, create `.planning/phases/14-adversarial-debate/14-01-SUMMARY.md`
</output>
