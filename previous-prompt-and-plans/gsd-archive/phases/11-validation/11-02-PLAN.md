---
phase: 11-validation
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - scripts/run-pipeline.js
  - scripts/run-quality-v4.js
autonomous: false
requirements: [VAL-02, VAL-03, VAL-04]

must_haves:
  truths:
    - "A second ticker from a different sector than Consumer Defensive generates a complete Pitch Deck via the API pipeline"
    - "The second ticker scores 85+ mechanical quality and passes methodology checks"
    - "Pipeline cost for the second ticker is $8-12"
    - "Pipeline runtime for the second ticker is under 40 minutes"
    - "The filing content fix from Phase 10 (Bug #3) is validated -- PSR agents produce qualitative analysis, not data-only stubs"
  artifacts:
    - path: ".thes1s/reports/{TICKER}/pipeline-output.json"
      provides: "Complete pipeline output for second ticker"
      contains: "sections"
    - path: ".thes1s/reports/{TICKER}/quality/pitch-deck-v4.quality.json"
      provides: "Quality report with both mechanical and methodology scores"
      contains: "overallMethodologyScore"
    - path: ".thes1s/reports/{TICKER}/quality/pitch-deck-v4.quality.md"
      provides: "Human-readable quality report with dual scores"
      contains: "Methodology"
  key_links:
    - from: "scripts/run-pipeline.js"
      to: "src/engines/pipelineManager.js"
      via: "runPipeline() call"
      pattern: "runPipeline"
    - from: "scripts/run-quality-v4.js"
      to: "src/engines/critic.js"
      via: "validateStage() call"
      pattern: "validateStage"
---

<objective>
Run the full Pitch Deck pipeline on a second ticker from a different sector to prove the pipeline generalizes beyond SFM. Validate mechanical quality (85+), methodology compliance, cost ($8-12), and runtime (under 40 min). This also validates the Phase 10 filing content fix (PSR agents reading actual 10-K text).

Purpose: VAL-02 (second ticker generalization), VAL-03 (cost target), VAL-04 (runtime target). Per D-06, the second ticker must be from a different sector than Consumer Defensive (SFM's sector). Per D-07, cost and runtime targets are already met for SFM -- verify they hold for the second ticker. Per D-08, the filing content fix is validated as part of this run.

Output: Pipeline output, quality reports (JSON + markdown) for the second ticker. PM checkpoint to review results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-validation/11-CONTEXT.md
@.planning/phases/11-validation/11-01-SUMMARY.md

@scripts/run-pipeline.js
@scripts/run-quality-v4.js
@src/engines/critic.js
@src/engines/pipelineManager.js
@src/engines/dataExport.js

<interfaces>
<!-- Key contracts for the executor -->

From scripts/run-pipeline.js:
```javascript
// Usage: node --loader ./scripts/node-esm-loader.js scripts/run-pipeline.js [TICKER] [STAGE]
// Default: SFM pitchDeck
// Output: .thes1s/reports/{TICKER}/pipeline-output.json
// Exit 0 if all sections produced, exit 1 otherwise
```

From scripts/run-quality-v4.js:
```javascript
// Usage: node --loader ./scripts/node-esm-loader.js scripts/run-quality-v4.js [TICKER]
// Output: .thes1s/reports/{TICKER}/quality/pitch-deck-v4.quality.json
//         .thes1s/reports/{TICKER}/quality/pitch-deck-v4.quality.md
// Prints overall mechanical + methodology scores to console
```

Pipeline output shape (from SFM run):
```javascript
{
  ticker: "SFM",
  stage: "pitchDeck",
  completedAt: "...",
  assemblyTimeSeconds: number,
  pipelineTimeSeconds: number,
  sectionCount: 13,  // 10 pitch deck + 2 PSR + 1 synthesis
  errorCount: 0,
  sections: [...],
  budget: { entries: [...], totals: { cost: 8.53, ... } },
  cacheStats: {...},
  errors: [],
}
```

SFM benchmark values:
- Cost: $8.53
- Runtime: ~19 minutes (1140 seconds)
- Sections: 13
- Errors: 0
- Mechanical quality: 94
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run pipeline on second ticker and generate quality reports</name>
  <files>scripts/run-pipeline.js, scripts/run-quality-v4.js</files>
  <read_first>
    - scripts/run-pipeline.js (understand CLI usage, output paths, expected section count)
    - scripts/run-quality-v4.js (understand CLI usage, how to trigger quality check)
    - .planning/phases/11-validation/11-CONTEXT.md (D-06 ticker selection criteria, D-08 filing content fix validation)
    - .planning/phases/11-validation/11-01-SUMMARY.md (methodology scoring is now available)
  </read_first>
  <action>
    **Ticker selection per D-06:** Pick a ticker from a sector different from Consumer Defensive (SFM's sector). Good candidates with strong EDGAR coverage:
    - **MSFT** (Technology) -- well-covered, large filing history
    - **UNH** (Healthcare) -- well-covered, different sector
    - **HD** (Consumer Cyclical) -- similar to SFM domain but different sector classification
    - **JPM** (Financial Services) -- tests bank overlay, different sector
    - **ODFL** (Industrials) -- user has pre-course research on this ticker

    Executor should pick one. Preference: a company the user has NOT already analyzed (avoid SFM, COST, LULU). **ODFL is a strong choice** -- the user has real manual research on it (pre-course example), different sector (Industrials), good EDGAR coverage, and it would validate against the user's own analysis quality expectations.

    **Step 1: Run the pipeline.**
    ```bash
    cd /Users/kylehoff/Desktop/stock-analyzer
    node --loader ./scripts/node-esm-loader.js scripts/run-pipeline.js {TICKER} pitchDeck
    ```
    This will take 15-25 minutes. Monitor console output for:
    - DataPacket assembly success (populated fields count)
    - Filing pre-processing (10-Ks and 10-Qs fetched -- validates Bug #3 fix from Phase 10)
    - Wave-by-wave completion with running cost
    - Total sections produced (expect 13: 10 + 2 PSR + 1 synthesis)
    - Total cost (expect $8-12)
    - Zero errors

    **Step 2: Run quality checks.**
    ```bash
    node --loader ./scripts/node-esm-loader.js scripts/run-quality-v4.js {TICKER}
    ```
    This produces:
    - `.thes1s/reports/{TICKER}/quality/pitch-deck-v4.quality.json`
    - `.thes1s/reports/{TICKER}/quality/pitch-deck-v4.quality.md`
    - Console output with mechanical + methodology scores per section

    **Step 3: Validate results against requirements.**
    From the pipeline output JSON, extract and verify:
    - `budget.totals.cost` is between 8.00 and 12.00 (VAL-03)
    - `pipelineTimeSeconds` is under 2400 (40 minutes) (VAL-04)
    - `sectionCount` is 13 and `errorCount` is 0
    - At least one PSR section (annual-reader or quarterly-reader) has narrative length > 500 characters AND does NOT contain "API Dispatch Mode limitation" (validates Bug #3 fix per D-08)

    From the quality JSON, extract and verify:
    - `overallScore` >= 85 (VAL-02 mechanical)
    - `overallMethodologyScore` is computed (methodology check runs)
    - Zero high-severity issues across all sections (VAL-02 zero high-severity)

    **Step 4: Prepare a validation summary table.**
    Print a table to console summarizing all 4 VAL requirements:
    ```
    === VALIDATION RESULTS ===
    VAL-01: SFM mechanical 85+          PASS (94)
    VAL-02: {TICKER} mechanical 85+     PASS/FAIL (score)
    VAL-02: {TICKER} methodology        SCORE (X/100)
    VAL-02: {TICKER} zero high issues   PASS/FAIL (count)
    VAL-03: Cost $8-12                  PASS/FAIL ($X.XX)
    VAL-04: Runtime < 40 min            PASS/FAIL (X min)
    Bug #3: PSR filing content          PASS/FAIL
    ```
  </action>
  <verify>
    <automated>cd /Users/kylehoff/Desktop/stock-analyzer && node -e "
const fs = require('fs');
const ticker = fs.readdirSync('.thes1s/reports/').filter(d => d !== 'SFM' && fs.existsSync('.thes1s/reports/' + d + '/pipeline-output.json'))[0];
if (!ticker) { console.log('FAIL: No second ticker pipeline output found'); process.exit(1); }
const p = JSON.parse(fs.readFileSync('.thes1s/reports/' + ticker + '/pipeline-output.json', 'utf8'));
const q = JSON.parse(fs.readFileSync('.thes1s/reports/' + ticker + '/quality/pitch-deck-v4.quality.json', 'utf8'));
const cost = p.budget?.totals?.cost;
const runtime = p.pipelineTimeSeconds;
const mechScore = q.overallScore;
const methScore = q.overallMethodologyScore;
const highIssues = q.sections.reduce((sum, s) => sum + (s.issues || []).filter(i => i.severity === 'high').length, 0);
console.log('Ticker:', ticker);
console.log('Sections:', p.sectionCount, '(expect 13)');
console.log('Errors:', p.errorCount, '(expect 0)');
console.log('Cost: $' + cost?.toFixed(2), '(target: 8-12)');
console.log('Runtime:', (runtime/60)?.toFixed(1) + ' min', '(target: < 40)');
console.log('Mechanical score:', mechScore, '(target: 85+)');
console.log('Methodology score:', methScore);
console.log('High issues:', highIssues, '(target: 0)');
const pass = p.sectionCount >= 11 && cost >= 8 && cost <= 12 && mechScore >= 85 && highIssues === 0;
console.log('Overall:', pass ? 'PASS' : 'FAIL');
process.exit(pass ? 0 : 1);
"</automated>
  </verify>
  <acceptance_criteria>
    - .thes1s/reports/{TICKER}/pipeline-output.json exists for a ticker other than SFM
    - pipeline-output.json contains sectionCount of 13 and errorCount of 0
    - pipeline-output.json budget.totals.cost is between 8.00 and 12.00
    - pipeline-output.json pipelineTimeSeconds is under 2400
    - .thes1s/reports/{TICKER}/quality/pitch-deck-v4.quality.json exists
    - Quality JSON overallScore is 85 or higher
    - Quality JSON overallMethodologyScore is present and computed
    - Quality JSON sections have zero high-severity issues total
    - At least one PSR section narrative is longer than 500 characters
  </acceptance_criteria>
  <done>Second ticker pipeline run completes with 13 sections, $8-12 cost, under 40 min runtime. Quality check produces 85+ mechanical score with zero high-severity issues. Methodology score computed. PSR agents produce qualitative analysis (filing content fix validated).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: PM reviews validation results for both tickers</name>
  <files></files>
  <action>Present validation results to PM for review. No code changes -- this is a review checkpoint.</action>
  <what-built>Complete validation of the Pitch Deck API pipeline on two tickers (SFM + second ticker). Both mechanical and methodology quality scores produced. Cost and runtime targets verified.</what-built>
  <how-to-verify>
    1. Review the validation results table printed by Task 1.
    2. Read the quality markdown report at `.thes1s/reports/{TICKER}/quality/pitch-deck-v4.quality.md` -- check that:
       - Section scores look reasonable (no section dramatically lower than expected)
       - Methodology gaps identified make sense (if any)
       - High-severity issues are truly zero
    3. Optionally open `.thes1s/reports/{TICKER}/pipeline-output.json` and spot-check a section's narrative for:
       - Substantive analysis (not stubs or generic text)
       - Citations referencing real data
       - Rule One methodology applied (valuation methods, growth analysis, etc.)
    4. Verify cost and runtime are in target range.
    5. If the PM wants to review the generated pitch deck report, check `.thes1s/reports/{TICKER}/pitch-deck-report.md` (if generated by the pipeline runner).

    **Expected results:**
    - VAL-01: SFM mechanical 94 (already achieved)
    - VAL-02: Second ticker mechanical 85+, methodology scored
    - VAL-03: Cost $8-12
    - VAL-04: Runtime under 40 min

    **Remind the PM:** Archive the pipeline output to Desktop to avoid version confusion (per feedback_archive_pipeline_runs.md).
  </how-to-verify>
  <verify>PM reviews and responds with approval or issues.</verify>
  <done>PM approves validation results. Phase 11 complete.</done>
  <resume-signal>Type "approved" to complete Phase 11, or describe any issues to address.</resume-signal>
</task>

</tasks>

<verification>
1. Pipeline output exists for second ticker with 13 sections, 0 errors
2. Quality report exists with mechanical score 85+ and methodology score computed
3. Budget totals cost is $8-12
4. Pipeline runtime is under 40 minutes
5. PSR sections contain qualitative analysis (not API Dispatch Mode stubs)
6. PM approves the validation results
</verification>

<success_criteria>
- VAL-01: SFM mechanical 94 (no rerun needed per D-05) -- PASS
- VAL-02: Second ticker from different sector scores 85+ mechanical with zero high-severity issues, methodology score computed
- VAL-03: Second ticker pipeline cost is $8-12
- VAL-04: Second ticker pipeline runtime is under 40 minutes
- Phase 10 Bug #3 fix validated: PSR agents produce qualitative filing analysis
- PM reviews and approves validation results
</success_criteria>

<output>
After completion, create `.planning/phases/11-validation/11-02-SUMMARY.md`
</output>
