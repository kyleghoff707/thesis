---
phase: 10-pipeline-integration-prompt-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/orchestrator/dispatch-table.json
  - agents/valuation-specialist/prompt.md
  - agents/business-analyst/prompt.md
  - agents/competitor-evaluator/prompt.md
  - agents/financial-analyst/prompt.md
  - agents/annual-reader/prompt.md
  - agents/quarterly-reader/prompt.md
autonomous: true
requirements: [FIX-01, FIX-03, FIX-04, FIX-05]

must_haves:
  truths:
    - "No agent prompt contains the phrase 'CC skill' or 'Claude Code'"
    - "No agent prompt tells the model to 'return an array of TWO JSON objects' since structured output forces single-object return"
    - "Agent prompts referencing unavailable custom tools (comparePeers, getMetric, getFinancialLine, computeMOS, readFilingSection, getTranscriptExcerpt) redirect agents to use DataPacket data directly or web search"
    - "The valuation-specialist prompt line 468 references API dispatch FGR presentation, not a CC skill sub-workflow"
    - "dispatch-table.json pitchDeck has exactly 10 section dispatch entries (one section per dispatch) plus synthesis, matching the 10-section Pitch Deck structure"
  artifacts:
    - path: "agents/orchestrator/dispatch-table.json"
      provides: "Split multi-section dispatches into one-section-per-dispatch entries"
      contains: "sections.*\\[1\\]"
    - path: "agents/valuation-specialist/prompt.md"
      provides: "Fixed CC skill reference at line 468"
      contains: "Present each of the 5 FGR inputs"
    - path: "agents/business-analyst/prompt.md"
      provides: "Fixed multi-section output instruction"
    - path: "agents/competitor-evaluator/prompt.md"
      provides: "Fixed comparePeers tool references and multi-section output"
    - path: "agents/financial-analyst/prompt.md"
      provides: "Fixed custom tool references and multi-section output"
    - path: "agents/annual-reader/prompt.md"
      provides: "Clarified readFilingSection unavailability with DataPacket workaround"
    - path: "agents/quarterly-reader/prompt.md"
      provides: "Clarified readFilingSection and getTranscriptExcerpt unavailability with DataPacket workaround"
  key_links:
    - from: "agents/orchestrator/dispatch-table.json"
      to: "src/engines/pipelineManager.js:runPipeline"
      via: "loadDispatchTable reads dispatch-table.json to determine per-wave agent dispatches"
      pattern: "dispatch-table\\.json"
    - from: "agents/*/prompt.md"
      to: "src/engines/aiResearch.js:dispatchAgent"
      via: "loadAgentPrompt reads prompt.md and injects as system message"
      pattern: "readFileSync.*prompt\\.md"
---

<objective>
Split multi-section dispatch entries in dispatch-table.json so each dispatch produces exactly one section (matching ReportSectionSchema's single-object output), then apply targeted prompt audit and fixes for API dispatch compatibility (D-06). Fix CC-specific references, multi-section output instructions, and custom tool unavailability in all agent prompts.

Purpose: Agent prompts were written for Claude Code context where custom tools were available and agents could return arbitrary JSON. Under API dispatch, agents receive only `web_search` tool and must return a single `ReportSectionSchema` object. The dispatch table must have one entry per section so each dispatch call produces exactly one section. Prompts that reference unavailable tools or multi-object returns will confuse the model.

Output: Updated dispatch-table.json with 10 section dispatch entries + synthesis, and 6 updated prompt.md files with targeted fixes. No full rewrites -- per D-06, fix what's broken, leave working prompts alone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-pipeline-integration-prompt-fixes/10-CONTEXT.md
@.planning/phases/10-pipeline-integration-prompt-fixes/10-RESEARCH.md

<interfaces>
<!-- Key context for the executor: what tools ARE available vs. what prompts reference -->

Available in API dispatch (provided by dispatchAgent):
- `web_search` (type: web_search_20250305) with max_uses per agent

NOT available in API dispatch (referenced in prompts but not wired):
- `readFilingSection` -- referenced by annual-reader, quarterly-reader
- `getTranscriptExcerpt` -- referenced by quarterly-reader
- `comparePeers` -- referenced by competitor-evaluator, financial-analyst
- `getMetric` -- referenced by financial-analyst
- `getFinancialLine` -- referenced by financial-analyst
- `computeMOS` -- referenced by financial-analyst

From dispatch-table.json (current pitchDeck section assignments -- NEEDS FIXING):
- business-analyst: dispatched ONCE with sections [1, 2] -- MUST split into two entries
- competitor-evaluator: dispatched TWICE -- sections [3] in wave 1, sections [4] in wave 2 -- OK
- financial-analyst: dispatched with sections [5] in wave 2, and sections [7, 8] in wave 2 -- MUST split [7, 8] into two entries
- management-evaluator: dispatched ONCE with sections [6] -- OK
- risk-analyst: dispatched ONCE with sections [9] -- OK
- valuation-specialist: dispatched ONCE with sections [10] -- OK

After fix: 10 dispatch entries (one per section) + synthesis = 11 total

From ReportSectionSchema:
- Structured output forces SINGLE object return (not array)
- All format fields (citations, searchesPerformed, redFlags) are enforced by schema
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split multi-section dispatch entries in dispatch-table.json</name>
  <files>agents/orchestrator/dispatch-table.json</files>
  <read_first>
    - agents/orchestrator/dispatch-table.json (full file -- understand current wave/phase structure)
  </read_first>
  <action>
    The dispatch-table.json pitchDeck `phases` array currently has agents with multi-section assignments that cannot work with ReportSectionSchema's single-object output. Split these into one-entry-per-section:

    **Phase 1 (Business fundamentals):**
    Current:
    ```json
    { "agent": "business-analyst", "sections": [1, 2], "parallel": true }
    ```
    Split into:
    ```json
    { "agent": "business-analyst", "sections": [1], "parallel": true },
    { "agent": "business-analyst", "sections": [2], "parallel": true }
    ```

    **Phase 2 (Financial deep-dive):**
    Current:
    ```json
    { "agent": "financial-analyst", "sections": [7, 8], "parallel": true }
    ```
    Split into:
    ```json
    { "agent": "financial-analyst", "sections": [7], "parallel": true },
    { "agent": "financial-analyst", "sections": [8], "parallel": true }
    ```

    Leave all other entries unchanged. After this change, the pitchDeck phases should have exactly 10 agent dispatch entries across the 3 phases:
    - Phase 1: business-analyst [1], business-analyst [2], competitor-evaluator [3] = 3 entries
    - Phase 2: competitor-evaluator [4], financial-analyst [5], management-evaluator [6], financial-analyst [7], financial-analyst [8] = 5 entries
    - Phase 3: risk-analyst [9], valuation-specialist [10] = 2 entries
    - PostProcessing: synthesis-writer = 1 entry
    Total: 10 section dispatches + 1 synthesis = 11

    Also apply the same split logic to the onePager section if it has multi-section entries:
    - business-analyst sections [1, 2] -> split into [1] and [2]
    - financial-analyst sections [3, 4] -> split into [3] and [4]

    Write valid JSON. Preserve all existing fields (parallel, note, dependsOn, checkpoint, etc.) on each entry. When splitting, both new entries inherit the original entry's flags.
  </action>
  <verify>
    <automated>node -e "const d=JSON.parse(require('fs').readFileSync('agents/orchestrator/dispatch-table.json','utf8')); const entries=d.pitchDeck.phases.flatMap(p=>p.agents); const singleSection=entries.every(e=>e.sections.length===1); const count=entries.length; console.log('entries:',count,'allSingle:',singleSection); if(count!==10||!singleSection){process.exit(1)}"</automated>
  </verify>
  <done>
    dispatch-table.json pitchDeck has exactly 10 agent dispatch entries, each with a single-element sections array. One dispatch = one section = one ReportSectionSchema object. onePager entries also split. All existing fields (parallel, note, dependsOn, checkpoint) preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix CC-specific references and multi-section output instructions</name>
  <files>agents/valuation-specialist/prompt.md, agents/business-analyst/prompt.md, agents/competitor-evaluator/prompt.md, agents/financial-analyst/prompt.md</files>
  <read_first>
    - agents/valuation-specialist/prompt.md (line 468 CC skill reference)
    - agents/business-analyst/prompt.md (line 583 multi-section output instruction)
    - agents/competitor-evaluator/prompt.md (line 638 multi-section output + comparePeers tool refs)
    - agents/financial-analyst/prompt.md (line 701 multi-section output + custom tool refs lines 341-367)
  </read_first>
  <action>
    **valuation-specialist/prompt.md line 468:**
    Replace:
    ```
    In Pitch Deck mode, the CC skill runs an interactive FGR derivation sub-workflow. Present each of the 5 FGR inputs with specific evidence and structured assessment:
    ```
    With:
    ```
    Present each of the 5 FGR inputs with specific evidence and structured assessment:
    ```
    (Simply remove the CC skill reference sentence. The rest of the FGR derivation instructions are correct and should stay.)

    **business-analyst/prompt.md line 583:**
    Replace:
    ```
    When given a DataPacket and asked to analyze a company, return an array of TWO JSON objects -- one for Section 1 (Company Info) and one for Section 2 (Minimum Standards). Each must conform to the ReportSectionSchema described above.
    ```
    With:
    ```
    When given a DataPacket and asked to analyze a company, produce a single ReportSectionSchema JSON object for the section specified in your Assignment. Your output is structured via the API schema -- focus on the analysis content, not the output format.
    ```

    **competitor-evaluator/prompt.md line 638:**
    Replace:
    ```
    When given a DataPacket and asked to analyze a company, return an array of TWO JSON objects -- one for Section 3 (Market Position) and one for Section 4 (Barriers & Moats). Each must conform to the ReportSectionSchema described above.
    ```
    With:
    ```
    When given a DataPacket and asked to analyze a company, produce a single ReportSectionSchema JSON object for the section specified in your Assignment. Your output is structured via the API schema -- focus on the analysis content, not the output format.
    ```

    **competitor-evaluator/prompt.md** -- comparePeers tool references (lines ~239, 247-278, 379, 494, 586, 655):
    - Line 239: Replace "Use the `comparePeers` tool to extract percentile rankings" with "Use the peer metrics data in your DataPacket (`dataPacket.peerMetrics`) to extract percentile rankings"
    - Lines 247-278 (the "Toolbox Tool: comparePeers" section): Replace the entire section with:
      ```
      ## Peer Metrics Data

      Your DataPacket includes `peerMetrics` with pre-computed metrics for all discovered peers. Use this data directly for quantitative peer analysis:
      - Each peer entry includes gross margin, operating margin, ROE, ROIC, revenue growth, debt/equity, and more
      - Compare across all peers in the DataPacket -- do not limit to 2-3 hand-picked competitors
      - Use web search for additional competitive intelligence not captured in financial metrics
      ```
    - Lines 379, 494, 586: Replace `comparePeers tool` with `peer metrics in the DataPacket`
    - Line 655: Replace "uses `comparePeers` for at least 5 metrics" with "compares at least 5 metrics using DataPacket peer data"

    **financial-analyst/prompt.md** -- custom tool references (lines 341-367):
    - Lines 341-367 (the tool documentation section for getMetric, getFinancialLine, computeMOS, comparePeers): Replace the entire section with:
      ```
      ## Working with DataPacket Financial Data

      All financial data is pre-computed and included in your DataPacket. Access it directly:
      - Growth rates: `dataPacket.growthRates` (earnings, revenue, BVPS, operating cash flow CAGRs across 10yr, 7yr, 5yr, 3yr, 1yr)
      - Return metrics: `dataPacket.returnMetrics` (ROE, ROIC, ROA per year)
      - Financial statements: `dataPacket.financials` (income, balance, cashFlow by year with all extracted fields)
      - FCF: `dataPacket.fcf` (free cash flow, owner earnings per year)
      - TTM data: `dataPacket.ttm` (trailing twelve month figures)
      - Key metrics: `dataPacket.keyMetrics` (P/E, P/B, EV/EBITDA, etc.)
      - Debt metrics: `dataPacket.debtMetrics` (debt/equity, interest coverage per year)

      Refer to the DataPacket Field Paths reference in your user message for exact available fields. Use web search for external context (analyst estimates, industry benchmarks, news).
      ```

    **financial-analyst/prompt.md line 701:**
    Replace:
    ```
    When given a DataPacket and asked to analyze a company, return an array of TWO JSON objects -- one for Section 3 (Meaning/Management KPIs) and one for Section 4 (Growth Metrics). Each must conform to the ReportSectionSchema described above.
    ```
    With:
    ```
    When given a DataPacket and asked to analyze a company, produce a single ReportSectionSchema JSON object for the section specified in your Assignment. Your output is structured via the API schema -- focus on the analysis content, not the output format.
    ```

    Per D-06: "Fix what's broken, leave working prompts alone."
  </action>
  <verify>
    <automated>! grep -r "CC skill\|Claude Code" agents/*/prompt.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "CC skill" agents/valuation-specialist/prompt.md returns NO matches
    - grep "return an array of TWO" agents/business-analyst/prompt.md returns NO matches
    - grep "return an array of TWO" agents/competitor-evaluator/prompt.md returns NO matches
    - grep "return an array of TWO" agents/financial-analyst/prompt.md returns NO matches
    - grep "Toolbox Tool: comparePeers" agents/competitor-evaluator/prompt.md returns NO matches
    - grep "Peer Metrics Data" agents/competitor-evaluator/prompt.md returns a match
    - grep "Working with DataPacket Financial Data" agents/financial-analyst/prompt.md returns a match
    - grep "section specified in your Assignment" agents/business-analyst/prompt.md returns a match
  </acceptance_criteria>
  <done>
    CC skill reference removed from valuation-specialist. Multi-section output instructions replaced with single-section structured output guidance in business-analyst, competitor-evaluator, and financial-analyst. Custom tool references (comparePeers, getMetric, getFinancialLine, computeMOS) replaced with DataPacket direct access instructions. All changes are targeted -- surrounding prompt content is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add DataPacket workaround notes for PSR agent tool unavailability</name>
  <files>agents/annual-reader/prompt.md, agents/quarterly-reader/prompt.md</files>
  <read_first>
    - agents/annual-reader/prompt.md (readFilingSection references at lines 109, 296, 304-338, 484-487, 570-582, 626)
    - agents/quarterly-reader/prompt.md (readFilingSection + getTranscriptExcerpt references at lines 107, 144-148, 257, 329-332, 342-364, 532)
    - agents/annual-reader/config.json (dataPacketSlice to understand what data PSR agents receive)
    - agents/quarterly-reader/config.json (dataPacketSlice)
  </read_first>
  <action>
    PSR agents reference `readFilingSection` and `getTranscriptExcerpt` tools that are NOT available via API dispatch. Per Research Pitfall 2 and the Open Question on PSR Tool Access: for Phase 10's "prove it works" goal, PSR agents will work from whatever filing/transcript content is available in their DataPacket slice. They will not have access to custom tools.

    **This is a known limitation for Phase 10.** The PSR agents will produce analysis based on available DataPacket data (filings metadata, financial statements, transcript availability, company events) instead of reading full filing text. If the DataPacket includes filing markdown or transcript text in its slice, PSR agents can reference it. If not, they produce the best analysis possible from structured data. The prompt changes below make this explicit without removing the tool documentation (which documents the IDEAL workflow).

    **annual-reader/prompt.md:**
    Add a prominent note block at the very beginning of the prompt (after the role description header, before the first section), approximately at the top of the file before line 10:
    ```
    > **API Dispatch Mode:** In this dispatch mode, the `readFilingSection` tool is NOT available. You will work from the financial data, filing metadata, and any pre-loaded filing content in your DataPacket. Analyze what you have -- do not attempt to call `readFilingSection`. Your primary value is verifying DataPacket financial figures against what the filings report and surfacing insights from the data you can see. If critical filing text is missing, note it explicitly in your `primarySourceInsights` array as a data gap.
    ```

    **quarterly-reader/prompt.md:**
    Add a similar note block at the beginning:
    ```
    > **API Dispatch Mode:** In this dispatch mode, `readFilingSection` and `getTranscriptExcerpt` tools are NOT available. You will work from the quarterly financial data, filing metadata, transcript availability info, and any pre-loaded content in your DataPacket. Analyze what you have -- do not attempt to call these tools. Your primary value is identifying quarterly trends, management commentary themes, and verifying DataPacket figures. If critical filing or transcript text is missing, note it explicitly in your `primarySourceInsights` array as a data gap.
    ```

    **Do NOT delete the existing tool documentation sections** in either prompt file. These sections describe the ideal workflow and will be useful when custom tools are eventually wired in. The dispatch mode note at the top is sufficient to redirect the agent's behavior.

    Per D-06: "Fix what's broken, leave working prompts alone."
  </action>
  <verify>
    <automated>grep "API Dispatch Mode" agents/annual-reader/prompt.md agents/quarterly-reader/prompt.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "API Dispatch Mode" agents/annual-reader/prompt.md returns a match
    - grep "API Dispatch Mode" agents/quarterly-reader/prompt.md returns a match
    - grep "readFilingSection" agents/annual-reader/prompt.md still returns matches (tool docs preserved)
    - grep "getTranscriptExcerpt" agents/quarterly-reader/prompt.md still returns matches (tool docs preserved)
    - grep "do not attempt to call" agents/annual-reader/prompt.md returns a match
    - grep "do not attempt to call" agents/quarterly-reader/prompt.md returns a match
  </acceptance_criteria>
  <done>
    PSR agent prompts include API Dispatch Mode notes explaining tool unavailability and redirecting to DataPacket-based analysis. Existing tool documentation preserved for future use. Agents instructed to flag data gaps in primarySourceInsights.
  </done>
</task>

</tasks>

<verification>
node -e "const d=JSON.parse(require('fs').readFileSync('agents/orchestrator/dispatch-table.json','utf8')); const entries=d.pitchDeck.phases.flatMap(p=>p.agents); console.log('pitchDeck dispatch entries:', entries.length, '- all single-section:', entries.every(e=>e.sections.length===1))"
Expected: pitchDeck dispatch entries: 10 - all single-section: true

grep -rn "CC skill\|Claude Code\|return an array of TWO" agents/*/prompt.md
Expected: Zero matches. All CC-specific and multi-section output references removed.

grep "API Dispatch Mode" agents/annual-reader/prompt.md agents/quarterly-reader/prompt.md
Expected: Both files have the dispatch mode note.

grep "Peer Metrics Data" agents/competitor-evaluator/prompt.md
Expected: New section header present.

grep "Working with DataPacket Financial Data" agents/financial-analyst/prompt.md
Expected: New section header present.
</verification>

<success_criteria>
1. dispatch-table.json pitchDeck has exactly 10 section dispatch entries, each with sections array of length 1
2. Zero instances of "CC skill" or "Claude Code" across all agent prompts
3. Zero instances of "return an array of TWO JSON objects" across all agent prompts
4. competitor-evaluator uses DataPacket peer data references instead of comparePeers tool
5. financial-analyst uses DataPacket field references instead of custom tool documentation
6. PSR agents (annual-reader, quarterly-reader) have API Dispatch Mode notes explaining tool unavailability
7. All existing prompt content outside targeted fix areas is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/10-pipeline-integration-prompt-fixes/10-02-SUMMARY.md`
</output>
